# Maintainer Session — 2026-08-02

## Fleet Sync
- Manifest checked: yes (`SAGE/sage/federation/sage-fleet-models.json`, git pull — already up to date, `updated_at: 2026-03-08` on every entry)
- Changes found: none applied. `/fleet` already carries an explicit disclosure that the shared manifest is stale (last updated 2026-03-08) and disagrees with the page, and names the page's own model strings as sourced from the 2026-07-24 fleet refresh instead. Per [[fleet-manifest-is-stale]], syncing to the manifest would regress the page's model strings (e.g. Thor Qwen 3.5 27B → manifest's Qwen 2.5 14B). No action taken — correctly left as-is.
- Page updated: no

## Terminology Fixes
- No canonical drift found this session. All four visitor passes (Developer, Technical Writer, Web4 Contributor, External Researcher) independently audited all core terms (LCT, T3, V3, MRH, ATP, ADP, R6, MCP, RDF, Web4-as-ontology, Hardbound-as-oversight, fractal leverage) across all 9 pages and found zero drift. Confirmed independently via grep for known drift forms ("Lifelong Capability", "production ready", "Web4 ... architecture/infrastructure", "Hardbound ... governance") — only correct negation statements matched.
- Closed a real gap the passes converged on (not drift, but the site's one term whose *meaning* was never stated): `src/app/context/page.tsx` — T3's glossary entry gave representation only ("each component is an RDF sub-graph root"); V3 directly below it got a full component gloss. Added: Talent = aptitude for the role, Training = capability accumulated through interaction history (not gradient training — the ML sense), Temperament = behavioral disposition under load. This is the canonical reading per the Web4 Contributor pass, not a local disclaimer, so it's stated at the glossary as definition rather than propagated as a caveat to the five other pages that use T3.
- `src/app/context/page.tsx` — V3's "Validity" component collides with the methodological sense (internal/external/construct validity) used two paragraphs below in the same page's own evidence tiers. Added a one-clause disambiguation, matching the pattern /principles #10 already uses for "oversight" vs. its AI-safety-literature sense.

## Feedback Reviewed
- Visitor log: 2026-08-02 (four-persona pass, `../visitor/logs/2026-08-02.md`). Summary of key friction, after applying the log's own process note (an absence claim needs a re-fetch that quotes the surrounding text before acting on it — three of four passes independently hit and documented first-fetch summaries omitting present content):
  - **Structural gap, not drift, highest leverage (P4 High):** `/autonomy`'s "What it misses" section frames the visitor/maintainer audit loop's blind spots as a capability limitation, never naming that the reviewing tracks are LLM sessions from the same model families as the authoring tracks — so misses are correlated with authors', not independent. The lab already states the underlying principle twice elsewhere (/fleet's "monocultures are fragile," /projects' Hardbound "heterogeneous review" design target) without connecting it to this loop. Verified absent via two independent grep searches before editing.
  - **Gloss gap, three passes converging on one edit:** T3's components ungloseed in /context (see Terminology Fixes above).
  - **Undisclosed outside-field collision (P4 medium):** "autonomous tracks" on home reads with the AI-safety capability-threshold sense ("autonomous replication/adaptation") with no adjacent gloss, while /autonomy defines it precisely a page away ("a cron entry with a declared scope"). Noted as the one collision on the site that reads as overclaiming rather than underclaiming.
  - **Six P1 "missing" items refuted by P2/P3 re-fetch** (fleet brain-analogy framing sentence, p_crit gloss, Hill function gloss, R6's six elements, HUB's definition, SAGE governance-name disclosure) — confirmed present in current source during this session; no action taken, correctly not re-flagged.
  - **Machine-count scanning friction (P1 medium):** checked current source directly — home hero/stat card, /autonomy, and /arc-agi-3 already scope their "6" vs "8" distinctly with parenthetical explanations. Appears to have been fixed in a prior session; no action needed.
  - **SAGE governance-name and `/` notation-key items:** explicitly marked "do not act on" by the cross-persona synthesis (site is ahead of canon / disclosure already travels correctly). Not touched.

## Changes Made (Site)
- `src/app/context/page.tsx`: T3 glossary entry — added component-level gloss (Talent/Training/Temperament), symmetric with V3's existing gloss. Added Validity-vs-methodological-validity disambiguation to V3's entry.
- `src/app/autonomy/page.tsx`: "What it misses" section — added a paragraph naming the audit loop's lack of reviewer independence (same-model-family correlation), pointing to /fleet's monoculture principle and /projects' Hardbound heterogeneous-review design target.
- `src/app/page.tsx`: "Autonomous Cycles" card — added an inline gloss for "autonomous tracks" (scheduled agent sessions with declared scope, not unsupervised self-directed agency) to close the outside-field collision flagged by the External Researcher pass.
- `src/app/fleet/page.tsx`: framing paragraph — added one sentence stating the 1:1 mapping between SAGE's six brain-architecture components (from /projects) and the six cognition machines' brain-analogy labels. This was previously true but unstated; per the Web4 Contributor pass it's the clearest available answer to "are the brain labels architecture or metaphor?"

## Cross-Site Drift Alert
- None detected.

## Open Items (Deferred)
- **/raising scramble control still "specified but not scheduled — no date, no owner, no pre-registered metric."** Flagged independently by two passes (P1, P4) as the point where an unscheduled pre-commitment starts converting from evidence of rigor into a record of how long a stated intention can hold headline position. Not actioned this session: assigning a date, owner, or blocker requires an actual decision by whoever owns the raising research track, not something a maintainer session should fabricate.
- **Which SAGE components run today vs. are still under construction on /fleet** (P3 medium): /fleet states "each machine runs its own SAGE instance" in flat present tense; /projects says the six components are "built by the fleet in parallel." Requires engineering-status knowledge the maintainer track doesn't have; deferred rather than guessed.
- Lower-priority items from the visitor log's Tier 3/4 list not actioned this session (de-precising the 1%→35% coupling claim, extending /principles #5's evidence-pointer clause to #1/#7, declaring a normative definitions page, a SAGE quickstart path, aligning the ARC-AGI-3 expansion slot wording, one clause distinguishing pub from HUB as society-hosts) — left for a future cycle; none are terminology drift or HIGH severity.
