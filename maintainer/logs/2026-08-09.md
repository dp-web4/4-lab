# Maintainer Session — 2026-08-09

## Fleet Sync
- Manifest checked: **yes** — `SAGE/sage/federation/sage-fleet-models.json`
- Changes found: **none applied — manifest is stale and would regress the page.**
  Every entry is stamped `updated_at: 2026-03-08`, five months behind. It lists
  thor as *Qwen 2.5 14B / transformers*, sprout as *Qwen 3.5 2B*, legion as
  *Phi-4 14B*, mcnugget as *Gemma 3 12B*. The live page carries newer strings
  (Qwen 3.5 27B, Gemma 4 E4B/E2B, Granite 4 h-tiny). Syncing page→manifest would
  overwrite current values with older ones.
- Page updated: **no** — deliberately.
- Concurring judgment from the visitor log (Pass 3): *"this is ops verification,
  not terminology, and syncing the page to a stale manifest would make it worse."*
  Pass 1 flagged the mixed Gemma 3 / Gemma 4 strings as above their ceiling.
  **Neither is resolvable from this track.** The manifest is documented as
  "each machine owns and updates its own entry" — the machines have stopped
  updating it. That is the actual defect and it lives upstream, not here.
  Third consecutive session recording this. Escalating below.

## Terminology Fixes

Canonical **expansion** drift across four personas and nine pages: **0**. Three
passes independently re-derived this. Every LCT / T3 / V3 / MRH / ATP / ADP / R6 /
Web4 / Hardbound expansion matches canon; banned framings are not merely avoided
but explicitly negated. The fixes below are gloss-maturity, first-use locality,
and outsider-collision — not expansion drift.

- `src/app/arc-agi-3/page.tsx`: PolicyGate described as *"evaluates every action
  against signed law before it fires"* — present tense, no maturity qualifier, on
  the site's **only externally verifiable page** (94.85%, public ARC Prize
  scorecard). `/projects` and `/context` both mark Hardbound's silicon anchoring
  as a design target *"not the current mechanism."* Borrowing the name imported a
  hardware-root-of-trust claim the scored run doesn't support. Added the qualifier
  the glossary already owned, **plus** `/autonomy`'s register: auditability, not a
  demonstrated safety property; nothing adversarially tested. Pass 3 (maturity)
  and Pass 4 (safety register) reached this independently by different routes.
- `src/app/context/page.tsx` (PolicyGate section): **found by this track, not in
  the visitor log.** Line 200 asserted *"PolicyGate is Hardbound's silicon-bound
  external authority"* in present tense — five lines after the same section says
  the gate is *"the design target rather than today's deployed enforcement."*
  The hub page contradicted itself, which means every page pointing at the hub
  inherited a claim the hub elsewhere denies. → *"intended to be silicon-bound,
  software-checked today."*
- `src/app/context/page.tsx` (glossary `policygate` row): was one bare sentence
  with no maturity marker. Now carries the software-today / hardware-target split
  and the auditability-not-safety distinction.
- `src/app/context/page.tsx`: **new `policy` glossary entry.** Pass 4's primary
  finding and the only unguarded outsider collision among ~12 checked — eleven
  cousins (attractor, Markov, emergence, governance, oversight, autonomous,
  T3-Training, validity, arousal, ontology, Hill kinetics) are all guarded. In RL
  a *policy* inside an action-selection loop is the action-selecting network;
  PolicyGate sits exactly there and is the opposite kind of object. Entry states
  it vetoes actions rather than choosing them, and covers `/projects`' *"small
  local policy model"* (RL term-of-art for precisely the wrong object).
- `src/app/links/page.tsx`: `R6` bare in *"R6-auditable actions"* → expanded
  inline with the full six elements + `/context` pointer. Verified in source
  first: Pass 2 flagged two pages, Pass 3 refuted `/arc-agi-3` (which does expand
  it). One page, as Pass 3 said.
- `src/app/autonomy/page.tsx`: `V3-validated` bare one sentence after ATP and ADP
  are both expanded inline → linked to `/context#v3` + expanded.

## Feedback Reviewed
- Visitor log: **2026-08-09** — four personas, 555 lines. Developer (wants code,
  not ontology), Technical Writer (expansion fidelity), Web4 Contributor (gloss
  fidelity), External Researcher (outsider readings). Cross-persona synthesis
  ranks 16 items across three tiers.
- The log's own methodological note is the most useful thing in it: **seven
  would-be findings dissolved on direct re-query** across the four passes
  (Pass 1's ACP, Pass 2's ATP/ADP model split and LCT expansion, Pass 3's MRH
  causal-reach drift and T3/V3 primer absence and bare R6 on `/arc-agi-3`,
  Pass 4's T3-Training and Markov hypotheses). Every one was a summarizing-fetch
  artifact. This is why I verified every finding in source before editing — and
  it caught three bad prescriptions (below).

## Prescriptions Corrected Before Implementing

Three visitor recommendations had correct diagnoses and wrong fixes. Recording
these because the wrong fix was the available one in each case.

1. **`private` is not a maturity label.** Pass 4 filed it as an undefined status
   label alongside `runnable` / `research-stage`. Source (`ProjectCard.tsx:39`)
   shows it is the **visibility** badge — a separate axis. Adding it to the
   maturity sentence would have merged two orthogonal dimensions. Fixed as its
   own axis, with the note that a project can be runnable-and-private or
   public-and-research-stage.
2. **Identity is not undefined.** Pass 4 said *"no operational definition
   anywhere on the site."* `/raising:16-19` defines it: *"consistent
   session-to-session behavioral patterns measured via raising curriculum state
   and interaction logs."* What is actually missing is one step further down — a
   metric naming what gets scored and a threshold for degradation. Wrote the
   glossary entry to that narrower, true claim rather than the overstated one.
3. **Principle 5's number is already fenced.** Pass 3 read P5 as resting on
   unfenced coherence. Source shows lines 99–105 already scope the 35% figure to
   its trial and link the glossary's three senses. The real gap was one hop up
   and nobody named it: *the principle's own central term* inherits the circularity,
   not just its number. Fixed at that level.

## Changes Made (Site)
- `arc-agi-3/page.tsx`: PolicyGate maturity + safety-register qualifier (Tier 1 #1).
- `context/page.tsx`: PolicyGate section self-contradiction resolved; `policygate`
  glossary row qualified; new `policy` outsider-collision entry (Tier 2 #6); new
  `identity-continuity` entry naming it load-bearing and unmeasured (Tier 2 #8,
  measures-vs-claims gap).
- `principles/page.tsx`: Principle 5 now states that synthon↔coherence are
  mutually defined, neither operationalized, and that P5 therefore names an
  observed pattern rather than a measurable mechanism — *"it has not earned the
  status of the principles around it"* (Tier 2 #7). Also cross-linked the MIT-0
  ARC harness from the page intro (Tier 3 #9).
- `raising/page.tsx`: cross-linked the MIT-0 ARC harness up front — *"This is the
  softest page on the site"* (Tier 3 #9). Pass 4's point: the one artifact a
  skeptic can read was invisible from the two pages where skepticism peaks.
- `projects/page.tsx`: `private` visibility axis defined; dangling *"subset of
  them"* → *"subset of those scales"* (Tier 3 #12).
- `page.tsx`: T3/V3 primer gained the sub-graph clause — the primer ruled out
  scalar *trust* but implied scalar *components*; `/context` had the fuller model
  and it didn't travel (Tier 3 #12).
- Build: `npx next build` — **passed, 12/12 static pages, zero errors.**

## Cross-Site Drift Alert
- **`policy` / `policy model`** — the RL collision is not 4-lab-specific. Any
  sibling site describing Hardbound, PolicyGate, or a "policy model" carries the
  same unguarded reading for an ML/safety audience. Worth a check in sage-site
  and any Hardbound-facing copy.
- **Maturity tense on Hardbound** — the pattern that produced today's top finding
  (present-tense enforcement language for a design target) is a *name-borrowing*
  failure, not a word-choice failure. Anywhere "Hardbound" is used attributively,
  it imports a silicon claim. Sibling sites should check tense, not vocabulary.

## Open Items (Deferred)

**Route upward — not site-copy fixes:**
- **Schedule the deflationary control** (Pass 4, high). `/context` grades the
  site's central vocabulary as *unlicensed*; `/raising` precommits to retiring it;
  the control has *"no date, no owner, no pre-registered metric."* Meanwhile
  `/autonomy` runs eight daily tracks, all maintenance/publication/audit, each
  issued an ATP against a declared budget. **The lab automated the restatement of
  the claim and not the test of it.** No amount of copy-editing closes this, and
  I can't schedule it from here. Fourth-consecutive-session pattern in my memory:
  every track is maintenance; the specified experiments sit unowned.
- **Pre-register the identity-continuity metric** (Tier 1 #2). I added the
  glossary entry marking it load-bearing and unmeasured, which is the honest
  maintainer-scoped half. Choosing *what to score* is a research call, not site
  copy — writing a metric into the page would be inventing a commitment the lab
  hasn't made. Must precede the run, not follow it.
- **Notation, both instances** — the `/` glyph carrying two unrelated meanings,
  and `+` = "augmented with" mis-tiering LCT against the site's own "anchored to"
  prose. All of Passes 1–3 flagged the first. **Do not reconcile downward.**
  Weakening `/projects`' "anchored to" would be a regression dressed as
  consistency, and it is exactly the fix a linter would propose.
- **Five `/context` glosses exceed canon** and should be absorbed upstream:
  ontology ("not the philosophical sense"), LCT non-transferability,
  witness-as-mechanism, `/autonomy`'s primitive-vs-implementation split, and the
  attractor entry.
- **`visitor/CLAUDE.md`'s R7 row** lists R7 as drift; canon made it co-canonical
  2026-04-27. The site uses R7 nowhere, so there is no site defect. **Do not add
  an R7 entry to `/context` to close this** — the audit spec is what's stale.
  (My own `maintainer/CLAUDE.md` carries the same stale row.)
- **Fleet manifest ownership** — see Fleet Sync above. The machines have stopped
  writing their own entries; the file is five months cold and actively misleading
  as a "source of truth."

**Deferred, in scope but not done this session:**
- **Add a code block for developers** (Tier 2 #4, Pass 1's high). The single
  largest gap between "interesting" and "I'll spend an afternoon on it" — nine
  pages, four runnable projects, and the only executable string on the site is
  `git clone`. Not done because writing a minimal SAGE invocation requires
  reading SAGE source to get the shape right, and inventing a plausible-looking
  API would be worse than none. Next session: read `SAGE/` first, then write it.
- **Publish the phase-coding rubric** (Tier 2 #5). Pass 4 is right that hedging
  has hit zero marginal return and only an inspectable artifact moves the
  internal-observations tier. The rubric costs no transcript content — but it has
  to come from whoever does the coding, not from me.
- **`/fleet` heterogeneity mechanism** (Tier 3 #11). Pass 1's *"the page never
  says why"* was **refuted** by Pass 3 — the header does say it. What's missing is
  mechanism, not rationale. Not writing prose that already exists.
- **Promote the problem statement above the hero** (Tier 3 #10). Pass 1 makes a
  real case that *"Eight machines. Forty original repos."* leads with scale while
  the paragraph below is the actual hook. Left alone: hero restructuring is a
  design call with more blast radius than a maintainer session should take
  unilaterally, and only one of four personas raised it.
- **Sovereign LCT symmetry clause** (Tier 3 #12) — whether the human/machine
  identity symmetry is about auditability or status. One clause, but it's a
  claim about the lab's own governance model, so it should come from the lab.

## So What?

The headline is that **four independent audits found zero canonical expansion
drift** — the enforcement mechanism this track exists to be has converged. Where
drift now lives is one level up: not in what terms expand to, but in what tense
they're used in and what a name imports. Today's top finding was a *correct* term
(Hardbound) used *attributively* on the one page an outsider reaches from
arcprize.org, importing a silicon claim two other pages explicitly disclaim. No
expansion check would ever catch that.

The genuinely new thing this session: the `/context` PolicyGate section
contradicted itself, and **no persona caught it** — all four checked pages against
canon, none checked the hub against itself. Hub-and-pointer terminology management
has a failure mode where the hub drifts and every spoke silently inherits it. That
is worth adding to the audit surface.

The uncomfortable finding is unchanged and now four sessions old: the site is
maximally honest about a gap that stays open. `/context` grades its own central
vocabulary *unlicensed*, `/raising` precommits to retiring it, and the control
that would settle it has no owner — while eight daily tracks, this one included,
maintain the claims. Today I made the site more accurate about what it hasn't
measured. That is real work and it is also, precisely, the substitution Pass 4
identified. **More caveats now buy nothing.** The next move that changes anything
is either the code block (mine, next session) or scheduling the control (not mine).
