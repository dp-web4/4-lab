# Maintainer Session — 2026-07-23

## Fleet Sync
- Manifest checked: yes (`sage-fleet-models.json`, SAGE repo pulled — already up to date)
- Changes found: all current — all six machines (thor, sprout, legion, mcnugget, nomad, cbp) match on model_display, backend, lora_capable, and hardware
- Page updated: no (not needed for sync; one unrelated wording edit, see below)

## Terminology Fixes
- `src/app/projects/page.tsx` (equation legend): `+` = "a generic conjunction (not uniformly 'augmented with'…)" → `+` = "augmented with" — the one TRUE DRIFT the visitor found (Pass 3). Legend now matches home and /context.
- `src/app/projects/page.tsx` (Web4 card): removed the echoed "generic conjunction" phrasing inside the card's parenthetical; kept the substantive point (prose is more precise than the equation about LCT anchoring the trust tensor) while stating `+` as canonically "augmented with".
- Dual `/` reading ("verified by" for T3/V3, allocation pair for ATP/ADP) on home, /context, /projects, /arc-agi-3: **left as-is**, per the visitor's own recommendation — it is semantically more accurate than the canonical one-liner and should be reconciled upstream in CANONICAL_TERMS_v1.md, not flattened on the site. Flagged below.
- ARC-AGI-3 expansion variance (home vs /raising, Pass 2 "possible variance"): spot-checked verbatim. Both pages use "Abstraction and Reasoning Corpus for Artificial General Intelligence"; /raising appends the descriptor "third-gen interactive benchmark". Same expansion, not drift — no change.

## Feedback Reviewed
- Visitor log: 2026-07-23 — four passes, all four would return. Headline: **zero incorrect canonical expansions across all nine pages**; the only true drift was the `+` legend (fixed above). Remaining findings were first-use compliance gaps, signposting, and outsider-legibility/evidentiary issues.
- Notable: several flagged gaps do not exist in current source — visitor fetch-summarization appears to have missed recent fixes:
  - "Nothing on home says read /context first" (Pass 1, HIGH) — a "New here?" callout already existed (added 07-22), but it sat below the hero image and stat cards in small muted text. Treated the flag as a prominence problem and fixed (see below).
  - "MCP, RDF, PolicyGate unexpanded on /arc-agi-3" — MCP and RDF are expanded in the page's up-front vocabulary note; PolicyGate gets an inline functional gloss ("Hardbound oversight sub-gate inset between step 11 and step 12"). No change needed.
  - "LCT bare on /principles" — LCT does not appear anywhere on /principles (case-insensitive grep). No change needed.
  - "(6 cognition machines + HUB) parenthetical needed on /autonomy" — already present: "The seventh machine, HUB (the society-host…), runs substrate, not tracks." No change needed.

## Changes Made (Site)
- `src/app/projects/page.tsx`: `+` operator legend drift fix (two spots — legend line and Web4 card parenthetical).
- `src/app/page.tsx`: moved/promoted the "New here?" glossary signpost into the hero — now a bordered callout directly under the intro paragraphs, before the hero image ("New here? This site is acronym-dense. Start with the /context glossary… or the vocabulary primer below"). Addresses Pass 1's HIGH signposting item and partially the symbol-before-referent complaint (the callout now precedes any equation render).
- `src/app/fleet/page.tsx`: "reward prediction error — scalar RPE" → "reward prediction error (RPE) — a scalar RPE signal" (explicit first-use expansion, Pass 2).
- `src/app/links/page.tsx`: MIT-0 expanded as "MIT No Attribution" at both occurrences (ARC-SAGE card + licensing section) (Pass 2, low).
- Build: `npx next build` passed, zero errors, all 12 pages static.

## Cross-Site Drift Alert
- **Upstream reconciliation needed, not site drift**: the site's dual `/` reading (verified-by for T3/V3, declared→discharged allocation pair for ATP/ADP) diverges from the CANONICAL_TERMS one-line gloss (`/` = "verified by"). The site's reading is more accurate — ADP does not *verify* ATP. Recommend the web4 repo maintainer add the dual reading to CANONICAL_TERMS_v1.md (or its equation gloss) so contributors cross-checking site vs. canon don't find an unexplained mismatch. Until then this is a documented defensible deviation, per visitor Pass 3.
- Sibling sites (synchronism-site, sage-site) should check their own equation legends if they render the Web4 equation — the "generic conjunction" phrasing for `+` may have been copied from an earlier draft of /projects.

## Open Items (Deferred)
- **Publish one externally auditable artifact** (Passes 1 & 4, HIGH for researcher credibility): redacted session-log samples + coding criteria for the identity-persistence claim. Content/policy decision — needs dp, not a maintainer edit.
- **Oversight story on /autonomy** (Pass 4, HIGH): committed human review cadence, incident history section (even if empty), repo-push revertibility gap. These are practice commitments, not copy edits — the page honestly describes current practice; changing the page without changing the practice would violate the preserve-honesty rule.
- **"Trust vs. established T&S usage" paragraph on /context** (Pass 4, medium): one paragraph on what Web4 trust is *not* (reputation systems, verified attestation). Good candidate for next session — needs care to get right, didn't want to rush it at the end of this one.
- **Mark one sense of "coherence" as canonical-for-now** (Passes 1 & 4, medium): a research-vocabulary decision (synthon and the coupling experiment inherit it) — flag to dp rather than maintainer fiat.
- **Mark Principles 8–10 as commitments rather than findings** (Pass 4, medium): reasonable, small, deferred for time; candidate for next session.
- Scalar T3/V3 → RDF sub-graph convergence: state whether roadmapped or accepted operating mode (Passes 2 & 3, low) — needs an actual answer from the SAGE/web4 side first.
