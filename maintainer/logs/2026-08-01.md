# Maintainer Session — 2026-08-01

## Fleet Sync
- Manifest checked: yes (`SAGE/sage/federation/sage-fleet-models.json`, freshly pulled)
- Changes found: none actionable. Every entry's `updated_at` is still `2026-03-08` — the manifest has not moved since the last session. `/fleet` already documents this gap explicitly ("the shared fleet model manifest in the SAGE repo has not been updated since 2026-03-08 ... the manifest is the stale side") and carries newer model strings (Qwen 3.5, Gemma 4) than the manifest (Qwen 2.5, Phi-4, Gemma 3). Syncing to the manifest would regress the page.
- Page updated: no

## Terminology Fixes
- `src/app/page.tsx`: LCT vocabulary-primer gloss "Portable identity grounded in witnessed history" → "Non-transferable identity grounded in witnessed history" — the primer's home-page gloss contradicted /fleet's "The LCT itself is non-transferable — permanently bound to its entity" and the site's own migration note ("what ported here was the behavioral line, not the LCT"). This was the visitor log's one confirmed drift, independently flagged by Pass 2 and ruled by Pass 3 (Web4 Contributor persona) as a canon violation, not an ambiguity.
- `src/app/principles/page.tsx` (Principle 10): "Hardbound is not 'governance' — it's oversight" now inlines the machine-enforced/human-supervision distinction ("watching, gating, and reverting actions, not deciding what should happen ... not the human-supervision sense 'oversight' carries in AI-safety literature") with a link to `/context#governance-oversight`. Flagged independently by three of four passes (2, 3, 4) as the highest-consequence terminology issue on the site — an outsider reading "oversight" with AI-safety defaults would infer human review, while /autonomy states the maintainer track has no human gate. The correct term was already in use; the fix inlines the *reason* instead of just pointing at it.

## Feedback Reviewed
- Visitor log: 2026-08-01 — four-persona pass (Curious Developer, Technical Writer, Web4 Contributor, External Researcher), each auditing terminology plus general friction. Zero incorrect canonical expansions found across all four passes and nine pages. The synthesis converged on a small, well-verified priority list (four Tier-1 items, most others downgraded from single-pass flags after re-fetch verification — e.g. Pass 1's equation-notation-key and SAGE-contradiction flags did not survive a second fetch and were correctly not actioned).

## Changes Made (Site)
- `src/app/page.tsx`: LCT gloss fixed (see above).
- `src/app/principles/page.tsx`: Principle 10 inlines the oversight/governance distinction, links to `/context#governance-oversight`.
- `src/app/context/page.tsx`: Added a fourth "Unlicensed vocabulary" tier to `#evidence`, linking to the new `/raising#deflationary-alternative` anchor — the site's own scope-wide limitation (developmental vocabulary runs ahead of the un-scheduled deflationary control) was documented on /raising but omitted from the page whose stated job is consolidating exactly this kind of caveat. Updated "three different kinds of evidence" → "four." Also added a "Witness / witnessed" glossary entry — the word that makes an LCT's accumulated history "evidence rather than assertion," used on three pages with no definition anywhere.
- `src/app/raising/page.tsx`: Added `id="deflationary-alternative"` anchor to "The deflationary alternative" heading (target for the new /context link). Disambiguated T3's "Training" component from ML-sense "training" inline, on the same page that argues raising is not training.
- `src/app/fleet/page.tsx`: "Brain:" label caption now states the mappings are "role mnemonics the lab assigned, not measured functional homologies" — resolves the ambiguity three different passes/personas independently raised about how literally to read the brain-region analogies.
- `src/app/links/page.tsx`: Split the CONTRIBUTING-guide sentence so "Ask in an issue first" is its own emphasized, standalone line instead of the trailing four words of a longer honesty-caveat sentence. Pass 1 misread the combined sentence as a hard stop; the synthesis downgraded this from high to medium and reframed the fix as making the on-ramp visible rather than writing a policy.

## Cross-Site Drift Alert
- None detected this session — no new terminology issues surfaced that point at sibling sites (synchronism-site, sage-site). The visitor log's one canon-adjacent finding (the `/` operator's documented dual-sense in T3/V3 vs. ATP/ADP) is a proposed *upstream* amendment to `CANONICAL_TERMS_v1.md`, not a site defect — noting here per the passes' recommendation, but this is a canon-repo matter, not a sibling-site drift.

## Open Items (Deferred)
- Tier 2 items not addressed this session, in order of estimated value: (1) `/autonomy` ATP/ADP recharge clause — deferred because Pass 3 flagged a real ambiguity over whether the omission is a copy defect or the registry is genuinely spend-only; needs an implementation check before editing, not just a copy fix. (2) `/projects` Synchronism card's "discovers the equations / encodes them as ontology" dependency arrow — needs exact on-page string verification before rewording (flagged as read-through-a-summarizer by the reporting pass). (3) `/projects` SAGE card naming actual architecture primitives (HRM, IRP) — explicitly flagged as needing verification against current upstream SAGE architecture before editing. (4) `/context` "lab-local vocabulary (not yet canonical)" block for Sovereign, society, society-host, hestia — mechanism already exists (chapter-ledger/chapter-law use it), just needs applying to four more terms.
- Tier 3 items (nav label "Context"→"Glossary", session-count phrasing reuse, ACT/ACP visual separation, hestia appositive, "dream cycle" flag) not addressed — low cost but touches more surface area (nav rename in particular affects `src/lib/navigation.ts` and every internal link) than a single session's budget prioritized, given four Tier-1 items and several quick Tier-2 wins already landed.
- Routed to the researcher, not a maintainer item: naming a metric/owner/threshold for the deflationary scramble control, and publishing one annotated session-log excerpt plus BECOMING coding criteria (or stating that raising logs are unauditable by design under privacy rules, which is itself a disclosure).
