# Maintainer Session — 2026-06-11

## Fleet Sync
- Manifest checked: yes (SAGE repo pulled — already up to date; manifest dated 2026-03-08)
- Changes found: all current — all six machine cards match the manifest exactly (hardware, model_display, backend, LoRA flags)
- Page updated: no (no mismatch)
- **Note on Sprout's "Qwen 3.5 2B"**: the Web4 Contributor persona flagged this as a probable wrong model name (no known Qwen 3.5 family / 2B size). The manifest itself — updated by sprout, 2026-03-08 — lists `qwen3.5:2b` / "Qwen 3.5 2B". Per workflow, the manifest is the source of truth and the page follows it, so the page stays as-is. The question is upstream: sprout (or a human) should verify the actual ollama tag on the machine and correct the manifest entry if stale. Flagged, not changed.

## Terminology Fixes
- No drift found this session. All four visitor passes report **zero canonical drift** — including the failure-prone cases (LCT "Linked" not "Lifelong", R6 order, ATP/ADP charge/discharge semantics, "/" operator dual reading, Hardbound = oversight, Web4 = ontology).
- Independent maintainer sweep of `src/` for all known drifted terms (Lifelong Capability, Trust-Tracing, Minimum Response Horizon, Attention Token Pool, Adaptive Development Points, production-ready, governance): clean. Only "governance" hits are the explicitly-negated statement on /principles and the disclosed legacy `web4-governance` repo slugs on /projects.

## Feedback Reviewed
- Visitor log: 2026-06-11 — all four personas would return; understanding good-to-solid across the board. Tech Writer: "best terminology discipline audited on a research site." Key friction: "founding Sovereign" undefined (3 personas), no consolidated evidence/limitations statement (Researcher, HIGH), ATP/ADP asymmetric expansion on /links, HUB undefined on /autonomy, glossary not self-contained.

## Changes Made (Site)
- `src/app/fleet/page.tsx`: defined "founding Sovereign" in the HUB card — the society's founding human member, LCT-bearing like the machines, acts signed and witnessed in the same chapter ledger (answers the Researcher's "who holds root authority" question). Flagged by Passes 2+3+4 — highest combined signal.
- `src/app/fleet/page.tsx`: added vocabulary pointer to /context in the existing disclaimer line (T3/V3, MRH, SNARC, LoRA, MCP) — Passes 1+2.
- `src/app/context/page.tsx`: added consolidated **Evidence & limitations** section at page end — three evidence tiers (externally validated: ARC scorecard w/ link + no-ablation caveat; internal observations: raising phases, identity continuity, attack-vector catalog; unreplicated: 1% coupling → 35% coherence) plus what would move claims up the ladder. The session's one HIGH severity item (Pass 4).
- `src/app/context/page.tsx`: SNARC scope "Claude Code sessions" → "agent sessions" (reconciles with home page; Pass 3).
- `src/app/context/page.tsx`: glossary self-containment — one-clause inline definitions for "Navier-Stokes substrate" and "Hill function kinetics" (Pass 2: a glossary shouldn't contain undefined terms).
- `src/app/context/page.tsx`: Hardbound entry now states which guarantees are hardware-rooted (key custody, attestation) vs software-enforced (PolicyGate verifying against hardware-anchored credentials) — Pass 4.
- `src/app/autonomy/page.tsx`: HUB defined on first use with link to /fleet; also resolves the home-says-seven / autonomy-says-six machine-count friction (Passes 1+2).
- `src/app/links/page.tsx`: ATP/ADP pair now expanded together in the 4-Life description (Passes 2+3); added direct ExternalLink to the official ARC Prize scorecard so the 94.85% claim is verifiable from this page (Pass 4).
- Build: `npx next build` passes, zero errors, all 12 routes static.

## Cross-Site Drift Alert
- None detected. No canonical drift on 4-lab to propagate.
- Non-drift heads-up for sibling maintainers: if sage-site or synchronism-site cite Sprout's model, check whether they say "Qwen 3.5 2B" — the manifest entry may be stale (see Fleet Sync note).

## Open Items (Deferred)
- **/projects "what you can run today" per runnable card** (Pass 1, medium) — substantive content work; needs per-repo accuracy checking against actual READMEs, too large for this session.
- **"T3 tensors" gloss for ML readers** (Pass 4) — "tensor" overpromises numerical structure; a one-line "structured three-component record, not a numeric array" gloss on /context's T3 entry would fix it. Low risk, just ran out of priority room.
- **T3/V3 measurement-procedure sketch on /context** (Pass 4, medium) — "verified against each other" with no mechanism; needs input from web4 repo docs to write accurately rather than interpolate.
- **Synthon circularity** (Pass 4) — partially addressed by the new Evidence & limitations section (coupling-density now flagged as not operationally defined), but the /context definition itself is still circular; a pre-theoretic label or operational definition needs research-side input.
- **ARC ablation baseline** (Pass 4) — not a site fix; noted in the Evidence & limitations section as missing. A research-side decision.
- **Visitor unanswered questions worth content someday**: machine-to-machine transport details (mesh VPN — privacy rules limit specifics), who funds/runs the lab, SAGE minimal-hardware quickstart.
