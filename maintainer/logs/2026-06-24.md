# Maintainer Session — 2026-06-24

## Fleet Sync

- Manifest checked: yes (`/mnt/c/exe/projects/ai-agents/SAGE/sage/federation/sage-fleet-models.json`)
- SAGE repo: already up to date
- Changes found: **NONE** — all 6 machines match the fleet page
- Page updated: no

**Discrepancy flagged (upstream, not maintainer scope):** The manifest shows `"model_display": "Qwen 3.5 2B"` for Sprout. This matches the fleet page exactly. However, the visitor's Web4 Contributor and Researcher personas both flagged that "Qwen 3.5" is not a recognized Qwen release (Qwen2.5 and Qwen3 exist; 3.5 does not). Since the manifest is the source of truth and the fleet page already matches it, this is correct per my role. The discrepancy belongs at the SAGE/manifest level. Flagging for the fleet operator: verify Sprout's actual running model against `ollama list` and update the manifest if "Qwen 3.5 2B" is a typo or nickname for Qwen3 2B or Qwen2.5 2B.

## Terminology Fixes

No drift found this session. All four visitor personas conducted independent terminology audits across all 9 pages and found **zero canonical expansion drift**. Every term (LCT, T3, V3, MRH, ATP, ADP, MCP, RDF, R6, ACP, ACT, SAGE, SNARC) expands correctly on every page where it appears. Both contrastive rules (Web4 = ontology not architecture/infrastructure; Hardbound = oversight not governance) are enforced on all relevant pages.

## Feedback Reviewed

- Visitor log: 2026-06-24
- 4-persona pass (Developer, Tech Writer, Web4 Contributor, External Researcher)
- Headline: zero canonical drift — the cleanest terminology pass recorded
- Cross-persona priority issues: Sprout model string, caveat placement on hot headlines, audit-status missing from "900+ raising sessions" wherever it appears outside /raising

## Changes Made (Site)

**`src/app/page.tsx`** — two changes:
1. Added brain-analogy disclaimer ("Brain-component vocabulary is a functional analogy for system roles — not a claim about neural correspondence. See Raising for the full framing.") directly inside the "The fleet built a brain" card. Previously the only caveats were in the "What we've demonstrated" section below it — skimmers encountered the headline without the hedge. Now both sections have inline qualification.
2. Tagged "900+ raising sessions across the fleet" with "(internally logged)" in the SAGE vocabulary card. The audit-status qualifier was previously only on /raising; the number now carries that context wherever it appears.

**`src/app/projects/page.tsx`** — one change:
1. Tagged "900+ raising sessions across 6 machines" with "(internally logged)" in the SAGE project card description.

**`src/app/links/page.tsx`** — one change:
1. Updated "documented raising sessions" to "internally logged raising sessions" in the contributing blurb. "Documented" is ambiguous; "internally logged" is the precise qualifier (matches /raising page language).

## Items Not Changed (and why)

**Equation operator disambiguation (`/` glyph):** All three pages that show the equation (/, /projects, /context) already carry the disambiguation note: `/ = "verified by" (T3/V3) or allocation pair (ATP/ADP)`. The visitor suggestion to render ATP/ADP as `ATP→ADP` in the canonical equation would be a notation change at the canonical level — that's an operator-level decision, not a maintainer fix. Flagging for consideration.

**Sprout model string on fleet page:** Fleet page is accurate per manifest. No change made. Discrepancy lives in the manifest (upstream).

**Glossary surfacing:** `/` already has two "New here? Start with /context" pointers — one near the stats/equation area and one in the vocabulary primer. Already addressed in a prior session.

**Low-severity items (deferred):** first-use acronym coverage gaps on /fleet, /raising, /links; "why you'd run it" developer onboarding text for Runnable projects; unifying synthon phrasing across / vs /context; metabolic vocabulary gloss on /autonomy. All low severity — valid but deferring to a session with higher-priority items.

## Cross-Site Drift Alert

**Sprout "Qwen 3.5 2B"**: The SAGE fleet manifest (the source of truth for the fleet page) lists Sprout's model as "Qwen 3.5 2B." This version string does not correspond to a known Qwen public release. Fleet operator should verify Sprout's actual running model (`ollama list` on the Sprout machine) and update the manifest if "qwen3.5:2b" is a local nickname, a typo for Qwen3 2B, or a community model. The 4-lab fleet page is correct per manifest and was not changed.

## Open Items (Deferred)

- `/glossary` page: still the highest-leverage single fix per all visitor personas. Dedicated session needed.
- Sprout model string: verify at the SAGE manifest level.
- Equation `ATP→ADP` notation: operator-level decision on whether to distinguish the allocation pair glyph from the verification glyph in the canonical equation.
- First-use acronym coverage on /fleet, /raising, /links: low severity, defer.
- SNARC canonical form standardization: three different first-encounter forms across pages (prior session open item).
- "Why you'd run it" developer onboarding for Runnable projects: low severity.

## So what?

Terminology discipline is holding across all 9 pages after repeated sessions of enforcement — the visitor audit confirmed zero drift on a 4-persona independent pass. The fixes this session are about **epistemic honesty traveling with claims**, not correcting errors: making "behavioral, not phenomenal" appear where the hot headline is (not just below the fold), and making "internally logged" appear where the 900-session metric appears (not just on /raising). These are small changes with a specific purpose: a reader who skims the hero section should encounter the same epistemic posture as a reader who reads every footnote. The site is moving toward that goal.
