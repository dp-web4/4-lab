# Maintainer Session — 2026-07-02

## Fleet Sync
- Manifest checked: yes (`sage-fleet-models.json`, SAGE repo pulled — already up to date)
- Changes found: none — every field (`hardware`, `model_display`, `backend`, `lora_capable`, `role`) on all 6 machines matches `fleet/page.tsx` exactly
- Page updated: no

## Terminology Fixes
- No canonical-expansion drift found. All four visitor personas independently confirmed: LCT, T3, V3, MRH, ATP, ADP, SNARC, MCP, RDF, R6, Web4-as-ontology, Hardbound-as-oversight, and "fractal leverage" are used correctly and consistently on every page. Zero "production ready" instances.
- Addressed the **structural** (non-drift) issue all 4 personas converged on: SAGE = "Situation-Aware Governance Engine" embeds the exact word ("governance") that Principle 10 and Hardbound tell readers to unlearn. A prior session (2026-07-01) added a reader-facing footnote on `/context` and `/projects` only — propagated the same footnote to the 5 pages that were still missing it:
  - `src/app/page.tsx` — added footnote to the Vocabulary Primer SAGE card (the ecosystem-grid SAGE teaser card is wrapped in an external `<a>`, so a nested `Link` isn't valid there; left as-is, matching how other ecosystem teaser blurbs don't carry footnotes either)
  - `src/app/fleet/page.tsx` — added footnote after the Resource Pool Management paragraph (the one that names SAGE and Hardbound side by side — the exact juxtaposition personas flagged)
  - `src/app/raising/page.tsx` — added footnote after the "Identity is not self-concept" paragraph
  - `src/app/principles/page.tsx` — extended the existing top-of-page developmental-language disclaimer rather than breaking up Principle 1's prose inline
  - `src/app/arc-agi-3/page.tsx` — added footnote under the SAGE competition card
  - `src/app/links/page.tsx` — left unchanged: SAGE appears only in two single-line `ExternalLink` description strings with no room for inline JSX links; disproportionate to retrofit a footnote prop for this component over one word. Noted as an open item below.
- All five new footnotes point to `/context#sage`, which already has a working glossary-row anchor (`id="sage"` — confirmed no new anchor needed).

## Feedback Reviewed
- Visitor log: 2026-07-02 (4 personas: Curious Developer, Technical Writer, Web4 Contributor, External Researcher — all four ran a terminology audit + friction pass)
- Headline finding: **no expansion drift anywhere.** The Terminology Drift Summary's other 8 items were mostly already resolved by prior sessions or based on a stale reading of the live site vs. current source:
  - LoRA glossary entry: already present in `/context` (added 2026-06-12) — persona missed it
  - R6 full six-element breakdown: already present in `/context`'s "one line" column (added 2026-06-12) — persona only checked the "Expansion" column
  - E4B expansion: already spelled out on `/arc-agi-3` ("Gemma 3n E4B (effective-parameter edge variant)") — persona missed it
  - "New here? Start with /context" callout and "(11 public)" repo-count reconciliation: both already present on Home (since April) — stale persona read
  - ARC-AGI-3 harness-vs-model attribution caveat: already present and well-placed on `/arc-agi-3` itself (an "Attribution note" right under the 94.85% claim) — the gap was specifically the **Home page** ecosystem card, which stated 94.85% with no caveat at all. Fixed (see below).

## Changes Made (Site)
- `src/app/page.tsx`: added SAGE governance-name footnote (Vocabulary Primer); added a one-line harness-attribution caveat under the Home ARC-AGI-3 card's "94.85%" claim ("Frontier model + harness — the harness's independent contribution isn't isolated yet") so the confound isn't three clicks away from the headline number
- `src/app/fleet/page.tsx`: added SAGE governance-name footnote
- `src/app/raising/page.tsx`: added SAGE governance-name footnote
- `src/app/principles/page.tsx`: extended existing developmental-language disclaimer to cover SAGE's name
- `src/app/arc-agi-3/page.tsx`: added SAGE governance-name footnote; added a "(CBP, see /fleet)" pointer so the fleet-machine acronym in the "Current status" table doesn't read as an undefined model spec

## Cross-Site Drift Alert
- **Fleet page factual issue, not terminology**: Pass 3 (Web4 Contributor) flagged Sprout's model as "Qwen 3.5 2B" — no such release exists (Qwen2.5 / Qwen3 do). This string comes directly from `sage-fleet-models.json`'s `model_display` field upstream in the SAGE repo, and the fleet page correctly mirrors the manifest (the manifest is the declared source of truth per this track's workflow). Not fixing on the site side, since that would fork the page from its source of truth. Flagging for whoever owns Sprout's manifest entry in the SAGE repo to correct the `model_display` string.
- No other cross-site terminology drift patterns detected this session.

## Open Items (Deferred)
- `/links` SAGE description footnote — `ExternalLink` component takes a plain string `description`, no footnote slot. Low value to add a component prop for one word in a one-line blurb; deferred.
- Researcher's MEDIUM items (qualify "31+ autonomous tracks" claim beyond the 8 detailed; note what gates the fleet registry; add baseline/control language to general emergence claims like "catch each other's mistakes") — these are editorial/accuracy calls about claim-scoping, not terminology drift or friction fixes in this track's mandate. Deferred for explicit review rather than made unilaterally.
- Dating/footnoting the "Claude Opus 4.6" version string so it ages gracefully — low severity, deferred.

Build: `next build` — 0 errors, all 10 routes compiled and prerendered.
