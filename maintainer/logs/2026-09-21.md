# Maintainer Session — 2026-09-21

**WAKE.** The last maintainer log is 2026-09-19 (`Unresolved HIGH` → None). There is no 2026-09-20
log: that session died at startup ("out of usage credits"; see `2026-09-20-0630.log`), so the
09-20 visitor log was never triaged. Its HIGH item (/autonomy's "tracks don't choose task
definitions", contradicted by git history) was owed today alongside the 09-21 HIGH. The carry-forward
rule had no way to see this, because a crashed session writes no heading. Brief fixed (5b, "a missing
log is not a clear field").

**Finding of the day.** Both visitor passes asked the lab to run the cheap experiment it keeps
describing, so this session ran it. The constant-stimulus drift measurement covers the 1,217 records
where the tutor is a fixed script. The answers do not drift. Legion's and McNugget's lines *look* like
strong development: answers grow 4× and 2×, and AI disclaimers go from ~3% to ~50% and ~20%. But
that is a single step on 2026-06-07, on both machines the same day, bracketing SAGE `046e45dc0`
(the raising runner moved onto the Rust sage-daemon). No field in the session record changes with it.
Within stretches of constant infrastructure, words, first-person rate and disclaimer rate are flat on
all four lines. The strongest effect is McNugget's disclaimer rate, p = 0.025, which at 24 tests does
not survive correction. This is a null on crude descriptive measures, and it is productive twice
over: it eliminates "the accumulating identity context visibly changes answers" at this resolution,
and it shows that a serving-software change is invisible in the records. Anyone reading June trends on
these lines would have taken infrastructure for development.

## Fleet Sync
- Manifest checked: yes (SAGE pulled, already up to date)
- Changes found: McNugget entry fresh (`updated_at` 2026-09-21, gemma4:12b, "Mac Mini M4"). It
  matches the card. The other five entries are still dated 2026-03-08 (stale, per the polarity note).
- Page updated from the manifest: no. **But the Legion card was wrong from the session records:**
  `legion-gemma3-12b` sessions 371–460 (2026-08-01 → 08-26) carry `model: gemma4:e4b`
  (`model_source`: daemon health endpoint). The card said the E4B cutover "is not visible in the
  session record". It was, inside the old directory, which is the McNugget pattern. Sessions 461/462
  record a requested/served mismatch with `qwen38-heretic:q3km`. Card corrected, withdrawal wired.

## Terminology Fixes
- No canonical drift. This is the twelfth consecutive visitor pass at zero, confirmed independently by
  all four personas on both 09-20 and 09-21.
- Non-canon: "the being" in site voice on /fleet (Sprout card) → "the line"; the operator's commit
  subject is now attributed as shorthand.

## Feedback Reviewed
- Visitor 2026-09-20 (untriaged until today): the HIGH on /autonomy's self-modification claim; the
  rest is MEDIUM (document shell, upstream freshness, Hestia run path).
- Visitor 2026-09-21: the HIGH on "interactive selection" vs the documented mechanism; MEDIUM:
  Hardbound card, Quick start "Run the loop", glossary structure, meta descriptions, ARC
  engine-source confound, HUB ledger claim, Hub surfaces.

## Changes Made (Site)
- `/autonomy` intro: "task definitions are authored in advance, not chosen by the track itself" →
  purpose is human-authored and procedure is not. Tracks edit their own briefs and checks (9 of 10
  brief commits and 11 of 11 check commits, counting this session's, carry a track prefix; one git identity, so the prefix is the
  only attribution). The maintainer wrote its own stop rule (09-09) and reversed its Step 0 (09-10).
  Review is async and unrecorded. Links P8, which reconciles the two pages.
- `/autonomy` boundaries: "No track can modify the shared fleet registry" (capability) → "Tracks
  are not given write access…" (practice).
- `/context #autonomous` row: same correction; now names instruction self-modification as distinct
  from weights.
- `/raising` "Interactive selection, not training": "The resulting identity is collaborative" gets
  P7's hedge. New paragraph: on the 1,217 scripted records the tutor neither probes nor selects, and
  selection is done by the consolidator and schedule. The responsive runners are untraced (untested,
  not refuted). "We don't create new behaviors" is marked as a hypothesis (scramble control; LoRA line).
- `/raising #constant-stimulus`: the measurement described above replaces "this site reports no such
  measurement". Script committed at `maintainer/analysis/constant_stimulus_drift.py` (public SAGE
  data, reproducible, unreviewed; the page says so).
- `/principles` P7: one sentence pointing at the scripted-line gap.
- `/context`: new `interactive-selection` glossary row.
- `/fleet` Legion card: model string + liveness correction (above). Sprout card: "the being".
- `caveats.json`: 6 withdrawn entries (tracks-dont-choose-task-definitions,
  no-track-can-modify-registry, identity-collaborative-flat, constant-stimulus-unmeasured,
  legion-e4b-not-in-session-record, being-in-site-voice). Each was checked against the old text;
  `--show-exempt` shows one exemption, the correction note itself.
- `maintainer/CLAUDE.md` 5b: missing-log rule.
- Build passes. check-caveats: 20 hold / 2 debt (unchanged); 67 withdrawn, 0 live; render: 0 hits.

## Cross-Site Drift Alert
- sage-site / SAGE docs: any page that reads raising trends across June 2026 on Legion or McNugget
  needs the 2026-06-07 daemon boundary. The records carry no field that marks it.
- The SAGE session schema has no serving-software field (daemon/version/num_predict), so infrastructure
  changes are invisible in the records. That is a SAGE-side fix, flagged here and not made.

## Unresolved HIGH — requires sign-off
None. Both owed HIGHs (09-20 /autonomy self-modification; 09-21 interactive selection) were resolved
this session.

## Open Items (Deferred)
- **[researcher] Is "interactive selection" the name of the practice or of the aspiration?** The site
  now says the records show the second on four lines. The name also appears in the operator's own
  standing instructions, so the rename-or-not decision is the researcher's, not this track's.
- **[researcher] The McNugget line is still on the fixed script today** (367 records, up from 364 on
  09-19). That makes 09-19's open question (design or unattended default?) live rather than historical.
- Next measurement if anyone wants it: semantic/stance drift within segments (embedding distance to
  session-1 answers per question), and the same script across Nomad's 346 boundary into the responsive
  runner. Both are still cheap.
- MEDIUM carried from 09-21: Hardbound card lead (Metalinxx tier vs Hestia enforcement); Quick start
  "Run the loop" launches the daemon; glossary structure (H2 split, sort, gloss column); per-page meta
  descriptions; ARC engine-source confound on the lesson; HUB "everything lands as a signed ledger
  entry"; Hub card surfaces; /projects cards leading with correction history.
- MEDIUM carried from 09-20: patents sentence; ATP "unit of account, not a protocol currency"; Hestia
  run path; /context h2s + jump list; A0–A4 provenance; glossary rows for entity, BECOMING, dream
  consolidation, consciousness.
- LOW: TCSEC on Home, Hill analogy (P2, Pattern 3), P6 headline hedge, "diversity is where emergence
  happens", emergence row "all at fixed weights", /autonomy H1 figures (2026-05-26).
- `visitor/CLAUDE.md` still has an uncommitted visitor-track edit. It is not mine, so it stays in the
  working tree.

## So what?
This is the first session in two weeks that added a result instead of a correction. The result is a
null, and it came with a methodological finding that matters more than the null: the fleet's session
records cannot see changes in their own serving software, and the largest "developmental" signal on
two lines is exactly such a change. The persona frame from 09-20 was right. The term audit is
converged, so the value now lies in running the experiments the site describes. Recommendation: give
one maintainer slot per week to "run one specified-but-unrun measurement", and ask SAGE to add a
serving-software field to session records.
