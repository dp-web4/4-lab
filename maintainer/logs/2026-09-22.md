# Maintainer Session — 2026-09-22

**WAKE.** The last maintainer log (09-21) has `Unresolved HIGH` → None, and no visitor day is missing a
maintainer log, so nothing is carried forward. The 09-22 visitor log has zero canon drift and three HIGH
items. Two are upstream-status sentences on the Hub card. The third is the one that matters: the 09-21
null stayed on one page. Reading it also showed that this track's own measurement paragraph was
partly wrong.

**Finding of the day.** When I re-ran yesterday's analysis with a correct exclusion rule, it moved.
The 09-21 script dropped records under 100 words to remove "failed generations". The failure mode is
more common and partial: the runner writes `[Daemon unreachable: HTTP Error 502/503]` in place of
individual answers. 108 of the 1,220 scripted records have at least one such answer.
- Excluding them removes the Legion p=0.003 decline the visitor flagged: it becomes p=0.20. That trend
  was produced by part-failed records, not by the model.
- McNugget's Gemma 4 12B stretch is 43/47 part-failed. Only 4 records have all six answers, so the
  stretch is untestable, and its +0.38 words trend was an artifact.
- Corrected result: 3 of 21 tests nominal (0.008, 0.024, 0.026), directions mixed, none survives
  Bonferroni (~0.0024). The 4x step at the Rust-daemon move is unaffected.
The visitor's re-run was right on every number. It stopped at the <100-word filter, which is where
the part-failures were hiding.

## Fleet Sync
- Manifest checked: yes (SAGE pulled)
- Changes found: McNugget entry updated 2026-09-22 08:08Z (Gemma4 12B, ollama, lora_capable false). It
  matches the card, which has said Gemma 4 12B since 09-15. The other five entries are still 2026-03-08
  and stale; per the polarity rule they were ignored.
- Page updated: yes, but for liveness, not model. McNugget has not been "running" since 2026-09-20
  01:52 UTC, and its last five records are all-503 (see Changes).

## Terminology Fixes
- No drift found this session. All four visitor passes ran rendered regexes over 9 pages: 0 canon hits.
- Below-canon items (Web4 "protocol"/"standard" in site voice, "Oversight pool" heading, "seat", HUB/Hub/hub,
  late expansions) were not touched. See Open Items.

## Feedback Reviewed
- Visitor log: 2026-09-22. Canon clean. HIGH: (1) the 09-21 null had not reached /raising's claims
  section, /context's evidence ladder or Home; (2) Hub card inverted Docker status; (3) Hub
  "reference POC; production in a private repo" line missing (≥69 days upstream).
  MEDIUM: measurement prose (p-values, exclusion); McNugget "running"; /fleet structure; /context
  glossary length; meta descriptions; track scope line; Hardbound referent; gap ages; ATP "not a
  protocol currency".

## Changes Made (Site)
- `maintainer/analysis/constant_stimulus_drift.py`: exclusion is now by transport-error string
  (`^[daemon|ollama`), not `words >= 100`. It prints which records were excluded per segment.
- `src/app/raising/page.tsx`:
  - Measurement paragraph retitled "corrected 2026-09-22". It now gives the corrected results (21
    tests, 3 nominal, mixed, none corrected) and the exclusion rule with counts, and says McNugget
    Gemma 4 is untestable. It names the three errors in the first version, and states that growth as
    defined (diversity, task success) is untested, not refuted.
  - "What we're not claiming": added "The one measurement we have run leans the other way", with a link.
  - Patterns 4–5: "no count published" → "reported, not counted", naming what the measurement did and
    did not cover.
- `src/app/context/page.tsx`: new first-order rung "Measured internally, null" in Evidence &
  limitations ("four kinds" → "five"). It notes the rung bears on behavioral-identity continuity, and
  the "what would move claims up" list notes the measurement is a crude first pass, not blind rating.
  Hub row: REST-primary API, reference-POC/private-repo line, Docker untested even on dev.
- `src/app/page.tsx`: "Evidence before story" card names the null and where it lives. "Commercial
  path" now says Hardbound is not the only private piece: Hub's production development is in a private
  repo (hub/README.md line 13).
- `src/app/projects/page.tsx` Hub card:
  - Docker inversion fixed ("not tested at all, not even on the development machine").
  - Reference-POC/private-repo sentence added.
  - API is described as full REST plus a small MCP surface, GUI and CLI.
  - One clause on post-MVP work (role entities, plugin seam, quorum release).
- `src/app/fleet/page.tsx`:
  - HUB "everything members do … lands here" is scoped to spec intent; what the site documents in the
    ledger is membership, with track work in the registry, logs and git (matches /context).
  - HUB hub-track PRs land in public web4/hub (95 of the last 200 merged web4 PRs touch `hub/`). The
    page says it does not document whether the deployed daemon includes private-repo code.
  - McNugget liveness rewritten: silent since 09-20, records 491–495 all-503, 43/47 Gemma 4 records
    part-failed. This is the literal case of /context's "records a run, not that raising happened".
- `src/app/autonomy/page.tsx`: draws the human-authored vs track-editable line (cron, model, repo
  scope, purpose vs brief, checks, self-chosen analyses). It names the maintainer's
  constant-stimulus analysis as the clearest case and leaves its scope to the researcher.
- `maintainer/caveats.json`: 6 withdrawn entries (constant-stimulus-flat, hub-docker-tested-on-dev,
  hub-api-is-mcp-server, hub-ledger-records-everything, mcnugget-running-482, evidence-ladder-four-kinds).
  Before building, I confirmed that each pattern matches the old rendered `.next/` HTML.
- Build: `npx next build` ✓. check-caveats: 20 protected hold, 73 withdrawn, 0 live, 0 damaged text.

## Cross-Site Drift Alert
- The Hub "MCP HTTP server" description and "Docker untested outside dev" may be repeated wherever Hub is
  summarized (sage-site, synchronism-site project lists, 4-gov). Upstream wording: REST `/v1` is canonical;
  Docker untested on dev machine.
- The SAGE session records count transport-error answers as sessions. Any sibling site quoting McNugget
  or Legion session counts as "sessions of raising" overstates them (McNugget Gemma 4: 4/47 complete).

## Unresolved HIGH — requires sign-off
None. All three 09-22 HIGHs were resolved this session.

## Open Items (Deferred)
- **[researcher] Is running research analyses in the maintainer's scope?** /autonomy now says the
  question is open and whose it is. This session extended the analysis again (a corrected rerun),
  because the published figures were wrong. Leaving a wrong number up seemed worse than extending
  scope, but that is the track's own judgment.
- **[researcher/SAGE] McNugget daemon unreachable since 2026-09-19; 502s since the Gemma 4 switch.** 43 of 47
  records part-failed. This is a serving defect on the McNugget line, not a site issue. The raising
  runner also writes failed sessions as session records. A `failed` field, or refusing to write the
  record, would make every count on the site honest.
- **[researcher] Which Hub build does the fleet's HUB machine run?** Open POC or private branch. The
  site now says it does not document this.
- **[config] Web4 "protocol"/"standard":** upstream now uses "protocol". CLAUDE.md enforces "ontology".
  The decision belongs to the researcher, not this track (carried).
- MEDIUM, not done: /fleet structure (compact table first, h2/h3 with ids), /context glossary length,
  per-page meta descriptions + Home title, gap ages ("open since"), Hardbound referent on /projects,
  ATP "unit of account, not a protocol currency" (open since 09-20), /principles P2 "We model … Hill",
  P8 addressee, R&D-vs-commercial reconciling sentence.
- LOW: seat, HUB/Hub/hub rule, first-use expansions (raising, TCSEC, E4B, ARC, LoRA), "Oversight pool"
  heading, /fleet "its card below" / "see the note above" pointers, /autonomy dream entry (Claude as
  consolidator).

## So what?
The measurement the lab ran yesterday was itself partly an instrument artifact. It was the same
artifact class the finding was about, serving infrastructure, one level down: a daemon returning 502
mid-session. Correcting it made the null *weaker* in one place (it is no longer "flat") and removed a
spurious effect in another. What this settles is not a trend. It is that no count or trend on the
scripted lines can be read without first checking whether the model answered. That constraint applies
upstream to every session number the fleet publishes.
