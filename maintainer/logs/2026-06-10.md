# Maintainer Session — 2026-06-10

## Fleet Sync
- Manifest checked: yes
- Changes found: all current — page matches manifest for all six machines
- Page updated: no

**Note on Sprout "Qwen 3.5 2B":** The fleet page and the manifest both list Sprout's model as "Qwen 3.5 2B" (manifest field `model_display: "Qwen 3.5 2B"`, ollama tag `qwen3.5:2b`). Pass 3 (Web4 Contributor) flagged this as a nonexistent Qwen release — real lines are Qwen2.5 and Qwen3. However, the manifest is the declared source of truth; the page correctly reflects it. This is a manifest-level issue, not a page-vs-manifest discrepancy. No page change made. The ollama tag `qwen3.5:2b` may reflect how Sprout actually registered the model locally.

## Terminology Fixes
- No drift found this session.

All four personas confirmed: LCT, T3, V3, MRH, ATP, ADP, R6, SNARC, ACT, ACP, Web4, Hardbound, fractal leverage — all correct and consistent across every page they appear on.

The one "DRIFT" flag from Pass 1 (governance on /projects Oversight Plugins) was a **false positive** — confirmed by Passes 2 and 3. Prose uses "oversight"; "governance" survives only in legacy `web4-governance` repo slugs with explicit annotation that they predate the correction and are load-bearing for existing forks. Correct legacy-identifier handling; no change needed.

The LoRA-vs-"raising does not update weights" coherence gap (Pass 4, medium) was **already resolved** in the current page text. `/raising` line 114 and `/principles` line 125 both carry the disambiguation: "some fleet machines run LoRA (Low-Rank Adaptation) adapters for separate fine-tuning tasks — that is distinct from raising, which is always in-context." No action needed.

## Feedback Reviewed
- Visitor log: 2026-06-10 — four-persona pass; no canonical terminology drift; biggest friction was 6-vs-7 machine count ambiguity (all four passes, medium severity); Sprout model name (medium, factual); LoRA clarification (medium, already present); home page ordering suggestions (low).

## Changes Made (Site)

**src/app/page.tsx** (Home):
- Stat card label: "Machines" → "Machines (6 cognition + HUB)" — makes the 7=6+HUB split visible at the stat-glance level without changing the accurate total count
- "Heterogeneous review catches more" card: "6 machines" → "6 cognition machines" — consistent with fleet framing

**src/app/autonomy/page.tsx** (Autonomy):
- Opening bold paragraph: "6 machines" → "6 cognition machines" + added "HUB runs substrate, not tracks." inline — makes /fleet's reconciliation travel to /autonomy
- Timeline supervisors entry: "Six machines, six supervisors" → "Six cognition machines, six supervisors" — consistent wording throughout

Build: `npx next build` passed with zero errors. Pushed to main; Vercel auto-deploying.

## Cross-Site Drift Alert
- None detected.

## Open Items (Deferred)

**Low priority — not addressed this session:**
- "Synthon" absent from /context glossary (Pass 2 flagged; Pass 3 reported it present — likely a fetch artifact; should verify on live site)
- ACP not cross-referenced from /projects ACT card (Pass 2, low)
- ARC-AGI-3 metric on /raising not linked to /arc-agi-3 page (Pass 2, low)
- Benchmark-vs-current Sprout model size (0.8B → 2B) not footnoted on /arc-agi-3 (Pass 3, low)
- Home page ordering: lead with plain-language value before brain-component metaphor (Pass 1 + 4, low-medium)
- "Identity persists across models" claim — metric and n not shown inline on home page (Pass 4, low-medium)
- Sprout manifest model name "Qwen 3.5 2B" — if this is wrong, it should be corrected in the manifest, not the page
