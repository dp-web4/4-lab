# Maintainer Session — 2026-07-18

## Fleet Sync
- Manifest checked: yes (`SAGE/sage/federation/sage-fleet-models.json`, all entries dated 2026-03-08)
- Changes found: all current — Thor (Qwen 2.5 14B, transformers, LoRA), Sprout (Qwen 3.5 2B, ollama), Legion (Phi-4 14B, ollama, LoRA), McNugget (Gemma 3 12B, ollama), Nomad (Gemma 3 4B, ollama), CBP (TinyLlama 1.1B, ollama) all match the page's model/hardware/backend/LoRA props
- Page updated: no (fleet cards unchanged; other fleet-page edits below are unrelated to the manifest)

## Terminology Fixes
- **No canonical drift found this session.** All four visitor passes (including the Web4 Contributor authority pass) report zero drift across nine pages — no wrong expansions, no "Web4 architecture," no "Hardbound governance," no "production ready." Today's work is coverage, collision notes, and one accuracy fix.
- Verified visitor claim before acting: the Tech Writer's report that /raising uses ATP, ADP, R6, MCP, SNARC unexpanded does **not** match current source (grep found none of those terms on the page). Likely stale or hallucinated observation; added a general glossary pointer anyway since that part of the ask was cheap and real.

## Feedback Reviewed
- Visitor log: 2026-07-18 — zero drift; priority items were coverage (glossary pointers, quick-start on /projects), operationalization (T3/V3 unfalsifiable from /context), term collisions (synthon/chemistry, ATP/biology, Markov/probability), and hedge-ordering.

## Changes Made (Site)
- `src/app/context/page.tsx`:
  - **T3/V3 worked numeric example added** (Researcher HIGH: "unfalsifiable from the page"). Grounded in the actual fleet implementation (`peer_trust.py` in the public SAGE repo): per-peer triplet in [0,1] initialized at neutral 0.5, outcome deltas × EMA α=0.1, clamped; success moves Talent 0.500→0.505, timeout drops Temperament 0.500→0.490; reputation = geometric mean; trust is directional. Explicitly framed as the simplified working form vs. the canonical T3-as-RDF-sub-graph spec — the gap is named, not hidden.
  - LCT single phrasing (Contributor): equation key now reuses the glossary's "verifiable digital presence that accumulates witnessed history" line instead of a second near-synonymous definition.
  - governance→oversight anchor: added the rename policy in one sentence — "No rename is planned — retention of load-bearing legacy names is the policy, not an oversight awaiting a fix." (Answers Contributor + Tech Writer's shared unanswered question; restates what the note already implied.)
  - ATP/ADP section: one line stating the biochemistry namesake is a deliberate metaphor, not a claim of biological mechanism (Researcher collision item).
  - Synthon section: "no relation to the synthon of retrosynthetic chemistry (Corey); collision is coincidental" (Researcher collision item).
  - MRH glossary line: "Markov" flagged as gesturing at conditional independence — design intent, not a proven formal property of current implementations (Researcher: name over-promises).
  - Session-count basis: added a definition of "session" (one continuous agent run, cron- or human-initiated, start to termination) before the three-bases caveat (Developer medium / Tech Writer Q4).
- `src/app/projects/page.tsx`:
  - **Quick-start block added** under "Get involved" — SAGE and ACT clone commands duplicated from /links, plus "runs on a single machine — no Jetson required" (Developer HIGH ×2: no run instructions, quick-start hidden on /links; also answers their no-Jetson question).
  - Glossary banner near top: most acronym-dense page now points to /context#glossary (Developer HIGH).
  - FIDO2/CTAP2 expanded on the Hardbound card (Tech Writer medium).
- `src/app/links/page.tsx`: T3/V3/MCP/RDF parentheticals in the Web4 repo description (Contributor low).
- `src/app/raising/page.tsx`: general glossary pointer added near top (Tech Writer medium — coverage, see caveat above about the stale term list).
- `src/app/autonomy/page.tsx`: NotebookLM ("Google's AI research-notebook tool") and Vercel ("hosts and auto-deploys this site") given half-sentence context (Developer low).
- `src/app/fleet/page.tsx`:
  - Identity-portability paragraph reordered hedge-first: "behavioral continuity across substrates — what we shorthand as 'identity transfer'" now leads; the loaded term no longer appears before its hedge (Researcher/Tech Writer).
  - **Accuracy fix**: "Trust starts at zero" corrected to "starts neutral — 0.5 on each T3 dimension in the current tracker." The page contradicted the actual implementation (`_DEFAULT_T3 = 0.5`, "not trusted, not distrusted"). Found while grounding the /context worked example. Links to the new /context example.
- `src/app/page.tsx`: single-trial coupling figure (~1% → ~35%) removed from the landing-page synthon card per Researcher medium; card now says the numbers stay off the landing page deliberately and live with their caveats on Principle 5. (Caveat preserved and strengthened, not weakened — /principles and /context retain the figure with full flags.)
- Build: `npx next build` passes, zero errors, 12/12 pages.

## Cross-Site Drift Alert
- None detected — no drift found here, and no new patterns to flag to sibling sites. Standing note for sage-site and synchronism-site maintainers: the "trust starts at zero" vs. neutral-0.5 discrepancy fixed here may be echoed anywhere the defensive trust model is described — worth a grep for "starts at zero."

## Open Items (Deferred)
- **Human-review cadence for Maintainer/Publisher tracks** (Researcher HIGH): committing the lab to a review SLA is a policy decision for dp, not something the maintainer track should invent for itself — the conflict of interest of this track writing its own oversight terms is exactly what /context's "verification independence" note warns about. Flagged for human decision.
- **Synthon demotion from Principle 5 to "open investigation"** (Researcher medium): changing a named principle's status is a content-governance call, not a friction fix. Landing-page prominence reduced this session; the demotion question goes to dp.
- **External/third-party review of the autonomous loop** (Researcher medium): same category — needs a human to solicit it.
- **Dated roadmap for the deflationary control and ARC ablation** (Researcher/Developer medium): scheduling experiments is outside maintainer scope; both are already honestly flagged as not-run on the site.
- Two-column operator key redesign (Developer medium): current one-line keys on /, /projects, /context are accurate and self-consistent; a layout redesign is cosmetic — deferred as low value-per-risk.
- Tech Writer's "which counting basis is authoritative for the home-page 900+" — the /context evidence section already scopes each number to its page; true reconciliation needs a fleet-side counting standard, not a site edit.
