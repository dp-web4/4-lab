# Maintainer Session — 2026-06-03

## Fleet Sync
- Manifest checked: yes (`/mnt/c/exe/projects/ai-agents/SAGE/sage/federation/sage-fleet-models.json`, SAGE repo already up to date)
- Changes found: **all current** — fleet page matches manifest exactly on all 6 machines
- Page updated: no

Fleet match details:
| Machine | Manifest model_display | Fleet page | Status |
|---------|----------------------|------------|--------|
| Thor | Qwen 2.5 14B (transformers · LoRA) | Qwen 2.5 14B (transformers) · LoRA | ✓ match |
| Sprout | Qwen 3.5 2B (ollama) | Qwen 3.5 2B (ollama) | ✓ match |
| Legion | Phi-4 14B (ollama · LoRA) | Phi-4 14B (ollama) · LoRA | ✓ match |
| McNugget | Gemma 3 12B (ollama) | Gemma 3 12B (ollama) | ✓ match |
| Nomad | Gemma 3 4B (ollama) | Gemma 3 4B (ollama) | ✓ match |
| CBP | TinyLlama 1.1B (ollama) | TinyLlama 1.1B (ollama) | ✓ match |

**Manifest note**: Three of four visitor passes flagged "Qwen 3.5 2B" as a factual error (no such Qwen release exists; lineage is Qwen 2.5 and Qwen 3). However, the manifest's `model_display` and `model` fields both say "Qwen 3.5 2B" / "qwen3.5:2b" (set by sprout). The fleet page correctly reflects the manifest. This requires a manifest-level correction by the sprout machine, not a fleet page edit.

## Terminology Fixes
No drift found this session.

All three terminology-capable visitor passes (Technical Writer, Web4 Contributor, External Researcher) independently confirmed **zero canonical drift** across all 8 pages. Every defined Web4 acronym matches canonical and is self-consistent across pages. The site explicitly names-and-rejects common wrong terms ("not governance," "not architecture," "not Lifelong Capability Token").

Two cleanup items addressed (not canonical drift — number agreement and placement):
- `src/app/autonomy/page.tsx`: "ATPs (Allocation Transfer Packets)" → "ATP (Allocation Transfer Packet)"; "ADPs (Allocation Discharge Packets)" → "ADP (Allocation Discharge Packet)" — canonical noun form is singular; semantics were already correct
- `src/app/projects/page.tsx`: Added "Get involved" section (see Changes Made below)

Additional verifications:
- Web4 "ontology framework" on /projects: NOT present in current code. Projects page shows "Trust-native ontology." — already clean. Technical Writer may have seen a cached/variant render.
- MRH usage on /fleet ("MRH MetabolicBlock bridge", "MRH composer architect"): SAGE architecture legitimately uses MRH as a component name (the Markov Relevancy Horizon module). Usage as a role label within brain-architecture descriptions is accurate. Not a misuse.
- ARC-AGI-3 dual-metric distinction: Already fixed in current code — lines 74–77 read "The 94.85% is the official ARC Prize action score (efficiency-weighted)... The game-solve rate is 96.0% (24/25 games). These are two distinct metrics; the action score reflects action efficiency, not just whether a game was solved."

## Feedback Reviewed
- Visitor log: 2026-06-03 — four-persona pass over all 8 pages
- Terminology result: zero canonical drift (strong positive signal)
- Primary friction: newcomer entry points on /projects (HIGH); jargon without back-links to /context (MEDIUM); two factual model-version questions on /fleet (HIGH/MEDIUM)

## Changes Made (Site)
- `src/app/projects/page.tsx`: Added "Get involved" section at bottom of page. Surfaces SAGE and Web4 issue links and points to /links — addresses HIGH-severity finding that contribution guidance was only discoverable on /links (two pages away from where the Developer persona first looked). Section reads: "SAGE and Web4 are the most active entry points — open an issue, clone a repo, or read the code to see how the pieces fit together."
- `src/app/autonomy/page.tsx`: Fixed ATP/ADP plural to singular to match canonical noun form ("Allocation Transfer Packet" singular, not "Allocation Transfer Packets").

Build: `npx next build` passed cleanly (12/12 static pages). Committed as `ebd4110` and pushed; Vercel auto-deploy triggered.

## Cross-Site Drift Alert
None detected in this session.

Note for sibling sites: Visitor personas did not flag drift in sibling sites. The "Qwen 3.5 2B" manifest accuracy question is internal to the SAGE fleet and does not affect sibling site content.

## Open Items (Deferred)

1. **"Qwen 3.5 2B" manifest accuracy** (HIGH severity, out of scope for this track): Three visitor passes flagged "Qwen 3.5 2B" as a nonexistent Qwen release. The fleet page correctly reflects the manifest. Correction requires sprout to update its own manifest entry to the actual deployed model identifier (Qwen 2.5 or Qwen 3). The fleet page will update automatically once the manifest is corrected.

2. **Acronym anchor-links to /context** (MEDIUM): Visitor, Technical Writer, and Web4 Contributor all flagged that deep-landing readers (arriving at /arc-agi-3, /raising, etc. from external citations) encounter undefined acronyms. Fix: link first use of LCT, T3, V3, MRH, ATP, ADP on each page to their /context anchor. Deferred — scope is broad and requires systematic per-page pass; no terminology correctness risk.

3. **Gemma 3 (deployed) vs Gemma 4 (hackathon repo) clarification** (LOW): /fleet shows Gemma 3 12B/4B; /links references "Gemma 4 Good Submission" repo. /links already has the clarifying description ("Hackathon submission — self-supervising AI with embedded oversight for constrained environments"). /projects already notes "Transfer to Gemma 4 E4B via membot cartridges" as Phase 2. Cross-linking not urgent.

4. **Synthon operational definition** (MEDIUM, Researcher lens): "Synthon" is central to Principles but never operationally defined or falsifiable. Adding an operational criterion ("what observation would count as a synthon forming?") would strengthen credibility for research-audience readers. Deferred — requires domain knowledge to define correctly; not a terminology or factual error.

5. **MRH Markov formalism question** (MEDIUM, Researcher lens): Researcher flagged that "Markov" implies a formal memorylessness property the site doesn't establish. Consider adding a note that "Markov" in MRH is analogical (horizon of what an entity can know/affect at this moment) rather than a strict Markov-chain formalism. Deferred — requires author judgment on how to characterize the intended formalism.
