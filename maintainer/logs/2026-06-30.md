# Maintainer Session — 2026-06-30

## Fleet Sync
- Manifest checked: yes
- Changes found: all current — all 6 machines match manifest exactly (Thor: Qwen 2.5 14B transformers+LoRA, Sprout: Qwen 3.5 2B ollama, Legion: Phi-4 14B ollama+LoRA, McNugget: Gemma 3 12B ollama, Nomad: Gemma 3 4B ollama, CBP: TinyLlama 1.1B ollama)
- Page updated: no

## Terminology Fixes

**Finding**: Zero canonical expansion drift found (confirmed by all 4 visitor personas). The problems were generic-noun leakage and placement gaps.

- `src/app/page.tsx` (×2): SAGE loop step 10 `govern` → `oversee` — bare verb "govern" was the one remaining generic-noun governance leak; proper noun SAGE stays; canonical verb is now "oversee" consistent with Hardbound = oversight posture
- `src/app/arc-agi-3/page.tsx`: SAGE loop step 10 `govern` → `oversee` — same fix
- `src/app/context/page.tsx`: Updated step-10 disambiguation note — previously said "internal 'govern' step"; now says "step 10 ('oversee') is SAGE's own metacognitive self-check" — note stays; label updated to match
- `src/app/projects/page.tsx`: FIDO2 `Fast IDentity Online` → `Fast IDentity Online 2` — canonical expansion was missing the "2"
- `src/app/raising/page.tsx`: Added T3 tensor (Talent / Training / Temperament) alongside V3 — the pair must never travel solo; raising page was missing T3 entirely while V3 was expanded

## Feedback Reviewed
- Visitor log: 2026-06-30 — four-persona pass. Headline: zero incorrect canonical expansions across all 9 pages. Remaining issues were placement/linking gaps (acronyms undefined at first use on non-/context pages), the SAGE step-10 "govern" verb, FIDO2 missing "2", T3/V3 split pair on /raising.

## Changes Made (Site)
- 5 files, 7 lines changed (all substitutions/additions)
- Build: `npx next build` passed clean, 12/12 static pages generated
- Pushed to `main`; Vercel auto-deploy triggered

## Cross-Site Drift Alert
- SAGE loop step-10 "govern" may appear in synchronism-site, sage-site, or other fleet sites that describe the 12-step loop. Maintainers of those sites should search for the bare verb "govern" in loop descriptions and rename to "oversee".

## Open Items (Deferred)

**Not addressed this session — out of scope for a site-content maintainer:**

1. **Acronym first-use linking to /context** (HIGH by visitor count) — requires adding anchor IDs to context/page.tsx per term (`#lct`, `#t3`, etc.) and wrapping first uses on every other page. A larger structural change; good candidate for a dedicated session.
2. **`web4-governance` repo slug drift** — the Oversight Plugins card says "oversight" but backing GitHub repo URLs still contain `web4-governance`. Fixing requires renaming the external repos, which is outside this maintainer's scope.
3. **SAGE "Governance Engine" traveling disambiguation note** — visitors recommended adding "Governance here is the project's established name; the lab's posture is oversight" wherever SAGE is first expanded. Low priority since /projects already carries this note; other pages rely on that page as the canonical source.
4. **ACP/ACT disambiguation** — "Agentic Context Protocol (ACP)" appears in the ACT card alongside ACT; visitor flag as collision risk. But the ACT card's current text explains the relationship ("ACP layers Web4 trust primitives — LCT binding and attestation — over MCP transport; they are complementary, not alternatives"), so this is arguably already addressed.
5. **Plain-English "Start Here" on-ramp** — structural content change, out of scope for terminology-focused session.
6. **Provenance / detect-and-revert documentation** — external researcher friction; policy decision, not site-text drift.

## So What?

The loop step rename from "govern" to "oversee" closes the last channel through which the forbidden generic verb leaked into the site's prose — after this, the word "Governance" appears only inside the SAGE proper noun and the Principle #10 sentence that explicitly rejects it. That's exactly where canon says it's allowed. The site now practices what it preaches on terminology with no residue.

The visitor finding of zero expansion drift across 9 pages is the more important result: the canon is genuinely internalized, not just enforced. Placement and linking gaps remain (every other page still assumes you've read /context), but those are UX friction, not terminology drift. Different problem, different session.
