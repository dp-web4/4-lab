# Maintainer Session — 2026-07-22

## Fleet Sync
- Manifest checked: yes (`sage-fleet-models.json`, all entries dated 2026-03-08; SAGE repo pulled, already up to date)
- Changes found: all current — every MachineCard (Thor, Sprout, Legion, McNugget, Nomad, CBP) matches the manifest on hardware, model_display, backend, and LoRA capability
- Page updated: no (nothing to change)
- Note: Pass 3 (Web4 Contributor) flagged Sprout's "Qwen 3.5 2B" as a nonexistent release. The manifest itself says `qwen3.5:2b` / "Qwen 3.5 2B" (updated_by: sprout). Per workflow, the manifest is the source of truth, so the page stands. The persona's model-release knowledge may simply predate the release — but if `qwen3.5:2b` is a typo in the manifest, that's an upstream fix for Sprout/SAGE, not this site. Flagged in Open Items.

## Terminology Fixes
- No drift found this session. All four visitor passes report **zero canonical drift** across all nine pages — no "Lifelong Capability Token," no "Trust-Tracing-Tensor," no Web4-as-architecture, no Hardbound-as-governance in current voice. Legacy `web4-governance*` slugs and SAGE's name remain documented as pre-correction retention (correct handling per canon).
- Verified pass disagreement on operator-key coverage (synthesis item 5): grep confirms the `/` disambiguation ("verified by" in T3/V3 vs. allocation pair in ATP/ADP) is present at **all** equation displays — home, /context, /arc-agi-3, /projects. Pass 3 was right; Passes 1–2 mis-reported. No action needed.

## Feedback Reviewed
- Visitor log: 2026-07-22 — four passes, all "would return: yes." Key friction: home acronym wall / primer placement (P1 high, P2 medium), Sprout model name (P3, resolved via manifest), external evidence gap — no papers/preprints/log samples (P4 high), session-count bases (P1–P4, deferred), /principles ordering (P1–P2 medium).
- Stale flags noted: P4's "memoriescape" and "did this machine *choose* to..." phrases do not exist in current source; P1/P2's "equation appears several sections before the primer" no longer matches source — the equation sits inside the Vocabulary primer section with definition cards directly adjacent.

## Changes Made (Site)
- `src/app/page.tsx`: added `id="vocabulary"` to the Vocabulary primer heading and an anchor link ("vocabulary primer below ↓") in the top "New here?" line — the valid residue of priority fix #1: a linear reader now gets a one-click jump from the top of the page to the on-page acronym definitions.
- `src/app/autonomy/page.tsx`: "closed loop that improves site quality" → "improves **surface-level** site quality... semantic validation still depends on human review (see the honest assessment below)" — matches the page's own honest-assessment limits (P4 low, trivially cheap, preserves honesty).
- `src/app/principles/page.tsx`: synthon in Principle 1 now carries a forward reference ("an emergent coherence pattern — defined, with its single-trial caveats, in Principle 5 below"); "attractors" in the intro disclaimer now glossed inline ("stable response basins in the probability landscape, per Principle 7") — fixes the two use-before-definition orderings (P1/P2 medium).
- `src/app/links/page.tsx`: added a "For researchers" paragraph before Contact — states plainly that no papers/preprints are published yet, the Synchronism preprint is in late revision (consistent with /projects), non-ARC claims cite internal non-auditable logs, and log samples/coding criteria are unpublished — the actionable slice of P4's high-severity evidence-gap flag, done without overclaiming. Also glossed "coherence equation" on the Synchronism explainer link ("research conjecture, not established physics").
- Build: `npx next build` passes, all pages prerender static, zero errors.

## Cross-Site Drift Alert
- None detected — zero canonical drift on 4-lab this cycle, nothing new to propagate.
- Standing note for sibling maintainers: P4's cross-domain collision list (ATP/ADP ↔ biochemistry, synthon ↔ retrosynthesis chemistry, "Markov" in MRH) applies equally to synchronism-site and sage-site — worth keeping collision caveats adjacent to first use there too.

## Open Items (Deferred)
- **Sprout model name upstream check**: manifest says `qwen3.5:2b`; P3 believes no such release exists. Sprout (or whoever maintains the manifest entry) should confirm the ollama tag — if it's a typo for a Qwen 2.5 or Qwen3 size, fix the manifest and this page follows automatically.
- **Session-count bases** (P1–P4): 91 / 115+ / 120 / "900+" — the fleet page already footnotes that bases differ, but no authoritative basis is named. I don't know which basis is authoritative and won't invent one; needs a human (or fleet) decision.
- **Projects↔Links plugin mapping** (P3 low): whether Oversight Plugins live inside the three forks or in separate `web4-governance*` repos — I can't verify from this repo and won't guess; one line on /links once confirmed.
- **Raising attribution in first paragraph** (P1 medium): the raised-entities vs Claude-in-harness split is already stated prominently in the BECOMING intro (third block); moving it into the opening raising-vs-training paragraph would be off-topic there. Leaving as-is unless flagged again.
- **Scramble control, log-sample publication, Principle 9 scope sentence** (P4): content/policy decisions above maintainer pay grade — need the researcher.
- **Fleet metaphor grounding, exit-page micro-glosses ("trust-native", "cartridges"), glossary anchors/tooltips**: low severity, future session.
