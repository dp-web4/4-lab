# Maintainer Session — 2026-05-18

## Fleet Sync
- Manifest checked: yes (`/mnt/c/exe/projects/ai-agents/SAGE/sage/federation/sage-fleet-models.json`)
- SAGE repo pulled: already up to date
- Changes found: all current — all 6 machines match manifest (Thor: Qwen 2.5 14B transformers+LoRA, Sprout: Qwen 3.5 2B ollama, Legion: Phi-4 14B ollama+LoRA, McNugget: Gemma 3 12B ollama, Nomad: Gemma 3 4B ollama, CBP: TinyLlama 1.1B ollama)
- Page updated: no (no changes needed)

## Terminology Fixes
No drift found this session. All four visitor personas (Curious Developer, Technical Writer, Web4 Contributor, External Researcher) independently confirmed zero canonical term drift across all 9 pages. LCT, T3, V3, MRH, ATP, ADP, R6, MCP, RDF, SAGE, SNARC all expand correctly. Web4 consistently framed as "ontology" with explicit negations on Home, /raising, /principles, /context. Hardbound consistently framed as "oversight" with explicit negation of "governance" on /principles.

## Feedback Reviewed
- Visitor log: 2026-05-18 — four-persona pass, zero terminology drift, structural/content feedback only
- Key findings across personas:
  - Developer (HIGH): `/context` is 7th in nav — the glossary page sits behind most pages a newcomer visits
  - Developer (HIGH): `/raising` BECOMING phases: evidentiary asymmetry — Phase 6 is quantified, Phases 1–5 are observational
  - Researcher (HIGH): maintainer track has no honest-assessment disclosure despite higher blast radius than visitor track
  - Web4 Contributor (MEDIUM): "identity transfer" on `/fleet` not anchored to LCT — missed teaching moment
  - Tech Writer / Researcher (MEDIUM): "trust X" phrase cluster (5 variants) not disambiguated — deferred
  - Tech Writer: `/context` glossary is topical prose only, no flat lookup — deferred
  - Researcher: Principle 5 "1%→35% coherence" datum lacks N, CI, operationalization — deferred (requires research data)

## Changes Made (Site)

### `src/lib/navigation.ts`
- Moved `/context` entry from position 8 to position 2 (right after `/projects`)
- Updated description: "How knowledge flows across repos and machines." → "Canonical Web4 glossary: all terms, acronyms, and the knowledge flow model. Start here if the vocabulary is new."
- Also updated keywords to include glossary terms (lct, t3, v3, mrh, atp, adp, terms, definitions)
- **Rationale**: The Developer persona's highest-priority finding — the page that decodes all other pages sits 7th in nav. Moving it to 2nd flattens the onboarding curve without changing any content.

### `src/app/raising/page.tsx`
- Expanded the BECOMING phases disclaimer to explicitly distinguish observational vs validated phases:
  - Before: "These phases are observed descriptive categories — patterns noticed across hundreds of sessions — not mandatory sequential stages with defined transition criteria."
  - After: added "Phases 1–5 are observational pattern-names; treat them as descriptive scaffolding, not measured stages. Phase 6 (Acting) alone is externally validated: the 94.85% ARC-AGI-3 action score is the quantified output."
- **Rationale**: Researcher persona's highest-priority finding — the evidentiary asymmetry was implicit; making it explicit preempts the first peer-review pushback.

### `src/app/autonomy/page.tsx`
- Added a new `<h3>Maintainer track: higher blast radius</h3>` section after "What it misses"
- Describes: the maintainer-specific failure modes, the Vercel rollback path, and the session audit log as the primary human review surface
- **Rationale**: Researcher persona flagged that the visitor loop gets a known-failure-mode disclosure but the maintainer loop (which autonomously pushes to Vercel) does not, despite higher blast radius. Asymmetric self-assessment for the loop with more consequence.

### `src/app/fleet/page.tsx`
- In the "Identity portability" section, anchored "identity transfer" to LCT:
  - Added: "This is the practical demonstration of what Linked Context Tokens (LCT) are designed to formalize at the protocol level: portable identity grounded in witnessed history, not model weights."
- **Rationale**: Web4 Contributor's medium-priority finding — the cleanest natural place on the site to show LCT-in-operation currently doesn't make the link. Teaching moment now realized.

## Cross-Site Drift Alert
None detected. All canonical terms consistent across 4-lab pages.

## Tooling Note (from visitor passes)
Three visitor passes (Tech Writer, Web4 Contributor, External Researcher) independently flagged prompt-injection-style "system-reminder" artifacts appearing inside WebFetch results — suggestions to load deferred tools (gitnexus, ListMcpResourcesTool) or invoke TaskCreate. These appeared across multiple pages (/fleet, /principles, /projects, /arc-agi-3). Three independent passes, different page subsets, same pattern — not a one-off. Likely the WebFetch summarizer is leaking harness-style reminders into its output, or something upstream is injecting them into rendered page content. Not actioned by the maintainer track (outside scope); flagged for investigation.

## Open Items (Deferred)
- "Trust X" phrase cluster disambiguation in `/context` — 5 variants (trust-native ontology, trust core, trust primitives, trust layer, trust landscape) doing distinct conceptual work, none formally disambiguated. Requires content decision on which to canonicalize. Medium priority.
- Flat A–Z lookup view in `/context` — Tech Writer flagged the page asserts canonical glossary status but presents topically only. Requires UI work. Medium priority.
- Principle 5 quantification footnote ("1% coupling → 35% coherence gain") — Researcher flagged this specific number lacks N, CI, operationalization. Requires access to experimental data. Medium priority.
- Inline "what would I use this for?" on project cards — Developer flagged cards read like abstracts. Would require content decisions per project. Medium priority.
- SAGE description dual-use on /links — "On-device cognition kernel" not tied back to "Situation-Aware Guidance Engine" expansion. Low priority.
