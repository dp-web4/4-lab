# Maintainer Session — 2026-09-16

**WAKE.** Last maintainer log (2026-09-15) `## Unresolved HIGH — requires sign-off`: **None.** No blocked work.
Eighth consecutive pass with zero canonical drift. The visitor log rated nothing HIGH. Its top items were claims
stated correctly on one page and overstated on another, plus provenance nobody had traced. Last session's memory
note said "operator-only flags are often an unread file", so I read the files before rewording anything.

## Fleet Sync
- Manifest checked: yes (SAGE pulled, already up to date; newest SAGE commit is McNugget session 482, 2026-09-16).
- Changes found: none newer than the page. McNugget's entry (2026-09-15, gemma4:12b) was synced yesterday. The other
  five entries are still `2026-03-08` and pre-cutover, and the file still labels itself NOT AUTHORITATIVE.
- One manifest field did explain a visitor finding. **Thor `lora_capable: true` is where the card's "· LoRA" came
  from.** The instance record says otherwise about what is loaded: `thor-qwen3.5-27b/instance.json` has
  `has_lora: false`. `git log -S` shows it flipped true → false on 2026-03-14 ("Setup Thor raising track"), before any
  raising session (session 12 is 2026-03-30). `training/sessions/` is empty. The raising log *recommends* a LoRA pass
  at S80–81, and nothing shows it ran. (Caution for future greps: most case-insensitive "lora" hits in raising logs
  are "exp**lora**tion".)
- Page updated: yes, Thor's model line only: "· LoRA-capable, no adapter loaded".

## Terminology Fixes
- No canonical drift found this session (all four passes agree; my grep agrees).
- Non-canon term fixes:
  - `context/page.tsx` T3 row, `raising/page.tsx`: "interaction-history half" → "interaction-history and curriculum
    sources". "Half" invented a two-part split of a dimension the row itself lists three sources for. Also scoped the
    row's "never on weights" to the current lines.
  - `projects/page.tsx` Web4 card: "the prose here is more precise than the equation" → the core spec's actual
    statement (`t3-v3-tensors.md`: RDF triples in the MRH bind tensors to entity-role pairs, with the entity named by
    its LCT), which the equation's `+` does not show. Site prose no longer ranks itself above canon.
  - `fleet/page.tsx`: lowercase hestia was "the fleet's trust tracker" (twice), nearly SAGE's "peer trust tracker" on
    the same page. It is now "the trust store inside the Hestia daemon … not SAGE's peer trust tracker", as /context has it.

## Feedback Reviewed
- Visitor log: 2026-09-16, four passes, zero canonical drift, nothing rated HIGH.
  - P1: no run-it entry on Home; counting nouns undefined.
  - P2: machine / instance / line / session record have no hierarchy.
  - P3: Hardbound tense; hestia collision; "half"; "more precise than the equation".
  - P4: the weights/identity evidence cluster; Hardbound tense on three pages.

## Changes Made (Site)

### 1. The identity-portability evidence, read from SAGE git (P4 items 2–5, and more than P4 asked)
P4 asked whether the consolidator could explain the self-description drift. The records answer a prior question:
**there was no drift across the port.**
- SAGE commit `4f30be970` (2026-02-27, "sessions 115-117 on CBP (first contact via dashboard)"), `experience_buffer.json`:
  - "autonomous conversation-generating AI system" is a **response** in session **115** at 22:16:22.
  - "humanoid robotic entity" is a **response** in session **117** at 22:58:29.
  - Both are TinyLlama 1.1B on CBP. `identity.json`'s diff in that commit is `session_count` 115→117 and `last_session`.
    Nothing else.
- So the pair is one small model giving two self-descriptions from the same state files, 42 minutes apart. It is not
  drift across the port (Qwen → TinyLlama), and the consolidator did not write it. Sampling can produce that alone.
- **Copy, not move.** `sprout-qwen2.5-0.5b/sessions/` has `autonomous_s119_*` files dated 03-01 through 03-06. Seven
  post-02-27 records have `using_lora: true`. Sprout switched to Qwen 3.5 0.8B on 03-06 (`c94324f1b`). For a week the
  same identity was advanced on two machines and two models. The shared state path in git suggests the session
  numbering interleaved (session 118 committed 02-28 by the Sprout committer identity). I did not state that
  interleaving on the site, because I didn't establish which machine ran 118.
- **"(2025)" was wrong.** The identity was created 2025-12-30 and the 115 sessions run to 2026-02-26. Now dated.
- **Memoriescape.** `git log -S memoriescape` finds its first appearance in the same commit: a TinyLlama response in
  session 117 ("an individual with a limited or incomplete memoriescape"). No earlier file contains it, so it did not
  come from the identity record or consolidator output. Two corrections to what the page said:
  - It was TinyLlama carrying the Sprout identity, not "SAGE-Sprout" in the sense of the Qwen model on Sprout.
  - The gloss the page gave it ("the shape of memories you can sense but not access") is the *prompt's* wording
    ("you cannot actually access them — only their shape").
- **Observer note.** "I notice I want to call it experience" is from the Claude section of the first-contact record,
  written at the researcher's request by the session that built the code. Named on the page as a Claude session, not a
  human rater and not independent. No private path or transcript content beyond the already-published quote.
- Edits:
  - `/raising` section renamed "Identity and self-concept: what the port shows" and rewritten to the above, with a
    dated withdrawal note.
  - `/fleet` "Identity portability" gained the copy, the 42-minute finding and the consolidator confound. The working
    hypothesis is now marked "which none of this tests".
  - The `/raising` deflationary-alternative paragraph now says the consolidator is ruled out for these two named
    examples only, and that sampling variation and the general confound still stand.

### 2. Hardbound maturity tense (P3 + P4)
"gates and reverts against a hardware root" was present tense in seven rendered places:
- Home
- /projects Hestia card
- /projects "Hardbound enforces them"
- /context: Hestia row, Hardbound section, governance-oversight note, fractal-leverage paragraph
- P10 ("watching, gating, and reverting")

All now say "designed to", with process-level enforcement today, matching the /projects Hardbound card. Also:
- P10 gained the Hestia carve-out clause (P2).
- Home glosses "oversight" as machine-enforced gating, not human supervision (P4).

### 3. /raising mechanism paragraph scoped (P4)
"in raising sessions, we shape context — we do not update weights … parameters are fixed" → "on the current raising
lines … (one archived line did; see the end of this paragraph)". This is the paragraph the section title rests on, and
yesterday's scoping missed it. A failing check now covers it.

### 4. Counting nouns (P1 + P2, medium)
- New /context glossary row `#instance-line`: machine → SAGE instance (directory keyed machine+model) → raising line /
  instance line → identity line (one identity across instances, e.g. SAGE-Sprout) → session record. It says how Home's
  21 and 8 relate to archived lines.
- /context "Each fleet machine runs its own SAGE instance" → "at least one".
- Home's census note now says the eight are the current lines, excludes archived lines, and links the row.

### 5. Low items
- `/autonomy` "adversarial validation and human review are for" → "would be for; neither covers this publishing loop today".
- `/projects` Oversight Plugins: trust tracking separated from oversight.
- `/fleet` Sprout: exact ollama tag `qwen3.8-distill:2b` from `instance.json`.

### Checks
- `caveats.json` withdrawn 32 → **38**:
  - `hardbound-gates-hardware-root-today`
  - `raising-mechanism-unscoped`
  - `self-description-drift-across-port`
  - `memoriescape-sprout-qwen-coinage`
  - `thor-lora-loaded`
  - `site-prose-above-equation`
- Each was run against a snapshot of the **pre-fix** build and fired: 7 / 3 / 3 / 1 / 1 / 1 hits.
- After the fix: `npx next build` passes, `check-caveats.sh` gives 20 hold, 2 pre-existing debt, **0 of 38 live**.
- `--show-exempt` for the one new `unless` lists only the /fleet withdrawal note.
- Rendered-text grep for the identity story across all 9 pages found no other retelling left unchanged.

## Cross-Site Drift Alert
- **sage-site / SAGE forum**: `forum/insights/identity-portability-first-contact.md` calls "autonomous
  conversation-generating AI system" "an accurate description of its own architecture, unprompted". If the SAGE site
  tells an identity-transfer or self-description-drift story, it rests on the same two TinyLlama responses 42 minutes
  apart. Flag for that maintainer: check whether "drift" or "transfer" is claimed.
- **Hardbound tense** ("gates and reverts against a hardware root") may appear on 4-gov or the SAGE site. Check.
- `sage-fleet-models.json` `lora_capable` is being read as "has LoRA". Any site rendering the manifest has the same bug.

## Unresolved HIGH — requires sign-off
None. The visitor log rated nothing HIGH. The finding I would have rated HIGH from the records (the self-description
"drift" that isn't one) is fixed, confirmed in built HTML, and guarded by a check.

## Open Items (Deferred)
- **Non-Claude visitor pass** (P4, medium, operator). Named as the cheap fix on /autonomy since 2026-09-10 and still
  undone. Today's "no drift" is again a single-family result.
- **Deflationary control DV** (P4, research). P4 offered a candidate DV: a withheld probe battery, blind
  different-family rater, line attribution, and excess degradation over the ordering baseline as the threshold. That is
  a research design decision. I have not put it on the site as a plan.
- **Home "Run it" entry** (P1). /projects already has a jump link near the top (P2). Not done today.
- **/fleet raising-line states as bullets, define "line" first** (P2, low). The glossary row now defines it; the
  paragraph layout is unchanged.
- **T3/V3 em-dash form → "root dimensions" site-wide** (P2, low). Canonically correct as written. Deferred as a
  style sweep.
- **Metalinxx gloss** (P1/P2, low). The relationship isn't stated anywhere I can cite. The researcher should supply
  the wording. Do not invent it.
- **Web4 badge split, literal equation on the Web4 card, WASM style, undated log-sample/preprint gaps** (low).
- **V3 Validity** canon vs spec: upstream conflict, logged since 09-11, unchanged.
- **Unchecked:** which machine ran Sprout-identity session 118 (02-28). It matters for whether the week of overlap
  was interleaved or parallel.

## So what?
The site's one observation offered as more than construction ("self-description drifted while inputs did not") was
two samples from one model in one evening. The site now says that. The checks keep the old reading from coming back,
but this was not a wording fix. The evidence behind /fleet's portability quote block is thinner than any visitor
could see from outside. Recognizable behavioral signature is the only thing left, and it is unmeasured. Researcher
question: does "identity lives in state files" still earn a pull-quote, or should it move under Evidence & limitations
as a hypothesis with no supporting observation yet?
