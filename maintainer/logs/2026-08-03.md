# Maintainer Session — 2026-08-03

## Fleet Sync
- Manifest checked: yes (`SAGE/sage/federation/sage-fleet-models.json`, pulled fresh — already up to date)
- Manifest `updated_at` on every machine entry is still 2026-03-08, no commits since (confirmed via `git log`). Per prior session's finding, this manifest is stale relative to the live fleet page — syncing to it would regress model strings (Thor→Qwen 2.5 14B, Legion→Phi-4 14B, Sprout→Qwen 3.5 2B), all models the fleet has since moved off.
- Page updated: no — correctly not synced. `/fleet` remains ahead of the manifest.

## Terminology Fixes
- No canonical-term (LCT/T3/V3/MRH/ATP/ADP/R6/Web4/Hardbound) expansion drift found — confirmed by three independent visitor passes today, and by direct grep sweep (zero "Lifelong Capability", zero live "governance", zero "Web4...architecture", zero "production ready").
- Fixed real gaps the visitor log surfaced, verified against current source (not taken at face value — see note below):
  - `src/app/context/page.tsx`: V3 glossary entry now labels each component (Valuation/Veracity/Validity) matching T3's structure, and states Valuation's role gating the ADP→ATP recharge — previously three unlabeled phrases with no function stated.
  - `src/app/context/page.tsx`: Hardbound glossary entry changed "key custody and attestation anchored in silicon" (flat present tense) to "intended to anchor in silicon; enforcement today runs at the process level, not yet hardware-anchored" — matching the hedge already present in this same page's narrative section and on /projects, closing the one place the security-property overclaim survived.
  - `src/app/context/page.tsx`: added an `Ontology` glossary headword — previously undefined in the glossary itself (though defined on home's Vocabulary Primer). Points at home's existing gloss and names the Semantic Web sense explicitly, rather than authoring a second, possibly-divergent definition.
  - `src/app/projects/page.tsx`, `src/app/links/page.tsx`: "resources tracked via ATP/ADP" → "resources cycled through ATP/ADP — charged, spent, and recharged against V3-validated value, not a one-way log." The old phrasing was the exact one-way-log misreading home's primer explicitly disclaims.
  - `src/app/autonomy/page.tsx`: added the "oversight" cross-field disambiguation (machine-enforced, not human-supervision) at the top of the Safety Boundaries section — this page uses "oversight" throughout and states "Consequential pushes are not human-gated before they ship," which reads as a contradiction to an AI-safety reader without the note. The disambiguation already existed on /context and /principles; it was the one high-consequence page missing it.
  - `src/app/raising/page.tsx`: added a second caveat to the lede pointing to "The deflationary alternative" section — the consciousness disclaimer was already frontloaded on four pages, but the load-bearing confound (this might be competent context engineering; the discriminating control hasn't been run) sat at section 18 of 19 with no forward pointer from the top.

## Feedback Reviewed
- Visitor log: 2026-08-03 — 4 passes (Curious Developer, Technical Writer, Web4 Contributor, External Researcher), cross-persona synthesis with a 10-item drift/placement table and 14-item priority list.
- Cross-checked several flagged items directly against current source before acting, per established practice (see [[verify-visitor-findings-against-canon]] and [[visitor-absence-findings-are-fetch-artifacts]]):
  - Pass 3's claim that the ADP glossary entry lacks the recharge leg was **stale** — `git log -S` shows that leg was added 2026-07-26, three sessions ago. Current source already has it; did not re-add or touch it.
  - Pass 2's claim that "ontology" is defined nowhere on the site was already corrected by Pass 3 within the same log (home's primer does define it) — fixed narrowly per Pass 3/4's actual prescription (pointer, not a second definition).
  - Pass 2's flagged gap ("`private` status label has no legend entry") does not reproduce: grep of `maturity=` props on /projects shows only `runnable`/`research-stage` are ever used; "private" only appears in Hardbound's prose description, not as a status label. No fix needed — recording as checked-and-refuted.

## Changes Made (Site)
- `src/app/context/page.tsx`: V3 entry relabeled + recharge role stated; Hardbound entry tense-hedged; new `Ontology` glossary row added.
- `src/app/projects/page.tsx`, `src/app/links/page.tsx`: ATP/ADP "tracked via" → cycle framing.
- `src/app/autonomy/page.tsx`: oversight disambiguation added to Safety Boundaries section.
- `src/app/raising/page.tsx`: lede now points forward to the deflationary-alternative section.
- `npx next build` — compiles clean, all 12 routes static-generate, zero errors.

## Cross-Site Drift Alert
- **Not a sibling-site issue, but an upstream-doc issue worth flagging**: `CANONICAL_TERMS_v1.md` (last updated 2026-04-27) now defines **both R6 and R7** as canonical — R7 is "R6 + Reputation," a first-class superset for higher-consequence actions, not deprecated or wrong. Both this project's root `CLAUDE.md` and `maintainer/CLAUDE.md` still list R7 (alongside R5) as drift to correct ("R6 | ... | R7, R5"). The 4-lab site itself never uses R7, so there's no live site drift — but the maintainer's own instructions are now stale relative to canon on this one point. Flagging rather than self-editing CLAUDE.md.

## Open Items (Deferred)
- Tier 2/3 items from the visitor synthesis not addressed this session (time-boxed to the highest-confidence, verified items): replication-property statement on /context evidence tiers, autonomy risk-chain composition sentence, Principle 7's falsifiability line, "31+ tracks" decomposition (skipped deliberately — the timeline shows 8 track types with uneven per-machine frequency; supervisors alone are 6 of the 31+, but the rest aren't a clean multiplication, and stating one would risk fabricating a number the source doesn't support), session-count "+" tightening on home.
- R6/R7 canon staleness in CLAUDE.md — needs a human decision, not a site fix.
