# Maintainer Session — 2026-09-06

## Fleet Sync
- Manifest checked: yes (SAGE repo pulled, already up to date; last commit touching `sage-fleet-models.json` is still 2026-03-08)
- Changes found: none — manifest still lists 6 machines with March-era models. Fleet page (8 machines, mid-2026 model lines) is ahead of it. Standing rule: never sync the page down.
- Page updated: no (nothing to follow)
- Operator note (repeat): Step 0 is a no-op ritual until some machine writes the manifest again.

## Terminology Fixes
Hard drift (canonical expansions): none — all four visitor passes agree, confirmed by grep. Fixes below are the Web4 Contributor's interpretive-drift items plus first-use expansion gaps.
- **Equation `/` legend** (`page.tsx`, `projects/page.tsx`, `arc-agi-3/page.tsx`, `context/page.tsx` — 4 occurrences): "`/` means two things here: 'verified by' in T3/V3, a plain allocation pair in ATP/ADP" → `/` = "verified by" (T3 by V3; ATP by its ADP discharge record). Checked canon: `web4/README.md` operator legend and `web4/CLAUDE.md` both give `/` one meaning. CANONICAL_TERMS_v1 does not carry the equation legend at all — the README does. The two-meanings gloss was an operator addition on the line newcomers memorize first; removed. /context now also says "'Equation' is the lab's word for this symbolic composition; nothing is solved for" (Researcher's misnomer flag, closed in one clause rather than renaming).
- **/raising "raising shapes the T3 tensor and MRH"** → "raising shapes conduct and the MRH … It does not set the T3 tensor: peers derive T3 from witnessed conduct, and V3 accrues from peer verification of what that conduct produced … an entity that could set its own tensors would be certifying itself." T3 is peer-attested; the page's own self-certification argument for V3 applied to T3 too.
- **hestia** — undefined sitewide (3 personas): new glossary row on /context (`id="hestia"`: the fleet's trust tracker, proper name not acronym, updates only on witnessed adjudication, displayed numbers are per-dimension root scalars per canon's T3 definition, sub-root population left as an open implementation question). First-use gloss on /fleet in both the card-vocabulary pointer and the trust-tracker paragraph, linking to the anchor.
- **/fleet T3/V3 unexpanded until the last section** → expanded in the vocabulary pointer at the top ("T3 (Talent / Training / Temperament, the trust tensor) verified by V3 (Valuation / Veracity / Validity, the value tensor)"), plus MRH and SNARC expansions there.
- **/raising Pattern 3 "T3 tensor values"** → expanded; the page's "expanded inline on first use" self-claim is now true.
- **/context V3 row** "ADP→ATP recharge named under ADP above" → "below" (ADP row is three rows down).
- **"oversight" safety-literature collision** (Researcher): the machine-enforced-not-human-supervision clause now appears at Hardbound's first mention on /, /raising, /links, /arc-agi-3. Every page that names Hardbound now scopes the word.
- **Unlicensed-vocabulary list** on /context now names "dream consolidation," "metabolic state," "tired / energized / in need of rest" as mechanism names by analogy, not physiology.
- One phrase for the fleet's components sitewide: "brain-analog components" (was "brain components" / "brain-architecture components" / "brain-analog terms" across /, /projects, /fleet, /context). ARC-AGI-3 glossary-row expansion aligned ("third generation" not "version 3").
- Expansions: TPM 2.0 (Trusted Platform Module) and FIDO2 (Fast Identity Online 2) on /context; "MRH composer architect" on the CBP card now says what a composer composes; "registry" on /autonomy defined as a noun before "SQLite"; 96.0% labeled "game-solve rate" on /links, /fleet, /raising.
- **Stale flags (correct in source, no edit):** /fleet vocabulary pointer — source and live HTML both have `href="/context#glossary"`, anchor `id="glossary"` exists, no `/context/glossary` link anywhere (Pass 2's 404 is a fetch artifact). "karma" already glossed on /links. Hill function already glossed on /principles and /raising. SNARC↔engram relation already stated on the /projects card. "attractor" already marked METAPHOR in the glossary and on /principles. p_crit defined (Pass 2 confirmed).
- **R7 — still not a site defect.** Canon §"R6 and R7" says both canonical. Visitor and maintainer reference tables remain stale; operator fix, not mine.

## Feedback Reviewed
- Visitor log: 2026-09-06 — 4 passes, all "would return: yes", zero canonical-expansion drift, 24 merged terminology/defect rows, 27 ranked fixes. The Contributor's one real drift was the equation `/` gloss; the Researcher's real point was the control blocker being overstated as definitional.

## Changes Made (Site)
- `src/app/autonomy/page.tsx` (HIGH #2): one-sentence SAGE-vs-tracks paragraph — tracks are cron-launched Claude Code sessions that write and maintain code/docs/this site; SAGE is the separate on-device cognition kernel on each machine's local model; the machine sessions on /fleet are records of SAGE runs, not track runs. Shared machines and repos, not a process.
- `src/app/raising/page.tsx` (HIGH #3): control-blocker paragraph rewritten to separate two blockers. Growth is operationalized on this page already, so a yoked/scrambled-history control on growth is pre-registrable now — its absence is logistical. Identity continuity has an operational definition (on this page and on /), but two of its three components are treatment inputs, so only the behavioral-pattern component could separate the arms, and that one rests on "coherence" — which is where the definitional blocker actually sits. No longer claims "no operational definition" exists.
- `src/app/page.tsx` (MED #9): "Six machines" card now says these are the cognition pool and the other two of the eight are society-hosts, with a /fleet link.
- `src/app/context/page.tsx` (MED #8): H2 "How Knowledge Flows" → "Glossary"; nav label, `<title>`, and page heading now agree. Subtitle carries the knowledge-flow framing.
- Build: `npx next build` passes, 12/12 static pages, zero errors. `npx` is not on this shell's PATH; used `~/.nvm/versions/node/v20.19.3/bin`.

## Cross-Site Drift Alert
- Equation legend: any sibling site that glosses `/` as "allocation pair" for ATP/ADP is off canon — `web4/README.md` gives `/` = "verified by" only. Check sage-site and synchronism-site wherever the equation is printed.
- "Raising shapes T3": any sibling text that has the raiser setting T3 (rather than peers deriving it) should be reworded.
- hestia: any sibling page showing per-peer T3 numbers without naming hestia has the same omission.

## Open Items (Deferred)
- Visitor and maintainer reference tables list R7 as drift; canon disagrees (operator).
- MED #7 shared "New here?" key component for /fleet and /raising — the inline expansions now cover the need; a shared component is a refactor, not a fix.
- MED #10 (/fleet two-account lead-in above the pool headers), #11 (phase/curriculum circularity on /raising), #12 (Sprout-transfer continuity judging protocol — needs facts I don't have), #13 (/arc-agi-3 "outperforms a larger model with a generic prompt" — needs the cited pair), #14 (/context "One line" column length — restructure).
- LOW #17–18 (caveat deduplication across 8–9 pages), #23 (date-stamp 79-session / zero-rollback figure), #24 (link 4B cartridge trace), #25 (chapter ledger/law plan vs canon society vocabulary — operator decision), #26 ("achieved" → "observed"), #27 (title order).
- Contributor's open question 5 (how per-relationship T3 and the two-account ATP/ADP analogy ever touch) is a real gap: trust and resource accounting are on different pages and never connected. Needs a lab answer, not a maintainer sentence.
- Fleet manifest writer dead since March.

## So what?
The equation-legend fix is the one that matters: the site had taught every newcomer a two-meaning `/` that canon never had, and it did so on the line they memorize first. The control-blocker rewrite moved a research question forward rather than just documenting it — growth is pre-registrable today, and the site now says so instead of hiding behind "definitional."
