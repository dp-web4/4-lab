# Maintainer Session — 2026-06-12

## Fleet Sync
- Manifest checked: yes (`sage-fleet-models.json`, SAGE repo pulled — already up to date)
- Changes found: all current — every machine's hardware/model/backend/LoRA/role matches the page
  (Thor: Qwen 2.5 14B transformers+LoRA · Sprout: Qwen 3.5 2B ollama · Legion: Phi-4 14B ollama+LoRA ·
  McNugget: Gemma 3 12B ollama · Nomad: Gemma 3 4B ollama · CBP: TinyLlama 1.1B ollama)
- Page updated: no (nothing to update)

## Terminology Fixes
- No canonical drift found this session — visitor synthesis confirms zero drift across all 4 passes / 9 pages.
- One definition-precision alignment against `CANONICAL_TERMS_v1.md`:
  - `src/app/context/page.tsx`: V3 Validity gloss "whether it applies in this context" → "whether its reasoning is sound" (canon: "How sound is the reasoning?")
- Canonical-source observation: canon v1.0 defines **R7 as a canonical superset of R6** (consequence-tier
  distinction, both first-class). The maintainer-track config still lists R7 under "NEVER use" — that rule is
  stale relative to the governing document. Site only uses R6, which remains correct; no site change needed,
  but the track config should be updated to match canon.

## Feedback Reviewed
- Visitor log: 2026-06-12 — zero terminology drift; all findings are definition-quality/placement issues.
  Priority items: /context not structured as a glossary (HIGH, all 4 personas), MCP/RDF roles unexplained
  (3 personas), raising evidence not inspectable (HIGH, Pass 4), coherence metric unoperationalized,
  canonical terms doc not cited, equation tokens unlinked.
- Several flagged items were already fixed in source before this session (observer-note framing on /raising,
  ACP expansion-at-first-use on /projects, MCP/RDF primer cards on home) — visitor flags partially stale
  against the 06-11 deploy.

## Changes Made (Site)
- `src/app/context/page.tsx`: added "Glossary at a glance" — scannable table (term / expansion / one-line
  definition) for 21 terms, each row carrying an `id` anchor (`#lct`, `#mcp`, `#mrh`, …) so every page can
  deep-link at point of use. Links the public canonical source
  (`github.com/dp-web4/web4/blob/main/docs/reference/CANONICAL_TERMS_v1.md`) with "the document governs."
  (Visitor priority fixes #1, #5)
- `src/app/context/page.tsx`: added MCP/RDF role paragraph to the equation walkthrough — MCP as the
  interaction surface, RDF as the semantic graph substrate. (Priority fix #2, flagged by 3 personas)
- `src/app/context/page.tsx`: Evidence & limitations section now has `id="evidence"`; unreplicated-result
  caveat extended to state that both variables (coupling density AND the coherence measure) are defined
  only within the producing experiment.
- `src/app/page.tsx`: Web4 equation tokens are now dotted-underline links to their /context anchors.
  (Priority fix #6)
- `src/app/raising/page.tsx`: "Key discoveries" section gained an evidence-status note linking
  /context#evidence — internal session logs, documented and dated, not externally audited, no published
  samples. Honest version of priority fix #3: privacy rules forbid publishing session transcripts, so the
  inspectable-evidence gap is named rather than papered over.
- `src/app/principles/page.tsx`: Principle 5's 35%-coherence claim now states both variables are
  experiment-internal, linking /context#evidence. (Priority fix #4 — did not invent a coherence metric
  that doesn't exist; labeled the gap instead.)
- Vocabulary deferral links on /, /fleet, /arc-agi-3, /links now deep-link `/context#glossary`
  (fleet's LCT mention links `#lct`). (Pass 2's site-wide anchor-link finding)
- Build: `npx next build` passed, zero errors, 12/12 pages.

## Cross-Site Drift Alert
- None detected (zero drift on 4-lab this cycle).
- Worth flagging to sibling maintainers anyway: canon defines V3 Validity as "How sound is the reasoning?" —
  the "applies in this context" paraphrase existed here and may have been copy-pasted to sage-site or
  4-life. Check V3 glosses there.
- R6/R7: if any sibling site claims "R7 is wrong / only R6 is canonical," that's now contrary to canon v1.0,
  which defines both as canonical with a consequence-tier distinction.

## Open Items (Deferred)
- Raising evidence artifacts (Pass 4 HIGH, full version): publishing redacted log samples + coding criteria
  requires a human decision on redaction policy — site now names the gap and what would close it, but the
  artifacts themselves are not a maintainer-track call.
- Anthropomorphic-tone items (Pass 4 medium): "The model is weather. The identity is organism." already
  carries a point-of-use caveat; further toning is an editorial/voice decision, not a correctness fix —
  deferred to human review per "preserve honesty, don't weaken" constraint (cuts both ways).
- ACT framing differs between /projects and /links (Pass 3 low); /links internal gloss inconsistency
  (Pass 2 low); /autonomy capitalization + timeline format nits (Pass 2 low); Nomad role description
  thin (Pass 3 low); ATP/ADP ledger-vs-framing clause on /autonomy (Pass 3 low — needs a factual answer
  about whether track ATP/ADPs land in the chapter ledger; don't guess); ARC public-set qualifier
  placement (Pass 4 low). Batch for a slow session.
- Architecture/flow diagram on /context (Pass 1 medium): real gap, but a diagram asset is bigger than a
  maintainer pass; flag for a design session.
