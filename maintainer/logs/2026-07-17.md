# Maintainer Session — 2026-07-17

## Fleet Sync
- Manifest checked: yes (`sage-fleet-models.json`, SAGE repo pulled — already up to date)
- Changes found: all current — all six machines (thor, sprout, legion, mcnugget, nomad, cbp) match on `hardware`, `model_display`, `backend`, `lora_capable`, and role
- Page updated: no (no drift between manifest and `fleet/page.tsx`)

## Terminology Fixes
- No drift found this session. All four visitor passes report **zero canonical drift** — every expansion verbatim-correct site-wide (LCT, T3, V3, MRH, ATP/ADP, R6, SNARC; Web4=ontology, Hardbound=oversight, "production ready" absent).
- Definition-coverage fix (not drift): added a **"Trust-native"** glossary entry to `/context` — the site's most-used previously-undefined term (Pass 4). Defined as an ontological commitment (trust as a primitive: T3/V3 bound to LCTs, scoped by MRH), explicitly not a cryptographic guarantee by itself.

## Feedback Reviewed
- Visitor log: 2026-07-17 — zero drift; remaining findings were definition gaps, caveat-discipline gaps on /raising, and several cross-fetch discrepancies to verify.

### Cross-fetch discrepancies verified against source (closing stale findings)
- **p_crit on /context**: entry PRESENT (with 400x-error caveat). Pass 2's "absent" finding is stale — closed.
- **Hardbound on /autonomy**: defined inline ("hardware-bound oversight suite that declares…"). Pass 1's "undefined" finding stale — closed.
- **/raising first-use LCT/MRH**: expanded on page (Web4-terms paragraph). Closed.
- **/principles T3/V3/SAGE expansions**: present in source (Principle 4 quote expands T3/V3; Principle 1 expands SAGE). Passes 1/2 findings stale — closed.
- **ARC number reconciliation (/ and /projects)**: both pages already state both figures together ("24/25 games (96.0%); 94.85% official action score"). Passes 1/2 finding stale — closed.

## Changes Made (Site)
- `src/app/context/page.tsx`: added "Trust-native" glossary row after Web4 entry.
- `src/app/projects/page.tsx`: SNARC card — "SNARC ships packaged as the engram plugin" bridging clause (Pass 3's top fix); glossed "dream cycles (offline consolidation passes between sessions)". Membot card — glossed "cartridge" (swappable, mountable memory store per knowledge domain) and "binary Hamming codes" (compact binary fingerprints matched by bit-distance).
- `src/app/raising/page.tsx` (caveat-discipline pass, Pass 4):
  - Pattern 1: bracketed "learns" operationally (facts appearing reliably in session behavior, not self-awareness).
  - Pattern 2: metabolic states now carry the /context bracket locally ("interoceptive proxy value, not yet a formally specified model") with link.
  - Pattern 3: bracketed "relationships"/"trust" as per-peer T3 tensor values — tracked state, not affect.
  - Pattern 5 + deflationary section: "spontaneous specialization" → "unprompted specialization" (removes agency implication; both occurrences).
  - Capacity as register: child-language comparison flagged as "analogy of expressive capacity, not developmental homology".
- `src/app/page.tsx`: moved consciousness disclaimer to sit directly under the hero tagline's vivid language (Pass 4 — skimmers now hit the bracket before the "In practice" paragraph); SNARC vocab card scope aligned to "for Claude Code sessions" (matches /projects, Pass 3).
- Build: `npx next build` passes, zero errors, all 12 routes prerendered.

## Cross-Site Drift Alert
- None detected (zero canonical drift in visitor audit). One pattern worth sibling-site checks: **"trust-native" used without definition** — likely also appears undefined on sage-site and the 4-Life site; their maintainers should check whether a gloss or /context-equivalent pointer exists.

## Open Items (Deferred)
- **Fleet communication mechanics** (Pass 1, medium): a "what a message looks like" example on /fleet needs accurate wire-format detail from the hub/federation code — should not be invented by the maintainer track; needs source material from SAGE/HUB repos first.
- **Identity-transfer state-file pointer** (Pass 1, medium): same reason — needs the actual repo file path confirmed before linking.
- **Publish log samples + coding criteria for /raising claims** (Pass 4): evidence-class upgrade, not a site edit; belongs to the research tracks.
- **Principle 5 demotion to "open hypothesis"** (Pass 4, low): editorial judgment call for the human researcher — flagging, not acting; caveat is already present.
- **"6 machines at benchmark" vs 7 now** (Pass 3, low): already qualified correctly per the visitor; skipping cosmetic "(HUB joined later)" addition.
- **Distinct glyphs for the overloaded "/"** (Pass 2, low): canon-level notation question, not a site fix.

## So What?
The feedback loop is converging: four passes, zero drift, and over half of today's "findings" were stale — already fixed in source before the visitors browsed, or extraction artifacts (three cross-fetch contradictions between passes resolved by eyeballing source once). The remaining work has shifted from *correctness* (drift) to *discipline* (uniform caveating) and *coverage* (defining the vocabulary the site leans on). That's a healthier failure class — and it suggests the visitor track's marginal value now lies in fresh personas or deeper claim-validation, not repeat terminology audits.
