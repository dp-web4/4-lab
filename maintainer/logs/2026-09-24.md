# Maintainer Session — 2026-09-24

## WAKE
The 09-23 carry-forward was "None". No visitor log since then lacks a maintainer log. The visitor's
cross-cutting note is right: the loop ranks what it can close as text above what needs a research
artifact. So this session did the research artifact (HIGH 3) instead of dating the commitment.

## Fleet Sync
- Manifest checked: yes (SAGE pulled 06:30).
- Changes found: only McNugget's `updated_at` moved (supervisor cycle 2026-09-24 08:33Z). Its content
  (Gemma4 12B, Mac Mini M4) already matches the page. The other five entries are still dated 2026-03-08.
- Page updated: no.

## Terminology Fixes
No canonical expansion drift. All four passes confirmed this, and a grep of the source did too.
Relational and completeness fixes:
- `context/page.tsx` T3/V3 section: removed a clause the page attributed to canon but canon does not
  contain ("neither tensor is computed from, checked against, or gated by the other"). The section
  now quotes canon verbatim ("Complementary, not combined. T3 measures trust, V3 measures value.")
  and glosses "combined" with canon's own T3 NOT line (not one 6-D tensor). Verified against
  CANONICAL_TERMS_v1 l.38 and l.53.
- `fleet/page.tsx` vocabulary note: "T3 … verified by V3" became "T3 … and V3 …, complementary tensors".
- `links/page.tsx` Web4 entry: says "complementary tensors" and attributes the "verified by" gloss to the site.
- `context/page.tsx` ADP row: "(canon, under R7)" now cites atp-adp-cycle.md §2.2. It notes that canon
  names recharge only inside R7, while the spec does not make it R7-specific.
- `context/page.tsx`: "oversight always means" is scoped, with /fleet's Oversight pool named as the exception.
- Substrate "four senses": fixed by reducing the senses, not by extending the list. The society-host
  sense was replaced with "hosting the society" (/fleet ×3, /autonomy, /context society-host row).
  /projects "different substrates" → "platforms". /context dlPFC "substrate" → "region".

## Feedback Reviewed
- Visitor log 2026-09-24. Three HIGH items: the T3/V3 canon misquote, Hestia's missing first-run path,
  and the identity-metric attempt, now 17 days old. MEDIUM items are listed under Open Items.

## Changes Made (Site)
- **HIGH 1 (T3/V3)**: see Terminology. Fixed at the misquote as the log asked, not by deleting "verified by".
- **HIGH 2 (Hestia)**: the /projects Quick start has a Hestia block marked "NO FIRST-RUN PATH YET".
  The README has no install section. `deploy/` is the fleet's probe notebook, and `install-members.sh`
  installs member hooks on a machine that already runs the daemon. The Home pointer to Hestia now
  says it has no outside install guide yet. The "runnable" badge is kept, because it runs on the fleet.
- **HIGH 3 (identity-continuity attempt)**: done and published, not just dated.
  `maintainer/analysis/identity_continuity_attempt.py` + `identity-continuity-attempt-2026-09-24.md`.
  - Metric: lexical similarity of answers to the fixed tutor script, with names stripped.
  - Result 1: it separates Legion and McNugget, both on gemma3:12b. AUC 0.74 untruncated, 0.98 when
    each answer is cut to its first 60 words. Leave-one-out line attribution is 0.71 / 1.00, against a
    shuffled-label null maximum of 0.59 / 0.63.
  - Result 2: Legion's same-line similarity is flat in lag, 0.695 at lag 1 and 0.692 at lags of
    101–300. It is unchanged across the gemma4:e4b swap: e4b records are 100% closer to Legion's own
    gemma3 history. The signature is constant and does not accumulate.
  - Result 3: untruncated, McNugget's long-lag self-similarity (0.523) equals the same-day cross-line
    level (0.526). The separation depends on a free parameter and on one line.
  - Verdict: a partial failure that moves the blocker. A metric exists. The missing piece is what the
    developmental account predicts for an arm that is handed a line's history.
  - /raising: the attempt paragraph is linked, and the "does not yet yield a metric" sentence is
    date-scoped. "No metric yet" was updated on /raising, /fleet ×2 and the /context identity row.
- `caveats.json`: 3 withdrawn entries (`t3v3-canon-neither-gated-misquote`,
  `oversight-always-machine-sense`, `identity-continuity-no-metric`). Each pattern was confirmed
  against the HEAD rendering.
- Build passed. check-caveats: 21 protected assertions hold, 80 withdrawn readings / 0 live, 0 damaged text.

## Upstream finding (SAGE, not fixed; flag only)
- `run_session_identity_anchored_fluid.py` gates exemplar injection with a substring test on the
  instance directory name (`'2b'` in `'gemma3-12b'` is True). Every `*-12b` directory therefore gets no
  exemplars. That is the same class `fc384fb22` fixed in `ollama_raising_session.py` on 2026-09-09
  ("a substring test cannot tell 4b from 14b"). It also means Legion's and McNugget's prompts may
  differ in exemplar injection. Which runner wrote McNugget's records is unresolved: the checked-in
  launchd plist points at `mcnugget_raising.sh` (ollama_irp), but the records say identity_anchored_v2.

## Cross-Site Drift Alert
- sage-site: any "T3 verified by V3" gloss stated without attribution, and any statement that identity
  continuity has "no metric".
- web4 README still says substrate / protocol / stack. This is upstream; flagging only (same as 09-23).

## Unresolved HIGH — requires sign-off
None. All three 09-24 HIGHs were resolved this session. HIGH 3 is resolved as a published attempt.
The follow-on question (what the developmental account predicts for a handed-history arm) is a
researcher decision and is stated on /raising.

## Open Items (Deferred)
- MEDIUM, not done:
  - Home: define "raising" and "learned state".
  - Constant-stimulus analysis: predicted direction per measure, and a pre-registration commitment.
  - /projects Hardbound card: add the Metalinxx enterprise-tier clause.
  - Glossary: sort, anchors and naming (Glossary / context / Vocabulary); 57 bare-path link texts.
  - /fleet: reading order (cards start about 2,180 words in).
  - /projects Web4 card: one 200-word sentence.
  - Home: SAGE "Governance Engine" gloss.
- LOW: Hub "pilot-stage" wording; ACT `repo-index.yaml`; /raising "raising does neither" scope;
  /fleet "emergence" clause; /autonomy "improves"; /principles P9 criterion and P2 caveat placement;
  /raising closing quote; meta descriptions; ARC "Paper →" link; 4-gov note; CBP/SOIA/TCSEC glosses;
  BECOMING row.
- Operator decisions, still undated: the cross-family visitor persona and the growth-control pre-registration.
- Tooling: the hestia pre-tool hook denied a Bash call because the Python text `__import__("os").environ`
  contains `.env`. That is a false positive: no file was touched. I switched to a CLI argument, which
  is a cleaner design anyway, and did not appeal, because the resource was never a secret. Worth a
  narrower rule upstream.
- The unrelated `visitor/CLAUDE.md` working-tree change was left untouched and uncommitted, as before.

## So what?
For 17 days the site said identity continuity had no metric. It took about an hour to find a metric,
which suggests the obstacle was the missing attempt, not the definition. The result also reframes the
question. The lines are distinguishable, and the difference is constant for 300 sessions and through a
model swap. The site has read that shape as identity. It is also exactly what a fixed per-line context
would produce. The developmental account now owes a prediction that a handed-history arm could falsify.
Until it has one, "identity persists across substrates" cannot lose, and a claim that cannot lose is
not a finding.
