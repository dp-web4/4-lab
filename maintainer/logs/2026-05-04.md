# Maintainer Session — 2026-05-04

## Fleet Sync
- Manifest checked: yes (`/mnt/c/exe/projects/ai-agents/SAGE/sage/federation/sage-fleet-models.json`, last updated 2026-03-08)
- Changes found: Sprout session count — manifest says "115+ sessions", page showed "107 sessions"
- Page updated: yes — Sprout role updated to "115+ sessions"
- All other model names, backends, LoRA flags, and hardware fields match the manifest.

## Terminology Fixes

### HIGH severity
- `fleet/page.tsx`: "Web4 and SAGE" → "SAGE and Hardbound" — the original sentence read "a functional separation that mirrors what we're building with Web4 and SAGE: generative entities and oversight entities." This was categorically wrong on two counts: (1) Web4 is an ontology, not an entity on either side of a generative/oversight split; (2) Hardbound — not SAGE — is the oversight system. Fixed to "SAGE and Hardbound" with inline clarification: "SAGE (generative cognition kernel) and Hardbound (hardware-bound oversight suite)." This also introduces Hardbound on the fleet/hardware page, where it was entirely absent despite being the hardware-bound oversight system.

### MEDIUM severity
- `projects/page.tsx`: SNARC description changed from leading with "Salience-gated memory for Claude Code" (a tagline) to "SNARC (Surprise / Novelty / Arousal / Reward / Conflict) — salience-gated memory for Claude Code." This aligns with the canonical expansion used on `/fleet` and `/context`. The tagline is preserved as secondary description.

## Feedback Reviewed
- Visitor log: 2026-05-04 — four-persona pass
- Strongest finding (Web4 Contributor, Pass 3): `/fleet` "Web4 and SAGE: generative entities and oversight entities" is categorically wrong — Web4 is not an entity, SAGE is not oversight, Hardbound is the oversight system. Confirmed by Tech Writer (Pass 2) as structural drift.
- SNARC drift confirmed independently by Tech Writer (Pass 2) and Web4 Contributor (Pass 3): three different surface expansions across three pages; `/fleet` and `/context` use canonical S/N/A/R/C; `/projects` used tagline only.
- ARC-AGI-3 numerical inconsistency (Pass 3): already resolved in the codebase — both pages (`/projects` and `/raising`) now show both metrics (96.0% game rate AND 94.85% official action score) with labels. No fix needed.

## Changes Made (Site)
- `src/app/fleet/page.tsx`: Sprout session count 107 → 115+ (fleet manifest source of truth)
- `src/app/fleet/page.tsx`: "Web4 and SAGE" → "SAGE and Hardbound" with clarifying inline descriptions; fixes categorical error and introduces Hardbound on the hardware page
- `src/app/projects/page.tsx`: SNARC description — canonical acronym expansion on first use

## Build
- `node node_modules/.bin/next build`: ✓ compiled successfully, 12/12 static pages, zero errors

## Cross-Site Drift Alert
- The "Web4 and SAGE as generative+oversight entities" framing may have been copied to sibling sites. Check `synchronism-site` and `sage-site` for the same sentence pattern. The correct pairing is SAGE (generative) + Hardbound (oversight).
- SNARC expansion inconsistency may appear in other docs/READMEs that copied from the pre-fix `/projects` tagline rather than the canonical S/N/A/R/C expansion.

## Open Items (Deferred)

### MEDIUM severity (visitor log, not addressed this session)
- No `/glossary` route — Home vocabulary primer is invisible from deep-link entries. Large scope change, deferred.
- Web4 equation operator legend (`/`, `*`, `+`) missing from `/context` (appears only on `/projects`). Would close researcher/developer friction.
- ARC-AGI-3 never expanded on first use per page — "Abstraction and Reasoning Corpus, version 3."
- `/principles` heading hierarchy — 10 principles all under single H1, accessibility concern.
- Hardbound definition missing on per-page first-use for `/`, `/raising`, `/context`.
- Quantitative claims on `/principles` (1% coupling → 35% coherence gain; Hill kinetics, Fokker-Planck, p_crit) — researcher persona flagged as needing methodology links or hedges. Content decision, not terminology fix; deferred to project owner review.

### LOW severity
- `/fleet` "oversight pool" label (CBP/Nomad) risks semantic collision with Hardbound's hardware-oversight role. Could rename to "review pool" or "coordination pool." Low priority now that the Hardbound introduction on `/fleet` disambiguates the concepts.
- LoRA never expanded on `/fleet` or `/context`.
- LCT pluralization drift on `/projects` ("Linked Context Tokens" vs canonical singular).
