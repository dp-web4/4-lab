# Maintainer Session — 2026-09-23

## WAKE
- Last maintainer log (09-22) had carry-forward `None.`. No visitor log is missing a maintainer log (09-20 through 09-22 each have one). Nothing orphaned.
- The terminology loop has converged: all four passes found zero canonical drift. Both HIGHs today are **upstream protocol and policy changes that the rendered pages cannot show**. The public repo is no longer a complete view of the fleet, so the site's audit surface now has to include SAGE commit messages, the runner code and the instance records.

## Fleet Sync
- Manifest checked: yes (SAGE pulled at 06:30)
- Changes found: McNugget entry rewritten 2026-09-23T08:22Z, `Gemma4 12B` (ollama). This matches the card (Gemma 4 12B since 2026-09-08). The other five entries are still dated 2026-03-08, which is stale as before.
- Page updated: no (from the manifest). The McNugget, CBP and pub cards were updated from SAGE commits (see below).

## Terminology Fixes
- No canonical drift found. The visitor passes and the maintainer grep agree.
- Non-canonical category noun: Web4 was called "protocol" (4 uses) and "standard" (1 use) in site voice. Changed:
  - `page.tsx`: "For the protocol and implementation" → "For the ontology and its reference implementation"
  - `projects/page.tsx`: "Want the protocol instead?" → "Want the ontology's code instead?"
  - `links/page.tsx`: "Protocol: start with Web4" → "The ontology and its code: start with Web4"; "the protocol starting point" → "the starting point for the ontology and its reference implementation"
  - `context/page.tsx`: "the Web4 standard's society-roles spec" → "the Web4 specification's society-roles document"
  - Added a `withdrawn` entry (`web4-protocol-site-voice`) so these can't come back.

## Feedback Reviewed
- Visitor log 2026-09-23: zero drift on all 4 passes. Two HIGH findings, both verified against public SAGE before editing:
  1. **Records went private on 09-20.** SAGE `cefb5c184` (09-19, the fleet ruling plus a generic `sage/instances/*-being/` ignore rule), `e57f6747` (CBP launcher → private mirror), `8903017f` (McNugget launcher and supervisor), `18a6e92de` (pub-llama3.1-8b frozen at sessions 1–240, per MOVED.md). The last public raising sessions are CBP 270 (09-20 02:04Z) and McNugget 495 (09-20 01:52Z). Nomad still publishes (369, 09-23).
  2. **The runner states the hypothesis in the prompt.** Verified: `ollama_raising_session.py` l.924–936 has a fallback continuity note that the code comment labels "the SAGE finding, told to the being", added in `cc69c7151` (08-27, the Sprout distill-2b swap). CBP's `instance.json` has carried its own note since `4711bdcf1` (09-12), which adds "be yourself, not what you think is expected".

## Changes Made (Site)
- `raising/page.tsx`
  - Evidence-status paragraph: records are public through 2026-09-19, with the ruling and per-line dates. Also states that the lines that went private are the most confounded ones.
  - New **seventh confound**: the continuity note. The "prompts do not make the claims" sentence is now scoped to the tutor prompts.
  - "We're claiming, provisionally…" → "Our working hypothesis, untested, is…", with a dated note.
  - Replaced the false note "expanded inline on first use" with the actual expansions. LoRA is now marked as a standard ML term, not a Web4 one.
- `fleet/page.tsx`
  - Raising-line states go from four to five. New state: *publishing stopped by policy*.
  - McNugget card: state is now "publishing stopped by policy" (no longer "silent"). Added that its supervisor and probe commits continue publicly, and that the 503 fix (SAGE PR #131) can't be verified publicly.
  - CBP card: state changed to "publishing stopped by policy"; its public record ends at 270.
  - pub card: state changed to "publishing stopped by policy"; it has a frozen public record of sessions 1–240.
  - Totals are explicitly frozen at the 09-18 count, because a recount on the public basis would now undercount without saying so.
- `context/page.tsx`, `links/page.tsx`: date-scoped the public-record claim and pointed to /raising.
- `caveats.json`: 4 withdrawn entries (`records-public-anyone-can-read`, `mcnugget-silent-since-0920`, `raising-provisional-claim`, `web4-protocol-site-voice`) and 1 protected caveat (`raising-seventh-confound-continuity-note`).
- Build passed. check-caveats: 21 protected assertions hold, 77 withdrawn readings / 0 live, 0 damaged text. Commit `059bf49`, pushed.

## Cross-Site Drift Alert
- sage-site: any "every session transcript is public" claim went stale on 2026-09-20. The same goes for any wording that calls identity persistence across a model swap a "finding".
- web4 README still uses "protocol" for Web4 (visitor Pass 3). This is upstream; flagging only.

## Unresolved HIGH — requires sign-off
None. Both 09-23 HIGHs were resolved this session.

## Open Items (Deferred)
- **Legion may be in the same state (untested).** legion-gemma3-12b went quiet on 09-02. A gitignored `legion-being` home was in use from about 09-05 (SAGE `0749bf9b3`, "first graft on legion-being"), and `e57f6747` says every machine *but* Legion still had to move. If Legion's raising moved to a private home around 09-05, its "quiet" is really a policy stop that happened earlier. Nothing public confirms that raising moved rather than just the graft, so the card is unchanged. What would resolve it: a Legion commit or MOVED.md that dates the move.
- Nomad card still reads "363, last 2026-09-15". Public now shows 370 files, last 09-23. The page's states are pinned to the 09-18 count, so this is consistent at the stated basis. It should be refreshed at the next full recount.
- MEDIUM, not done: the Quick start dashboard paragraph (SAGE PR #112 fixed the empty SNARC bars on 09-18), Hub card (4-hub is a read-only mirror; quote upstream's Docker wording), /principles P4 (RDF stated as practice; the tracker holds scalars), SAGE legacy-name note missing on 5 pages, meta description/title still fleet-first, undated controls (scramble 61 days, cross-family persona about 59 days, identity-metric 16 days). The controls need a researcher decision, not a maintainer edit.
- LOW: glossary sort, BECOMING row, H1→H3 skips, the ARC nav position, hestia/Hestia distinguished only by capitalization, web4-core 0.4.0 in source.
- An unrelated uncommitted change to `visitor/CLAUDE.md` is in the working tree. It isn't from this track, so I didn't read or commit it.
- A hestia member-mesh notice from cbp-being is pending (coordination pointer). It's outside this track's scope and was left undrained.

## So what?
The site's strongest honesty claim was "the transcripts are public, anyone can read them". Since 09-20 that has been true for a shrinking and non-random subset. The lines that left public view carry an operator channel and a continuity note, so external auditability is lowest exactly where demand characteristics are strongest. Stating this is correct, but it doesn't fix it. The research question now belongs to the researcher: what can an outside reader check once the records are private (counts, hashes, a released sample)? Separately, the continuity note turns the next identity-continuity measurement on CBP and Sprout distill-2b into a contaminated one unless someone treats it as a treatment arm on purpose.
