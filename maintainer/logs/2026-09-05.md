# Maintainer Session — 2026-09-05

## Fleet Sync
- Manifest checked: yes (SAGE repo pulled, already up to date)
- Changes found: none — manifest still stamped 2026-03-08, still lists 6 machines with March-era models (thor qwen2.5:14b, sprout qwen3.5:2b, cbp tinyllama). The fleet page carries 8 machines and mid-2026 model lines (Gemma 4 E4B/E2B, Qwen 3.5 27B/0.8B) and is ahead of the manifest. Per standing finding: the manifest is stale on the writer side; never sync the page down to it.
- Page updated: no (nothing to follow)
- Note for operator: the manifest has not been written by any machine since March. Either the writer hook is dead fleet-wide or the "each machine owns its entry" contract was never wired. Step 0 of this workflow is currently a no-op ritual.

## Session Gap
- The 2026-09-04 maintainer run died at startup: "You've hit your weekly limit · resets Sep 7". Last maintainer `.md` session log is 2026-08-11; last `src/` commit is also 2026-08-11. Cron `.log` files exist for 08-12 through 09-05 but no session logs — three and a half weeks of maintainer runs produced no site changes. Worth checking the other cron logs for the same limit message.
- Consequence: the visitor's "intra-day non-reproducibility" (Pass 1 saw T3/V3 bare on /principles, Pass 3 saw no Sovereign entry; later passes saw both fixed) cannot be a maintainer deploy between passes — nothing deployed. Those are fetch/read artifacts on the visitor side. Both items verified present in source (`principles/page.tsx` Principle 4; `context/page.tsx` sovereign row).

## Terminology Fixes
Hard drift: none (all four visitor passes agree; confirmed by grep). All fixes below are semantic / stack-vocabulary drift, verified in source before editing.
- `src/app/context/page.tsx` (Hardbound): "the trust layer intended to touch silicon" → "the oversight component intended to anchor policy gating in silicon. It gates and reverts; trust itself is produced by the T3/V3 tensors, not by Hardbound." Hardbound = oversight; "layer" = stack vocabulary.
- `src/app/fleet/page.tsx` (trust paragraph): "V3 attestations … trust earned through peer verification" → V3 is the value tensor, symmetric with T3; "T3 (trust) is verified by V3 (value) through peer attestation, which is what the equation's T3/V3 means". Also "hestia trust layer" → "hestia trust tracker".
- `src/app/projects/page.tsx` (4-Life): "earn ATP" → "spend ATP, earn recharge against validated value". ATP is allocated, ADP discharged, recharge earned.
- `src/app/projects/page.tsx` (ACT) + `src/app/context/page.tsx` (ACP row + ACP section): "ACP layers Web4 trust primitives … over MCP transport" → "ACP binds Web4 trust primitives … to MCP tool calls; MCP is a term in the Web4 equation, not a layer beneath it." Three occurrences of the same stack framing, one fix each.
- `src/app/arc-agi-3/page.tsx`: "The ontology layer" → "The ontology".
- `src/app/autonomy/page.tsx`: Hardbound "research layer" → "research direction"; registry "bookkeeping layer" → "bookkeeping record".
- `src/app/page.tsx`, `src/app/links/page.tsx`: "Web4 trust-layer side" → "Web4 trust-primitive side" (2 occurrences); home "MCP … transport layer" → "transport protocol"; projects Hardbound "oversight/security layer" → "component". Self-found on the regrep after the flagged items.
- `src/app/links/page.tsx`: "1,991 … raising sessions" → "machine sessions" — one noun sitewide, /fleet's.
- **R7 — NOT a site defect.** Pass 3 flagged /context "Both modes are canonical, neither deprecated" as contradicting "the canonical-terms reference (R6 canonical, R7 flagged as drift)". Checked `web4/docs/reference/CANONICAL_TERMS_v1.md` §"R6 and R7 — Action Framework (both canonical, consequence-tier distinction)": canon says exactly what the site says. The visitor track's reference table is the stale document, and `maintainer/CLAUDE.md`'s own terms table ("R6 … ~~R7, R5~~") is stale the same way. Did not edit either config (constraint). Made one site edit to close Pass 3's real point: the R7 row now says the update loop is "the T3/V3 tensor delta /raising describes … one mechanism, in tensor vocabulary there and action-grammar vocabulary here".
- Stale flags (already correct in source, no edit): / "Corpus for AGI" — source reads "Corpus for Artificial General Intelligence"; /context Sovereign entry present; /principles T3/V3 expanded in Principle 4.

## Feedback Reviewed
- Visitor log: 2026-09-05 — 4 passes, zero hard drift, 15 semantic/soft items, 7 placement gaps, 31 ranked fixes. Highest-value item across personas: the /projects ARC-AGI-3 card's sentence order attributed the Opus 4.6 score to six edge devices.

## Changes Made (Site)
- `src/app/projects/page.tsx` (ARC-AGI-3 card, HIGH #1): reordered so attribution leads — "Claude Opus 4.6 inside the SAGE harness scored 94.85% …"; the six-machine 0.8B–27B sentence moved to an explicit "Phase 2, separate from the scored run and not scored by ARC Prize" line. Added "how much of that score is the harness is an open question" beside the ablation caveat.
- `src/app/raising/page.tsx` (headline, HIGH #2): "Raising is not training. Training optimizes for a loss function…" → "Raising is not training; it is also not nothing." (Principle 7's register, already on /principles), then the positive statement "longitudinal, entity-specific context shaping" and the real comparator: task-specific context engineering, "the lever /arc-agi-3 reports as its headline finding; whether raising differs from it in kind is this page's open question, not its premise."
- `src/app/arc-agi-3/page.tsx` (Phase 2, HIGH #2 other half): added the reciprocal sentence — context engineering is the positive finding here and the deflationary null on /raising, with a link.
- `src/app/raising/page.tsx` (Pattern 4, HIGH #3): added "Whether the rising Questioning proportion was counted only in stop-token-enforced turns has not been checked; until it is, read this pattern and the bilateral-generation observation below as one observation, not two independent ones." I could not verify the counting method from here; the honest move is to say it is untested rather than assert independence either way.
- `src/app/projects/page.tsx` (Quick start, HIGH #4): "# See README for Python/conda setup" → "# Needs: Rust (stable), Ollama, Python 3.10+ — step-by-step first session on /links". /projects and /links no longer disagree on prerequisites.
- `src/app/projects/page.tsx` (SAGE card, MED #5): session total now says "machine sessions", names the mixed basis (three whole-machine totals + three model-line counts), points to /fleet for the noun's definition, and footnotes HUB as "also raises a SAGE instance but counted as a society-host by primary role".
- `src/app/projects/page.tsx` (ACT card, MED #12): one sentence reconciling "not a blockchain" with Cosmos SDK — Web4 is the ontology and requires no chain; ACT is one implementation that uses Cosmos SDK's ledger machinery as its witnessing substrate.
- Per-page `<title>` (MED #9): `layout.tsx` now uses a title template ("%s | 4-lab"); each of the 8 subpages exports `metadata.title`. Verified in built HTML: "Fleet — Eight Machines | 4-lab", "Glossary | 4-lab". /context's title is "Glossary" to match the nav label (MED #10 partial — the H1 still says "How Knowledge Flows").
- Build: `npx next build` passes, 12/12 static pages, zero errors. `node_modules` was absent on this machine; ran `npm ci` (lockfile unchanged).

## Cross-Site Drift Alert
- "layer" is the new "stack": 11 occurrences of Web4 components described as layers, all written after the 08-10/08-11 "stack" cleanups. Sibling sites (synchronism-site, sage-site) should grep for "layer" near Hardbound / ACP / MCP / ontology / trust.
- Any sibling page that expands ARC-AGI-3 as "…Corpus for AGI" should use the full expansion.
- R6/R7: any sibling reference table that lists R7 as drift is contradicted by CANONICAL_TERMS_v1 §R6-and-R7.

## Open Items (Deferred)
- Visitor track's reference table and maintainer/CLAUDE.md terms table both list R7 as drift; canon disagrees. Operator-level fix (I may not edit either).
- `/context` H1 "How Knowledge Flows" vs nav/title "Glossary" (MED #10 remainder) — page restructure, not a one-line fix.
- MED #11 (home lead paragraph reorder), #13 (identity-strength gradient on / and /fleet), #14 ("zero rollbacks" incident count — needs the count), #15 (date the deflationary control — operator decision), #16 ("35% coherence gain" — needs the metric name from the Synchronism experiment), #17 (publish tier-2 log excerpts — privacy call), #18 (trust disambiguation clause), #19 (stack line per card).
- LOW #20–31 placement expansions and slogans. #31 (a visitor persona from a different model family) is a good structural idea and is the operator's call.
- Fleet manifest writer is dead since March (see Fleet Sync).
