# Maintainer Session — 2026-05-07

## Fleet Sync
- Manifest checked: yes
- Changes found: all current — all 6 machines match manifest
  - Thor: Qwen 2.5 14B · LoRA (transformers) ✓
  - Sprout: Qwen 3.5 2B (ollama) ✓ — visitor flagged "3.5" as possible typo; manifest confirms it is correct
  - Legion: Phi-4 14B · LoRA (ollama) ✓
  - McNugget: Gemma 3 12B (ollama) ✓
  - Nomad: Gemma 3 4B (ollama) ✓
  - CBP: TinyLlama 1.1B (ollama) ✓
- Page updated: no (all current)

## Terminology Fixes

### Confirmed drift fixed:
- `src/app/arc-agi-3/page.tsx`: "Abstraction and Reasoning Corpus, generation 3" → "Abstraction and Reasoning Corpus for Artificial General Intelligence, version 3" — Chollet's canonical expansion drops "AGI" in current site text; confirmed by Passes 2, 3, 4

### Borderline-drift fixed:
- `src/app/arc-agi-3/page.tsx`: "The architecture is the capability." → "The loop is the capability." — generic English use of "architecture" sits near Web4 equation and collides with /principles anti-drift rule ("Web4 is not 'architecture'"); three personas flagged; rewording removes the ambiguity without loss of meaning

### No drift found (confirmed clean):
- `/projects` Oversight Plugins blurb: Pass 1 flagged "governance" wording; current source reads "Web4 oversight model ... implemented as plugins" — already correct. Repo URL slugs carry "web4-governance" (GitHub legacy names, load-bearing for forks) with footnote explaining the discrepancy.
- LCT, T3, V3, MRH, ATP, ADP, R6: all expansions correct everywhere they appear
- Web4 as "ontology": correct site-wide
- Hardbound as "oversight": correct site-wide
- "production ready": absent
- "unification" / "scope inflation": absent except as foils on /principles

## Feedback Reviewed
- Visitor log: 2026-05-07 — 4-persona pass; key findings:
  1. Canonical-term hygiene on expansions is excellent (all correct where they appear)
  2. ARC-AGI-3 name expansion drops "for Artificial General Intelligence" — confirmed drift
  3. "The architecture is the capability" collides with /principles anti-drift rule
  4. /arc-agi-3 missing from main nav — page with strongest external credibility signal is orphaned
  5. Research conditions caveat (public game set, Opus 4.6, network access) is mid-page, not in subtitle
  6. Nav home description says "Eight repos" but home page says "Fourteen repos"
  7. Operator semantics (/,*,+) exist on /projects and /context but not on the equation in /arc-agi-3 card — noted but the equation in arc-agi-3 is inline in prose, not a standalone display equation
  8. Glossary table on /context deferred (pending from prior sessions; needs dedicated session)

## Changes Made (Site)

- `src/app/arc-agi-3/page.tsx`:
  1. ARC-AGI-3 name expansion: added "for Artificial General Intelligence"
  2. Subtitle: added "public game set, research conditions (Claude Opus 4.6 with network access)" — surfaces the caveat immediately rather than 3 sections down
  3. "The architecture is the capability." → "The loop is the capability."

- `src/lib/navigation.ts`:
  1. Added ARC-AGI-3 as nav item (position 3, after Projects) — was missing from 8-item nav
  2. Fixed home description: "Eight repos" → "Fourteen repos" (matched home page hero text)

Build: `node_modules/.bin/next build` — clean, 12 static pages, zero errors

## Cross-Site Drift Alert
- None detected this session. The operator semantics pattern (legend below equation) is already present on /projects and /context — other dp-web4 sites should check whether they display the Web4 equation without the legend.

## Open Items (Deferred)

- `/context` glossary table: all 4 personas converged on this as the highest-leverage fix; needs a dedicated session to add a sortable term/expansion/gloss/page-reference table at the top of /context
- SAGE loop step numbers on /arc-agi-3: "PolicyGate at step 8.5" has no numbered referent in the step sequence; low priority
- Operator legend on inline arc-agi-3 equation (in Web4 card): the equation appears in a paragraph, not as a display block; adding a legend there is awkward; linking "/context" from the card is a better fix — deferred
- MRH operationalization and Synthon measurement criteria (Pass 4 research flags): legitimate research-integrity gaps, not site-maintainable — pass to session primers
- Canonical CLAUDE.md table needs update: R6 and R7 are BOTH canonical per CANONICAL_TERMS_v1.md (consequence-tier distinction, not deprecated). Current maintainer CLAUDE.md table lists R7 as wrong drift — this is incorrect and should be corrected. R6 is correct for routine actions; R7 adds Reputation for higher-consequence actions.

## So What?

The ARC-AGI-3 expansion drift was the most significant find — it was dropping "for Artificial General Intelligence" from Chollet's benchmark name, which is load-bearing for external credibility (a researcher who knows the benchmark would immediately notice). Fixed. The nav omission of /arc-agi-3 is a self-inflicted reach problem given that it's the most externally shareable page — fixed. The "architecture is the capability" reword is small but closes a real tension with the explicit /principles anti-drift declaration.

The visitor audits this session were high-quality: the technical writer and contributor passes found things the developer pass missed, and the researcher pass identified structural epistemic gaps (Synthon operationalization, MRH measurement) that are accurate but not site-maintainable. The glossary table deferred item is increasingly load-bearing — four consecutive sessions have flagged it.
