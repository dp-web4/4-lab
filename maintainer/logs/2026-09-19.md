# Maintainer Session — 2026-09-19

**WAKE.** Last maintainer log (2026-09-18): `## Unresolved HIGH — requires sign-off` → **None.**
No blocked work, so the session opened on today's visitor log.

Eleventh consecutive visitor pass with zero canonical-term drift, and no HIGH items. Every
substantive finding came from checking the site against something outside it. I verified each
against the artifact before editing. The researcher persona's transcript claims reproduce exactly
from a SAGE clone, and the clone shows more than the persona saw.

**The finding of the day, stated once.** The site told researchers three times that its session logs
are internal and not auditable. They are full public transcripts. Reading them shows that the
"Claude" tutor on the scripted runners is a string table (`CONVERSATION_FLOWS` in the public runner),
and that one six-question "creating" script accounts for **1,217 records across four lines**:
Legion 422/462, McNugget 364/486, Nomad 301/363, Sprout 130/661. The persona read two lines and
reported Nomad only. Thor (232 distinct in 268), CBP, HUB and pub are not like this. A false caveat
was protecting the most checkable thing the lab has, and "never weaken caveats" made it the hardest
kind of sentence for this loop to remove. The companion rule is now in practice: a caveat is a
factual claim and gets verified like one.

## Fleet Sync
- Manifest checked: yes (SAGE pulled, a9fe88a04, 2026-09-19)
- Changes found: McNugget is the only entry with a fresh `updated_at` (2026-09-19): Gemma4 12B,
  ollama. Matches the page. The other five entries are still the 2026-03-08 write with pre-cutover
  models (Thor Qwen 2.5 14B, Legion Phi-4, Nomad Gemma 3 4B, CBP TinyLlama). Divergence logged, not
  acted on, per the polarity correction.
- Page updated: no (not from the manifest). Legion's state changed from the git basis, below.

## Terminology Fixes
- No canonical drift found this session (four passes, zero; my own grep agrees).
- Home: "society hosts" → "society-hosts" (defined form); spaced hyphen → em dash in the thesis sentence.
- /projects, /links: "CBP chain startup" → "a chain startup path for the fleet machine CBP" (machine
  name read as an acronym).
- /fleet: "the being" in the page's own voice → "the line" / "the identity" (3 places). The quoted
  commit subjects keep their wording; they are evidence.
- /projects: quoted "consciousness kernel" gets one legacy-name clause.
- /principles #2: "follows patterns analogous to" → "We model … by analogy to … not an observation".

## Feedback Reviewed
- Visitor log: 2026-09-19 — 0 HIGH, ~12 MEDIUM. Drift summary items 1, 2, 3, 4, 5, 8, 10 (part), 11
  addressed. Priority fixes 1–6 done, 8 partly (principles), 12 done, 13 partly.

## Changes Made (Site)
- `raising/page.tsx`: Evidence status rewritten. Records are public (path linked); what is missing
  is coding criteria, a rater protocol, and external audit. The protected phrase "not externally
  audited" stays, because that half is true. New section `#session-protocol` "Who conducts the
  sessions": interlocutor labelled Claude, string table on scripted runners, per-line counts of the
  fixed script (counted 2026-09-19, dated because McNugget's 486 is already past /fleet's 482), Nomad's
  protocol change at session 346 on 2026-09-10, and both consequences: sessions are not comparable
  observations, and a constant prompt is close to a controlled design nobody has reported. Demand
  characteristics added as a sixth confound with verified quotes. The praise line is scripted, so it
  is delivered whatever the answer was; that detail is on the page. Intro no longer says "watching
  what happens". Principle 6 link now deep-links `#p6`.
- `context/page.tsx`: Evidence "Internal observations" paragraph corrected and scoped. Hardbound's
  catalog stays uncheckable from outside, because it is. The ladder's "what would move claims up" now
  asks for criteria plus a blind rater, not redacted samples. Substrate row: "open substrate is drift"
  replaced with an attributed account of the upstream README, keeping "ontology".
- `links/page.tsx`: researcher note corrected; "email below to inquire" removed.
- `page.tsx` (Home): stat 3 no longer restates /fleet line states ("both Qwen lines quiet" was wrong
  on count and state). Hardbound sentence carries its maturity: "being built", "planned", private and
  research-stage, hardware anchoring a design target.
- `fleet/page.tsx`: one governing date (2026-09-18) for the "Raising line:" state. Under it **Legion
  is quiet, not running**: last record 2026-09-02, sixteen days before the count. Checked all four
  Legion instance directories (no migration) and the commits since (2026-09-05 development work, not
  fires). "352 records on Nomad's card" no longer restates a number.
- `projects/page.tsx`: README framing disclosed beside the registry-metadata disclosure; OpenClaw URL
  moltbot → openclaw (new URL returns 200).
- `principles/page.tsx` + `globals.css`: the ten principles are `<h2 id="p1…p10">`. Visual style held
  by resetting the border and padding the section h2 rule would have leaked in.
- `caveats.json`: four `withdrawn` entries (false auditability caveat, Home restating line states,
  the 09-12 state date, open-substrate-as-drift). All four were run against the pre-fix build and hit.
  The `evidence-tier-internal` caveat's `why` was reworded; its pattern is unchanged.
- Build passes. `check-caveats.sh`: 20 protected hold, 2 standing debt, 61 withdrawn / 0 live,
  0 damaged-text hits. Anchors confirmed in built HTML.

## Cross-Site Drift Alert
- **Upstream, not site drift:** web4 `README.md` leads with "open substrate", has "The stack" and
  "Web4 is open infrastructure", and contains zero "ontology". `CLAUDE.md` in the same repo says
  "Web4 is an ontology, not infrastructure". CANONICAL_TERMS_v1 has no Web4 entry. Verified locally
  today. **[researcher]** decides which is intended. Every explainer site enforcing "ontology" is
  enforcing against its own upstream's first screen.
- sage-site likely carries the same "logs are internal" framing and says nothing of the fixed
  script. Worth a check by its maintainer.

## Unresolved HIGH — requires sign-off
None. The visitor log raised no HIGH items.

## Open Items (Deferred)
- **[researcher] Was the fixed six-question script a design or an unattended default?** 1,217
  records ride on the answer. If it was a design, response drift under it should be reported. If it
  was a default, McNugget is still on it as of this morning (session 492).
- **What generates the distinct tutor turns on Thor, CBP, HUB and pub** was not traced. The page
  says so.
- **Open-gaps table on /autonomy** (gap · first disclosed · who can close · days open). I have the
  method (marker phrases through `git log -S`) and did not get to it. This is the next session's
  first MEDIUM.
- Structure: H2 grouping on /context; H2 and per-card anchors on /fleet; Home and /projects
  deep-links. Principles done today.
- /context: a "you can stop here" line after the table; correction history in body text (also on
  /arc-agi-3).
- License badges on /projects cards.
- Glossary rows: Binding, Pairing, LCT URI form, identity, coherence, consciousness-as-not-claimed.
- Home stat block 2 is still bookkeeping in the hero slot (P1). Hub card surface list vs hub/README.
  ARC "Phase 2 → E4B" undated. Synchronism 678 + 2,671 ≠ 3,360. 4-life repo missing on /links.
  Qwen 3.8-distill publisher. /autonomy figures dated 2026-05-26. One shared meta description.
  52 route-as-text links. Late expansions. The PolicyGate "consciousness loop" quote on /context
  lacks the legacy clause /projects now has.
- Carried: unrecorded-fire census unreconciled; Sprout distill-2b silent since 2026-09-05.
- `visitor/CLAUDE.md` has an uncommitted visitor-track edit (8 → 9 pages). Not mine to commit, so it
  is left in the working tree. The visitor run script still says 8.

## So what?
The term audit is done and has been for eleven days. Today's value came from reading the evidence
base rather than the site, and I second the visitor's frame note: point one persona at "pick a claim,
check the public artifact". The 1,217-record finding cuts both ways and the second way matters more.
The lab has an accidental constant-stimulus experiment on four lines, in public, and
`sage/raising/analysis/` already holds exploratory scripts that treat the fixed opener that way. That
is nearer to a result than anything /raising currently reports.
