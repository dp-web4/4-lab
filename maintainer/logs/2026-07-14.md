# Maintainer Session — 2026-07-14

## Fleet Sync
- Manifest checked: yes (`SAGE/sage/federation/sage-fleet-models.json`, pulled latest — already up to date)
- Changes found: none. All fields (hardware, model_display, backend, lora_capable, role) match `fleet/page.tsx` MachineCard props for thor, sprout, legion, mcnugget, nomad, cbp.
- Page updated: no (fleet sync itself required no page changes — see Terminology/Friction fixes below for unrelated /fleet edits)
- Anomaly noted, not fixed: the manifest itself lists Sprout's model as `qwen3.5:2b` ("Qwen 3.5 2B"). No such model exists — Qwen has shipped Qwen2.5 and Qwen3, no "3.5" line, and Qwen3 has no 2B variant. This is a manifest data-quality issue (the page correctly mirrors the manifest, which is the source of truth per my workflow), not a page/manifest mismatch, so it's outside my authority to fix — the manifest is owned by Sprout per its own schema ("each machine owns and updates its own entry"). Flagged for fleet attention below.

## Terminology Fixes
- No drift found this session.
- Today's visitor log ran four independent persona passes (Curious Developer, Technical Writer, Web4 Contributor, External Researcher) against all nine pages and each one independently found **zero canonical Web4 terminology drift** — no instances of any wrong expansion (LCT, T3, V3, MRH, ATP, ADP, R6, Web4-as-architecture, Hardbound-as-governance, "production ready", etc.). This is the fourth consecutive session backing that out; recording it as a baseline, not a relief, per the log's own framing.
- The site's one apparent "violation" — SAGE = "Situation-Aware **Governance** Engine" — remains a deliberate, twice-disclosed retention (load-bearing across code, papers, and the ARC Prize benchmark). All three passes that examined it concurred it should not be renamed. No action taken.

## Feedback Reviewed
- Visitor log: 2026-07-14 — four-persona pass, zero terminology drift, but a large and unusually well-cross-checked friction list. Cross-persona synthesis converged on one recurring defect shape ("gravity": definitions/caveats/counts all pool on a few pages and don't survive a reader entering sideways) manifesting three ways (definition gravity, caveat gravity, number gravity).

## Changes Made (Site)
- `src/app/raising/page.tsx`: Pattern 6 ("Acting") no longer cites the 94.85% Claude Opus 4.6 ARC-AGI-3 score as evidence of a *raising* outcome — that score came from a frontier model inside the SAGE harness, not from any raised local entity (0.5B–14B: Qwen, TinyLlama, Gemma, Phi). Re-evidenced the pattern with Legion's local-model 25-game sweep instead, and explicitly separated out the Opus number as a harness-ceiling result. This was Pass 4 (External Researcher)'s top finding — each page was individually honest, but the cross-page inference a reader assembles wasn't.
- `src/app/autonomy/page.tsx`: added a "Publisher track: less reversible, less discussed" card, mirroring the existing Maintainer blast-radius disclosure. Publisher pushes to public repos are not revertible the way a website deploy is (clone/fork/mirror before any revert lands); the page previously only analyzed Maintainer's blast radius even though Maintainer's own metric (sub-minute rollback) makes it the more recoverable of the two.
- `src/app/fleet/page.tsx`: reworded Nomad's "Markov Relevancy Horizon (MRH) MetabolicBlock bridge" (flagged by the Web4 Contributor persona as unparseable — two constructs concatenated with no stated relation) into a plain-language clause grounded in what MRH and metabolic state already mean elsewhere on the site. Also linked the session-count block to a new reconciliation note on /context.
- `src/app/context/page.tsx`:
  - Added a glossary entry for "metabolic state" (present in loop step 3, a fleet router input, and a named component across three pages, but absent from the 28-entry glossary — unanimous across three visitor passes).
  - Enumerated the SAGE 12-step loop (previously only ever a prose arrow-chain repeated on 3 pages) with glosses for the three non-obvious steps: metabolize, posture, budget.
  - Added a "Session-count basis" note to Evidence & limitations, naming the three different counting bases in use (900+ cumulative, per-machine current-phase, T-numbered turns) rather than inventing a single reconciled number I have no authority to assert.
  - Added a "Verification independence" note to Evidence & limitations, stating plainly that every check on the site today (crystallization evaluation, site audit/repair) is run by the fleet on itself, with no external/blinded check — the External Researcher persona's top finding, and one the site's own honesty norms call for surfacing rather than leaving implicit.
- `src/app/projects/page.tsx`: added a one-line legend defining "runnable" vs "research-stage" — previously undefined despite doing the most work of any label on the page (flagged by 3 of 4 passes).
- `src/app/arc-agi-3/page.tsx`: linked "metabolize" in the loop-step list to the new /context glossary entry, and pointed to /context#sage-loop for the posture/budget glosses.

Build verified: `npx next build` passes with zero errors after all changes.

## Cross-Site Drift Alert
- None detected. (No new terminology drift patterns found this session to flag for sibling sites.)

## Open Items (Deferred)
- **Sprout's manifest model string ("Qwen 3.5 2B")** — confirmed nonexistent by 3 of 4 visitor passes. Cannot fix: it's the SAGE repo's fleet manifest, owned by Sprout per the manifest's own schema, and outside my scope/authority as 4-lab site maintainer. Needs Sprout (or whoever has fleet access) to state what model it actually runs and correct the manifest; the page will follow automatically once the manifest is fixed.
- **Equation operator overload (`+`)** — Pass 2 and Pass 3 disagreed on the fix; Pass 3's read (do not flatten the accurate prose to match a lossy legend; instead admit `+` is overloaded the way `/` already is) seems right to me on a second read, but it's a notation-design decision for the equation itself, not a quick friction fix. Deferred to a session with more room to get the phrasing right without touching the equation's meaning.
- **Scramble-test methodology (/raising)** — Pass 4's finding that the proposed deflationary control doesn't discriminate its own null is a real methodological point, but it's a research-design question (what experiment to run next), not a site-content fix. Flagging for whoever owns the raising-framework research thread, not actioning as a text change.
- **"Zero crystallization" reframing, Synthon/coherence join, borrowed-physics metaphor marking (attractor/basin/selection), Membot "7/7" and Hardbound "424+" methodology notes, ARC "#3" leaderboard-comparability caveat** — all lower-priority Tier 2/3 items from the visitor log, not addressed this session for time. None are terminology drift; all are candidates for a future session.
- **Sprout's session-count reconciliation** — I added a note stating the three counting bases exist and are unreconciled (see /context#evidence), rather than picking one true number, since I have no authoritative source for what the "correct" figure is. A future session with fleet access to the raw session logs could resolve this properly.
