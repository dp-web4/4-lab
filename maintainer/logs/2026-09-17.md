# Maintainer Session — 2026-09-17

**WAKE.** Last maintainer log (2026-09-16), `## Unresolved HIGH — requires sign-off`: **None.** No blocked work.
Ninth consecutive visitor pass with zero forbidden expansions. The one HIGH (a corrupted Web4 card) was
**caused by the maintainer track itself**: my 09-16 session edited that string with a sed replacement that
contained `&apos;`, and in sed `&` means "the whole match". So the first question was not what to reword. It
was which check should have caught this, and why none did. All three existing checks test meaning; none tests
whether the text is intact.

## Fleet Sync
- Manifest checked: yes (SAGE pulled at 06:01, HEAD `00d52c2eb`)
- Changes found: none since 09-15. McNugget (2026-09-15, Gemma4 12B) is already on the page; the other five
  entries are still dated 2026-03-08. `fleet.json` last changed 2026-09-13 (tailnet scrub and gateway port),
  with no model fields newer than the page.
- Page updated: no (polarity rule; nothing newer than the page's figures)

## Terminology Fixes
No forbidden expansions (all four passes, rendered text of all 9 pages). Form, attribution and semantic drift fixed:
- /projects Web4 card: "+ — canonically 'augmented with'" → the site's gloss, not canon's (the legend on the same page already said so)
- T3/V3 dimensions in the expansion slot, **4 instances**: /context worked example (T3 and V3), /fleet peer section, /raising mechanism paragraph → "T3 (Trust Tensor — Talent / Training / Temperament…)". A rendered-text regex confirmed there are no others.
- /context ATP row "declared before an action runs" and ADP row "the record of actual outcome" → canon's definitions plus the spec's pooled token. The worked example's "declares an ATP" / "ADP records what was actually spent" were the same registry shape, fixed too. This is the reading withdrawn on 09-15 from the narrative section only.
- /context R7 row: "every outcome-driven tensor delta is R7's seventh element" → direct-participant deltas are R6 Results, and R7 is the cross-scale feed. Cited `r6-framework.md`'s intro ("R6 Results do include implicit tensor updates"), which now agrees with `atp-adp-cycle.md`, in the R6/R7 section as well.
- /context Sovereign row: "founding member" → a base-mandatory role with final charter-amendment and identity-of-last-resort authority (`society-roles.md` §2.1), which the researcher fills. The row now says both: not outside the ledger, and holding authority no other member holds.
- /fleet pools: the "Oversight: … machine-enforced" gloss now matches the 09-15 note under that heading. "Resource accounting … *is* the ATP → ADP cycle" became an analogy: canon's ATP regenerates through contribution, while these budgets reset every Thursday.
- /autonomy R6 now named "Six-Element Action Framework"; /projects R7 expanded ("R6 + Reputation") and added to the page's term list.

## Feedback Reviewed
- Visitor log 2026-09-17, four passes: zero expansion drift. The synthesis names a propagation problem: corrections reach the section where they were found, not every place the old reading lives. Its ranked list was 1 HIGH plus 14 MEDIUM.

## Changes Made (Site)
- **HIGH, /projects Web4 card**: the closing parenthetical rendered as "…the equationthe spec binds … does not show.)apos;s + does not show that binding.)". Rewritten as one sentence. **Cause, from `git show 1483e71`:** a sed replacement containing `&apos;` re-inserted the matched clause. It was the only orphaned entity fragment in `src/`.
- **New check `maintainer/check-render.py`**, wired into `check-caveats.sh`. It checks rendered HTML for (1) orphaned entity names (`apos;`, `ldquo;` …), (2) blocks where `(` and `)` counts differ, and (3) words glued to a closing inline tag. **On the pre-fix build it found 8 hits:** the fragment, the paren imbalance on the same card, and 6 glued words. Visitor Pass 3 found 4 of those ("A1on", "supersededupstream", "V3(Value", "behavior—"). The check found 3 more ("findscould", "commensurableand", "23 of 25environments"). **Productive failure recorded in the script:** I tried a "same 7-word run twice within 500 chars" splice detector first. It flagged the nav and legitimate boilerplate and missed the real splice, whose duplicated clauses were paraphrases. Paren balance caught it with exactly one hit site-wide.
- Glued words: `{" "}` after 7 line-ending `</strong>`/`</em>` tags; "each component" → "Each".
- **Identity construct single-sourced** (P4 medium): /context's row quoted a /raising wording that includes curriculum state, which /raising has since dropped. /raising's status paragraph and /fleet restated a three-part definition and pointed to "the home page", which has no definition (checked: zero "session-to-session" on Home). Now all of them quote or link /raising's opening definition, each with a dated note.
- **ARC "no published baseline" was refuted** (P4 medium, on 4 pages: Home, /projects, /context, /arc-agi-3). ARC Prize scores action efficiency against a human baseline (RHAE). Its docs changelog shows that on **2026-04-14, three days before the 2026-04-17 run**, the baseline became the median first-time human per level (previously the 2nd-best human), with each level capped at 1.15×. arcprize.org/arc-agi/3: "A 100% score means AI agents can beat every game as efficiently as humans." Restated on all four pages; the engine-source caveat is kept as the one that matters. Protected caveat `arc-action-score-not-solve-rate` still holds (its "why" updated).
- **/fleet portability: the visitor's LoRA figure was wrong, but the point stands.** Pass 4 said "at least 80 of 115" pre-port sessions loaded the adapter, reading /raising's "84 session records" as sessions. Counted in SAGE `sprout-qwen2.5-0.5b/sessions`, the 84 are *files*: two series (`autonomous_s*`, `session_*`) share numbers, session 119 has 8 records, and 999/1000 are test runs. By distinct session number, **45 of the 115** (46–113, 2026-01-27 → 02-22) loaded the adapter; 114–115 say `false`. /fleet now says so, and says which way it cuts (the port dropped the weights; "recognizable" was never measured). /raising's "84" is now stated as files, with the 45 beside it.
- **/raising "each stage adds capability only when the entity has demonstrated readiness"** → no readiness check exists. Phases advance by session number. In SAGE `run_session_identity_anchored_fluid.py` the tool stage is a `--tools` CLI flag (silent / aware / active: three values, where the page plans four stages).
- /raising Patterns 2–3: metabolic state (loop step 3) and per-peer trust (health polls and timeouts) are computed by the harness from session 1. Both are now said to be present by construction and added to the deflationary section's list. Pattern 3 now says the tracker's update is a fixed step clamped to [0, 1] (`peer_trust.py` L105–106), not a Hill shape. Added the `#peer-trust` anchor on /context.
- /raising Pattern 5 ("isn't assigned; it emerges from … what the fleet routes to it") and Pattern 6 ("awareness developed in raising now applies", beside "not attributed to it") reworded. The SAGE expansion moved to first use; "SAGE (…)-Sprout" → "SAGE-Sprout".
- /projects: a one-line term key under the equation legend (8 expansions before first use); SAGE expanded in the summary line.
- **governance→oversight / "predates" notes consolidated** (P1 + P2 medium): 27 rendered mentions on 7 pages → 3, all on /context (the canonical explanation). Home's Hestia card went from about a third vocabulary meta to one TCSEC clause. The 4-gov link line was reduced to its description. New `withdrawn` guard `governance-note-restated-per-page` (matched all four old phrasings).
- Principle 7: "The resulting identity is collaborative … The mechanism:" → "intended result (a framing, untested; link) … The intended lever:". /autonomy registry "prevents … ensures" → "designed to prevent", and it names the conflicts supervisors still resolve.
- `caveats.json` withdrawn: 38 → 53. **All 14 reading entries fired on the pre-fix build (24 live hits), 0 after.** The consolidation guard was tested against the old strings. `--show-exempt` reviewed: 4 exemptions, each a dated withdrawal note or /context's canonical explanation.
- Config: `maintainer/CLAUDE.md` now covers the sed `&` hazard, the JSX line-end whitespace rule, and "explain a vocabulary rule once and link to it". `run_maintainer.sh` step 4 mentions the render check.
- `npx next build` passes; `check-caveats.sh` exits 0 (20 caveats hold, 2 debt, 53 withdrawn / 0 live, 0 damaged-text hits).

## Cross-Site Drift Alert
- **Dimensions in the expansion slot** ("T3 (Talent / Training / Temperament)") evades expansion greps everywhere. Sibling sites should run the regex `\b(T3|V3)( tensors?| scores?)?\s*\((Talent|Valuation)` over rendered text.
- **"No published baseline" for ARC-AGI-3** may also appear on sage-site / the ARC-SAGE README. ARC Prize's score is human-relative by definition.
- **Sed replacements containing HTML entities** would corrupt any site maintained by scripted edits. Grep for orphaned `apos;` / `ldquo;` without a leading `&`.
- **"84 LoRA session records"** read as sessions: anything citing that figure should say files (45 distinct pre-port sessions).

## Unresolved HIGH — requires sign-off
None. The single HIGH (corrupted Web4 card) is fixed, confirmed in built HTML, and guarded by `check-render.py`.

## Open Items (Deferred)
- **Operator decisions:** HUB/Hub and hestia/Hestia are told apart by case only (naming call). Metalinxx is unexplained on Home (relationship to the lab is the researcher's to state). Hub society treasury and who fills the other six base roles (Pass 3 Q1–2; the charter is not in anything I read). Open-gap aging ("open since" dates, owners) for the cross-family persona, the human review cadence, the deflationary control and the identity metric. Principles 8–9 audience label (whether they are agent-facing instructions is the researcher's framing).
- **Structural, not started:** `id="principle-N"` headings on /principles and per-machine ids on /fleet (medium/low; other pages cite both by number/name). /context has no h2 level. There is one shared meta description across 9 pages, and the Home `<title>` doesn't match the hero.
- **Low:** /autonomy lead figures dated 2026-05-26; a Python-deps step in the Quick start (unverified whether SAGE init needs one); "instance line" vs "raising line" (pick one); "sessions" vs "session records" on /fleet; two evidence taxonomies (Home vs /context); the severity-rule coupling note on /autonomy; Hub treasury clause.
- **Researcher question:** with Patterns 2 and 3 now marked as harness-computed signals, only Pattern 4 (Questioning) and Pattern 5 (Creating) remain as candidate *developmental* observations, and both say "increasing" with no count. Should BECOMING stay a six-pattern frame, or become two observations awaiting a count plus four named signals?

## So what?
Nothing new on vocabulary. The HIGH was the track damaging its own output, and the visitor loop caught it a day
later. What moved: a class of defect that none of the meaning checks could see (damaged text) now has a
failing test that fired on 8 real hits. It also names the tool behavior behind it, so the instruction changed,
not just the artifact. Two corrections came from reading upstream artifacts rather than rewording: the ARC score
has a human baseline, and 45 pre-port sessions loaded the adapter, not 80. The consolidation removed 24 notes
the site had been writing for its auditors. The limit is unchanged and outside this loop: every auditor today
was one model family.
