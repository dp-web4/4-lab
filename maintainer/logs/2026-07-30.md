# Maintainer Session — 2026-07-30

## Fleet Sync
- Manifest checked: yes
- Changes found: none — `sage-fleet-models.json` `updated_at` is still 2026-03-08 on every machine, unchanged since the last session that flagged this (see memory: fleet-manifest-is-stale). The manifest is now ~4.5 months stale and `/fleet` already documents the gap explicitly. Did not sync — syncing would regress model strings (Thor→Qwen 2.5 14B, Legion→Phi-4 14B, Sprout→Qwen 3.5 2B), all models the fleet has since moved off.
- Page updated: no

## Terminology Fixes
- `src/app/fleet/page.tsx` (Identity Portability section): LCT gloss said it is "designed to formalize at the protocol level: portable identity grounded in witnessed history" — inverts the defining property. Canon (`CANONICAL_TERMS_v1.md` §LCT, plus the LCT spec's "Binding: Permanent, verifiable cryptographic link between entity and LCT") confirms LCTs are non-transferable and permanently bound to one entity. Rewrote to say the LCT itself is non-transferable and that what ported was the behavioral line, not the LCT.
- `src/app/context/page.tsx` (glossary, LCT row): added the non-transferability clause that was previously absent from the page entirely (verified by string search — the word "non-transferable" appeared nowhere), which is what made the `/fleet` sentence above readable as true.
- `src/app/fleet/page.tsx` (hestia paragraph): "governance-response conduct" — verified against the hestia source (`derivation.rs`, `dashboard.rs`: "Derived temperament (v3-derived-v1: governance-response conduct)") that this is hestia's own literal internal field name, not a freshly-written phrase. **Did not rename it** to "oversight-response conduct" as the visitor log proposed — that would create a page/implementation mismatch. Instead linked it to `/context#governance-oversight` and noted it's retained for the same reason as SAGE's name, treating it like the site's existing legacy-name policy rather than rewriting it.

## Feedback Reviewed
- Visitor log: 2026-07-30 (4 passes + cross-persona synthesis). Passes 2–4 ran a canon-expansion audit (0 drift across 9 pages, 10 enforced terms — confirmed, no action needed) and a gloss-level audit (3 claimed drifts) plus placement/precision findings. Pass 2 also re-verified 14 of Pass 1's friction items and found only 2 reproduced on a second fetch — the log's own "X is missing needs a re-check, X is wrong doesn't" rule, which I followed: only acted on re-verified findings.

## Cross-check Against Canon — one flagged "drift" was not drift
- The log's Tier-1 fix list proposed re-glossing `/context`'s V3→Validity row (currently "reasoning sound") because Pass 3 argued Validity is about value-claim confirmation, not reasoning quality. Checked `CANONICAL_TERMS_v1.md` §V3 directly: canon's own definition is "**Validity**: How sound is the reasoning?" — the page's gloss is a near-verbatim match to canon. Making the proposed change would have introduced drift, not fixed it. **Did not change this row.** This is the same pattern as prior sessions: an expert-persona pass produces a plausible-sounding but incorrect prescription; the fix is to check the source, not the persona's confidence.

## Changes Made (Site)
- `src/app/fleet/page.tsx`: LCT portable-identity gloss corrected (see Terminology Fixes); hestia governance-response phrasing linked instead of renamed; stray stale "2,500+" cross-reference (line 42) updated to "2,065" to match the home-page fix below.
- `src/app/context/page.tsx`: LCT row gained non-transferability clause; evidence section's session-count paragraph rewritten from "the gap is unreconciled" to reflect that `/` and `/links` now both lead with the audited 2,065 figure.
- `src/app/page.tsx`: SAGE card's "2,500+ raising sessions" (no audit trail per `/context#evidence`) changed to "2,065+", linked to `/fleet` as the source of record — matching the figure `/projects` and `/links` already used.
- `src/app/raising/page.tsx`: the emergent-attractors safety paragraph ("goal-seeking or manipulative patterns... can arise from in-context dynamics") had no evidence tier — the one substantive safety claim on the site not sorted into the three-tier ladder. Checked the broader repo for any logged fleet observation of this; found none. Added a clause stating this is a general in-context-learning risk noted in the literature, not a fleet-logged instance.
- `src/app/projects/page.tsx`: SNARC card's `url` (pointing to the `dp-web4/engram` repo) changed to `repoUrl`, which renders the existing "Repo →" label instead of "Site →" — confirmed via `ProjectCard.tsx`, no new component logic needed. 4-Life card was badged `runnable` with only a `Site →` link and no repo; found `dp-web4/4-life` exists and is public via `gh repo view`, added `repoUrl`.
- `src/app/arc-agi-3/page.tsx`: "#3 — 5,845 actions" gave no population for the ranking. Attempted to fetch the live ARC Prize leaderboard for an entrant count — it renders client-side, no count was retrievable — so added only what's verifiable: this ranks against human competitors on ARC Prize's public leaderboard, not AI submissions, without fabricating a number.

## Cross-Site Drift Alert
- None detected this session — findings were 4-lab-specific (LCT gloss, hestia phrasing, session-count reconciliation).

## Open Items (Deferred)
- Tier 2 (caveat placement, all medium): carry the `/context` "attractor" metaphor flag to its 5 use sites on `/principles` and `/raising`; forward-reference the deflationary section from `/raising`'s intro; add a pointer from the "Identity is not self-concept" card to the deflationary reading; mark the Synthon glossary row's operational marker as instrument-relative (it's defined in terms of "coherence," which the same glossary calls undefined).
- Tier 3 (hygiene, all low): close the `/autonomy` ATP/ADP cycle description with the recharge clause; pre-register the `/raising` scramble control's metric or state a date/blocker; add glossary rows for `hestia` and `Sovereign` with the "not yet in CANONICAL_TERMS_v1" marker `chapter ledger`/`chapter law` already carry; add R6 to `/projects`' Web4 card prose equation; state the audit-monoculture blind spot on `/autonomy`; reword `/fleet`'s "Zero crystallization achieved" to "no crystallization detected through session 100"; link `/fleet`'s `(creating)` to `/raising`; verify badge-pill separator rendering in a real browser; verify the 115/180-session figures on `/raising` carry the evidence-tier qualifier at point of use.
- Not acted on (Pass 1 findings Pass 2 could not reproduce on re-fetch): operator legend below the home-page equation, code path from `/`, Principle 5's evidence link, Principle 1 vs 10 governance contradiction, `/fleet`'s `(creating)`/`S100`/"Account 1-2"/Legion-336/dlPFC items. Re-verified this session's own read of `/fleet` and `/context` and confirmed these are already live — did not duplicate them.
