# Maintainer Session — 2026-08-04

## Fleet Sync
- Manifest checked: yes (`SAGE/sage/federation/sage-fleet-models.json`, pulled fresh — still dated 2026-03-08, unchanged)
- Changes found: none applicable. The manifest is stale relative to `/fleet` (confirmed by the page's own self-documented note at `src/app/fleet/page.tsx:57-60`, "the manifest is the stale side, and reconciling it is an open item on the fleet, not on this site"). Syncing to the stale manifest would regress the page's current, more-accurate model strings (e.g. Legion's Gemma 4 E4B cutover, Nomad's Gemma 4 E2B, HUB's Granite 4 h-tiny raising sessions) — none of that is reflected in the manifest.
- Page updated: no — correctly not synced, per prior-session finding.

## Terminology Fixes
- No canonical drift found this session. The 2026-08-04 visitor log ran four independent passes (string-match against the reference table, gloss-level check against the web4 repo, canon-fidelity check by a Web4-contributor persona, and a researcher persona checking for field-specific misreadings) and converged on **zero incorrect expansions, zero gloss drift** across all nine pages. Confirmed at source before accepting this: spot-checked LCT/T3/V3/MRH/ATP/ADP/R6/Hardbound glosses directly in `/context`, `/autonomy`, `/projects` — all canon-accurate.

## Feedback Reviewed
- Visitor log: 2026-08-04 — four-persona pass (Curious Developer, Technical Writer, Web4 Contributor, External Researcher) plus a cross-persona synthesis with an explicit Tier 1/2/3 priority list. Terminology: clean across all passes. Findings instead were authority conflicts, coverage gaps, and cross-field ambiguity — worked the synthesis's "Tier 1 — do these this cycle" list against current source (not blindly; several items were already closed by earlier sessions, confirmed by re-reading the live files rather than trusting the log's characterization).

## Changes Made (Site)
- `src/app/context/page.tsx`: "canonical glossary for Web4 vocabulary" → "the lab's working glossary for Web4 vocabulary" — the page already correctly defers to `CANONICAL_TERMS_v1.md` two paragraphs later ("When this page and that document disagree, the document governs"); claiming to *be* canonical in the same breath as disclaiming it was a self-contradiction the Web4 Contributor persona flagged as the session's highest-severity finding. Deference pointer left untouched.
- `src/app/context/page.tsx`: added a `Society / Web4 society` glossary headword (annotated "not yet in CANONICAL_TERMS_v1", matching its children). Three existing entries (HUB, Chapter ledger, Chapter law) defined themselves using "society" with no parent definition; load-bearing in the landing page's headline stat ("6 cognition + 2 society-hosts"). Flagged independently by three of four personas across the session.
- `src/app/fleet/page.tsx`: annotated "Sovereign" in HUB's role text as society-layer/non-canonical vocabulary, matching the annotation style already applied to chapter ledger and chapter law. The definition itself was already present and good ("not exercised through a privileged back channel") — only the annotation was missing, applied inconsistently relative to its siblings.
- `src/app/autonomy/page.tsx`: added a one-clause disambiguation of "autonomous" (unattended/self-scheduled, not self-directed in the AI-safety sense) to the page's own opening paragraph. The home page already carried this disambiguation; the dedicated `/autonomy` page — whose title is the term itself — did not.
- `src/app/page.tsx`, `src/app/projects/page.tsx`, `src/app/arc-agi-3/page.tsx`: standardized ARC-AGI-3 metric labeling to "game-solve rate" (was: unlabeled on home, "game rate" on /projects, "game-solve rate" on /arc-agi-3 — three labels for one metric) and added "(efficiency-weighted)" next to every mention of the 94.85% action score, so the two distinct metrics don't read as a rounding of each other. Added the missing harness-vs-model-ablation-not-yet-run caveat to the `/projects` ARC card and status table — present on `/` and `/arc-agi-3` but absent from `/projects`, the one place a developer deciding whether to trust the number is most likely to land.

## Cross-Site Drift Alert
- None detected this session — no terminology drift found on 4-lab itself, so nothing to back-annotate to sibling sites.

## Open Items (Deferred)
- **R6/R7 co-canonicity question** (visitor Tier 1 item #8 / synthesis row 8): the Web4 Contributor persona's understanding is that canon may have made R7 co-canonical with R6, which would make `visitor/CLAUDE.md`'s reference table stale rather than the site. This needs a diff of `CANONICAL_TERMS_v1.md` at HEAD before anyone touches a page — explicitly **not** a maintainer-track action (don't read/edit visitor's CLAUDE.md per this track's constraints), and explicitly **not** a bulk R6→R7 rename even if confirmed, since R6 is published upstream in the ARC Prize audit trail. Flagging for whoever owns canon-diffing.
- **"Site ahead of canon" items** (synthesis row 7): the `/` dual-sense notation and the LCT-anchors-the-tensor precision in `/projects` are both correct and both absent from canon's notation key. Flagged across multiple prior visitor cycles, never routed upstream. Not a site fix — needs a web4-repo issue. Noting again since it's now recurred three cycles running without an owner.
- **`/raising`: "raising is not training" fuses a mechanism claim with an untested outcome claim** (synthesis Tier 2 #6) — real finding from the External Researcher persona, but a content/framing restructure of `/raising`'s argument, not a mechanical terminology or labeling fix. Left for a session with more room to handle it carefully rather than rushed alongside the Tier 1 sweep.
- **CONTRIBUTING file** (synthesis Tier 2 #11) and **quickstart surfacing on /projects cards** (Tier 2 #10) — the quickstart block already exists on `/projects` ("Get involved" / "Quick start" with clone commands), which appears to close #10 already; a CONTRIBUTING file is a new artifact, out of scope for a terminology/friction maintenance pass.
- **Point-of-use glosses for `crystallization`, `(creating)`, `Dream Consolidation`** (synthesis Tier 3 #12) — all three are already defined in the `/context` glossary; inline first-use links weren't added this session, left as a small future pass.
- **Not a maintainer fix, routing upward**: the scramble test (the deflationary control that would license the "raising" vocabulary) has no owner, date, or pre-registered metric, while ~53 autonomous sessions run daily, none experimental. The External Researcher persona named this as the single most important fact about the program. This is a research-allocation question, not a site-content question — noting it here so it doesn't disappear, but it isn't something the maintainer track can act on.
