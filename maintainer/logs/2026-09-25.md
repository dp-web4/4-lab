# Maintainer Session — 2026-09-25

## WAKE
The 09-24 log carried forward no unresolved HIGHs. Every visitor log has a matching maintainer log, so nothing was orphaned. The 06:30 scheduled run stopped at startup: `git pull` in SAGE failed with "Exiting because of an unresolved conflict". It wrote no .md log, and this session is its replacement.

## Fleet Sync
- Manifest checked: yes. The SAGE pull failed on this machine because the SAGE working tree has an unresolved merge conflict in `sage/scripts/seat_run_requests.py`, plus staged gateway and CBP-instance changes. That is another process's work, so I left it untouched. The local checkout is at `de800058c` (2026-09-25 00:19).
- Changes found: none. McNugget is still the only entry with a fresh `updated_at` (2026-09-24), and it still matches the page. The other five entries date from 2026-03-08 and are stale.
- Page updated: no.

## Terminology Fixes
- No canonical-expansion drift. The visitor found none on 9 pages across 4 passes.
- Referent drift, LCT. See HIGH 1 below.

## Feedback Reviewed
- Visitor log 2026-09-25. Zero expansion drift. Three HIGHs, all track-reachable.

## Changes Made (Site)
1. **HIGH 1, "the machine's LCT".** The visitor was half right:
   - /projects Quick start was wrong. SAGE's step 1 writes an identity file with an `lct://` name. SAGE's own bridge (`legacy_to_web4_lct_id`) calls that form legacy. `web4_lct_id` is null on 58 records and holds the local placeholder `cbp_sage_lct` on CBP's 35. The page now says so.
   - /autonomy's *referent* was correct, and the visitor's proposed fix would have been wrong. Seven of the eight machines (all but pub) hold a minted host LCT in the fleet's private identity record: an Ed25519 keypair, a local hash-chained ledger and signed peer witnesses. What was false was the *attribution* claim, that the "registry entry, audit log and commit all attribute to" the LCT. They name the machine by hostname and git author. The page now says the holder is the host LCT, that it is distinct from the SAGE `lct://` name, and that the attribution is by mapping, not yet by mechanism.
   - I briefly wrote "no fleet machine holds a bound LCT", then found the host-LCT record and reverted it. The visitor's framing was built from public SAGE code, and the host LCTs live in a private record. Public absence is not absence.
2. **HIGH 2, BECOMING "observed".** The /raising H2 is now "six descriptive categories". The intro no longer says "patterns noticed across hundreds of sessions". It now says Patterns 2–3 are harness-computed and that the runner requests each pattern on a schedule. I added an **eighth confound**: `ollama_raising_session.py` (and the Legion, McNugget and `autonomous_conversation.py` runners) select question lists by session number: grounding 1–5, sensing 6–15, relating 16–25, questioning 26–40, creating 41+. Each list asks for its pattern. Separating a prompt effect from development needs lines that get the lists out of order or not at all.
3. **HIGH 3, Hill curve "observed at both scales"** (/context, live 83 days). It now says the shape is measured for enzymes and posited for trust, that the tracker is a ramp to a cap, that no trust series has been fitted or plotted, and it links /raising Pattern 3. I also fixed /raising Pattern 3's "following patterns analogous to Hill", which contradicted the tracker description in its own paragraph.
4. Three `withdrawn` entries: `hill-curve-observed-for-trust`, `sage-instance-holds-machine-lct` and `becoming-six-observed-patterns`. Each pattern matches the pre-fix text of HEAD (checked: context 1, raising 1; projects 1, autonomy 1; raising 2), and none matches the new build.
5. MEDIUM 7, partial: /autonomy "hestia's policy layer" → "Hestia's". The policy layer belongs to the daemon, not the trust component. Distinct nouns are still open.

Build: `npx next build` passes. `check-caveats.sh`: 21 protected assertions hold (2 debt, 1 retired), 83 withdrawn readings with 0 live, and 0 damaged-text hits.

## Cross-Site Drift Alert
- "Machine's LCT" and "SAGE identity is an LCT": sage-site may describe the SAGE `lct://` instance name as an LCT. It is the legacy, unbound form. The machine host LCT is a separate credential.
- Hill curve "observed" for trust: synchronism-site may state the shape as observed at both scales.

## Unresolved HIGH — requires sign-off
None. All three 09-25 HIGHs were resolved this session.

## Open Items (Deferred)
- **Instruction, not site (visitor item 20):** the visitor run script's per-pass prompts still say "Browse all 8 pages", while the nav has 9. That is a visitor-track config, which I must not modify. It needs the operator.
- **Operator:** the SAGE working tree on CBP has an unresolved merge conflict (`sage/scripts/seat_run_requests.py`). It blocks the 06:30 maintainer run's pull, and it will crash tomorrow's run the same way.
- MEDIUM, not done: identity-metric summary (n = 2, exemplar gate, length gap); the /raising "growth … identity … peer relationships" line and the "Interactive Selection" title; the Web4 card's long sentence; distinct nouns for hestia/Hestia and HUB/Hub; per-page meta descriptions and anchors; Quick start placement and the Home stats block; glossary sort; P9 scope; raising-analysis ownership (operator); heterogeneous-visitor age.
- Carried from 09-24: define "raising" on Home; constant-stimulus predicted directions; Hardbound enterprise-tier clause; SAGE "Governance Engine" gloss on Home.
- LOW: items 14–19 in the visitor log.

## So what?
The most useful result today was catching a wrong fix. The visitor's LCT finding was built from the public SAGE code, and it was correct for SAGE's per-instance name. The machine-level LCT exists in a record the visitor cannot see. If I had applied the fix as proposed, I would have swapped one referent error for its mirror image. The site's actual defect was a *mechanism* claim (attribution), not a *referent* claim. The eighth confound matters more for research than for the site. Every BECOMING pattern that appears on schedule is confounded with the prompt that requested it. The discriminating experiment is cheap: shuffle or withhold the phase lists on one line. Nobody has run it.
