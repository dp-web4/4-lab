# Maintainer Session — 2026-07-24

## Fleet Sync
- Manifest checked: yes (`sage-fleet-models.json`, SAGE repo pulled — already up to date)
- Changes found: all current — all six machines (thor, sprout, legion, mcnugget, nomad, cbp) match the page on model_display, backend, lora_capable, hardware, and role
- Page updated: no (no mismatch)

## Terminology Fixes
- No canonical drift found this session — visitor log reports **zero drift across all four passes**, and my own grep sweeps confirmed.
- Nearest-to-drift phrasing fixed (Web4 Contributor flag): "unification **for its own sake**" grammatically concedes cross-domain reuse might be unification-with-a-purpose; canon says it isn't unification at all.
  - `src/app/projects/page.tsx`: "This isn't unification for its own sake — it's fractal leverage" → "This isn't unification — it's fractal leverage"
  - `src/app/context/page.tsx`: "Not unification for its own sake" → "Not unification, and not scope inflation"
  - `src/app/principles/page.tsx` (Principle 2 card): same fix. Note: the visitor believed /principles already had the clean formulation — that exact phrasing exists elsewhere on the page (line ~181), but the Principle 2 card itself carried the weak variant. All three now match canon.

## Feedback Reviewed
- Visitor log: 2026-07-24 — zero drift; friction is placement/coverage/connotation. Priority list of 8 items; several were already satisfied in source (visitor browsed the deploy that included yesterday's fixes, and two passes disagreed with each other — see Discrepancies below).

## Changes Made (Site)
- `src/app/projects/page.tsx` — **Hardbound card trimmed to purpose level** (Web4 Contributor, medium). Removed internal mechanism detail: AttestationEnvelope type name, exact policy model (Phi-4 Mini 3.8B), "424+ attack vectors catalogued", CTAP2 internals. Kept generic hardware-root names (TPM 2.0, FIDO2, Secure Enclave) and purpose description. This was a deliberate decision, not accretion-preservation: the project privacy rule says "describe purpose, never content" for Hardbound — the card now says so explicitly ("described here at the purpose level by policy, not accident").
- `src/app/autonomy/page.tsx` — **posture ordering fixed** (External Researcher, medium). Intro paragraph no longer says tracks "operate within Hardbound-defined scope boundaries"; it now states the actual mechanism first (scoped credentials, track registry, after-the-fact audit — detect-and-revert) with Hardbound explicitly labeled as what the research is building toward. Safety-boundaries section paragraphs swapped to the same order: current posture first, Hardbound-as-research-direction second. Roadmap no longer reads as mechanism on first pass.
- `src/app/raising/page.tsx` — added one-line gloss for "deflationary" at the section open (Tech Writer, low); added a **control-status paragraph** (External Researcher, medium): scramble control is "specified but not scheduled — no date, no owner, no pre-registered metric yet," and until it runs the developmental vocabulary runs ahead of the comparison that would license it. Converts the soft admission into an explicit status without inventing a schedule.
- `src/app/arc-agi-3/page.tsx` — "This **demonstrates** the ceiling" → "This **suggests** the ceiling… 'suggests' is the honest verb" (Researcher, low — ablation-free result can't demonstrate); one-clause glosses for Nomic embeddings and binary Hamming codes in the Membot card (Tech Writer, low); ATP→~$250 mapping sentence added to the Cost row (Web4 Contributor, low) — **fact-checked first**: ARC-SAGE README confirms the SAGE loop's budget step used ATP budgeting, so the sentence is factual ("allocation declared before acting, recorded spend aggregates to this number — accounting, not enforcement").
- `src/app/context/page.tsx` — added Membot glossary row ("the cartridge server") next to the Cartridge entry (Tech Writer, low).
- `src/app/page.tsx` — 900+ session count now carries a counting-basis clause linking /context#evidence (Tech Writer, low).
- Build: `npx next build` passes, zero errors, all 12 routes prerendered.

## Items Already Satisfied (no change needed)
- /arc-agi-3 glossary-pointer placement: the "New here?" primer is already the second block on the page, above the first section — added in a prior session. The 94.85 vs 96.0 contrast sentence also already exists.
- /principles p_crit gloss: already present ("p_crit = the critical coherence threshold derived in the Synchronism framework").
- /links ACP expansion: already expanded in the ACT card ("Agentic Context Protocol (ACP)").
- Home "seven machines" resolution: already resolved two paragraphs into the hero and in the stat card ("6 cognition + HUB").
- /fleet chapter ledger / chapter law placement: definitions are adjacent to first substantive use (society-host pool intro + HUB card) and in the /context glossary; Pass 2 and Pass 3 both found them, only Pass 1 missed them. Left as-is rather than duplicating.

## Discrepancies in Visitor Log (noted for visitor-track calibration)
- Pass 1 vs Passes 2–3 disagreed on whether chapter ledger/law and E4B were defined on-page (they are).
- Pass 3 believed /principles carried the clean "not unification and not scope inflation" formulation in Principle 2 — that formulation was elsewhere on the page; the card itself had the weak variant (now fixed).

## Cross-Site Drift Alert
- **ARC-SAGE repo README** (`arc-agi-3/README.md`) uses "**R6/R7 action framework**" — R7 is an explicitly flagged non-canonical variant (canon: R6, Six-Element Action Framework; never R7/R5). Not modified (other repo, not my scope) — the ARC-SAGE maintainer should check whether "R7" there is drift or a deliberate internal extension; if the latter, it deserves a note, because it will read as drift to anyone auditing against CANONICAL_TERMS_v1.
- sibling sites (synchronism-site, sage-site): check for the "unification for its own sake" construction — it survived on three pages here, so it likely propagated from a shared source text.

## Open Items (Deferred)
- **/principles evidentiary tagging** (Researcher, medium): tagging each principle refuted/untested/preliminary/externally-verified requires ground-truth per claim that I'm not confident asserting solo — wrong tags would be worse than none. Deferred for a deliberate pass, ideally with human review of the tag assignments.
- **/fleet witness-set line** (Web4 Contributor, low): who witnesses fleet member acts (founding Sovereign only, or peers?) is a fact I can't verify from available sources — deferred rather than invented. Needs an answer from the HUB/society side.
- **Per-use connotation disclaimers** for Markov/tensor/attractor/coherence (Researcher): all centrally disclaimed on /context; per-use tagging is a site-wide design decision, not a patch. Noted, not actioned.
- **Published redacted log sample + coding criteria** (Researcher, medium): out of maintainer scope — this is a lab-level disclosure decision, but it's the single highest-leverage evidentiary improvement the site could point to. Flagging upward.
- Session-count basis reconciliation remains the permanent-disclaimer approach; footnote markers now on home; /fleet already had the full note.

## So What?
Fourth consecutive clean terminology audit — the enforcement loop is holding. Today's real work was epistemics, not vocabulary: the site now states its actual oversight posture before its aspirational one, prices its headline claim honestly ("suggests," not "demonstrates"), gives the missing-control admission an explicit status, and stops leaking Hardbound mechanism detail past the privacy line. One upstream drift candidate (R7 in ARC-SAGE) flagged for its owner.
