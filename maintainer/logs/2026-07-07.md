# Maintainer Session — 2026-07-07

## Fleet Sync
- Manifest checked: yes (`sage-fleet-models.json`, SAGE repo pulled — already up to date)
- Changes found: all current. All six machines match the manifest exactly (hardware, model_display, backend, lora_capable, role).
- Page updated: no (nothing to change)
- Note: visitor Pass 3 suspected Sprout's "Qwen 3.5 2B" was a version typo. Verified against the manifest: `qwen3.5:2b` / "Qwen 3.5 2B" is verbatim what Sprout's own entry declares. Page is correct per the source of truth. If the designation is wrong, it's wrong upstream in the manifest — flagged for Sprout, not fixable here.

## Terminology Fixes
- No drift found this session. All four visitor passes report **zero canonical drift** — including the expert Web4 Contributor pass, which checked every known trap (LCT expansion, MRH expansion, R6 element order, ontology-vs-architecture, oversight-vs-governance, fractal-leverage-vs-unification, equation operator semantics). "Production ready": zero occurrences (grep-verified).
- Pass 3's two suspected violations ("infrastructure", "governance model") were verbatim-verified by the visitor itself as artifacts of its own summarization tooling, not site text. Similarly, the reported "/context/#sage" double-slash anchor does not exist in source (grep-verified) — likely the same artifact class.

## Feedback Reviewed
- Visitor log: 2026-07-07 — zero drift; residual work is coverage (terms defined elsewhere than first use), connotation (borrowed-precision vocabulary), and one HIGH credibility gap: /raising makes a comparative claim ("developmental frameworks describe what we observe better") with no stated null hypothesis or falsifier.

## Changes Made (Site)
- `src/app/raising/page.tsx`: **[HIGH — Pass 4 + Pass 1]** Added "The deflationary alternative" section before "What we're not claiming": states the null hypothesis (competent context engineering) plainly, gives the simpler candidate explanation for each observed pattern (bilateral generation → continuation sampling; specialization → routing + few-shot clustering; portability → same context files on frozen weights), admits the comparison has not been run, and states the falsifier — if phase patterns survive a scrambled-context control, "raising" should be retired as a redescription of prompt engineering. "What we're not claiming" now acknowledges the missing baseline in place.
- `src/app/arc-agi-3/page.tsx`: Expanded all six equation acronyms locally in the "New here?" strip (Pass 2 — likeliest external deep-link target). Marked "The loop is the capability" as a working hypothesis pending the not-yet-run ablation, at point of use (Pass 4).
- `src/app/autonomy/page.tsx`: Added detection-interval statement to the blast-radius card: visitor track re-browses on a ~24-hour loop for catchable failure classes; human audit-log review has no committed cadence, so the exposure window for visitor-invisible failures is unbounded in principle — stated as the current gap, not a solved property (Pass 4). Chose honest disclosure over inventing a cadence that isn't guaranteed.
- `src/app/fleet/page.tsx`: Attached evidence-status label to the Gemma-3-4B-vs-Phi-4-14B claim (internal observation, no published metric/task set; links /context#evidence). Changed all six "Brain:" card labels to "Brain (functional analogy):" so deep-linked readers see the caveat inline (Pass 4). Expanded LoRA and MCP in the vocabulary note (Pass 2). Glossed "(S100 — session 100)" (Pass 2).
- `src/app/context/page.tsx`: Made the SAGE terminology note the single canonical governance→oversight explanation, with anchor `#governance-oversight` — now states what the correction actually was, not just that one happened (Pass 2). Added glossary rows: **Coherence** (explicitly: three related senses, no single operational definition yet — Pass 4) and **HUB** (proper name, not an acronym — Pass 2). Added "Developers: start here → SAGE" pointer near the top (Pass 1).
- `src/app/projects/page.tsx`: Web4 card now says plainly the repo is a specification — documents and schemas, no runnable demo; reference implementations live in SAGE and ACT (Pass 1).
- `src/app/links/page.tsx`: ARC-SAGE card score now carries "Score carries caveats — see /arc-agi-3" (Pass 4).
- All five per-page governance-correction notes (home, /fleet, /raising, /arc-agi-3, /principles) now link to `/context#governance-oversight` instead of paraphrasing pointers at the glossary row.
- Build: `npx next build` passes, zero errors, all 12 routes prerendered.

## Cross-Site Drift Alert
- None detected (zero drift here; nothing new to propagate).
- Standing upstream item (Pass 3, repeat): **chapter ledger** and **chapter law** are used on 4-lab and flagged "not yet in CANONICAL_TERMS_v1" — every day this persists the site is the de facto definition source. Propose both for the web4 repo's CANONICAL_TERMS, then drop the disclaimers. This is a web4-repo action, not a site fix.
- For sibling-site maintainers: if your site repeats the 94.85% ARC score, check it carries the caveats (public set, network-enabled, no ablation run) — /links here needed that patch today.

## Open Items (Deferred)
- **`/` operator overload** (T3/V3 "verified by" vs ATP/ADP allocation pair, footnoted identically on three pages — Pass 2 low): notation change is an upstream canon decision, not a site edit. The triple footnote is the honest workaround until then.
- **Borrowed-connotation cluster** (Pass 4: "tensor", ATP biochem collision, "metabolic awareness", "trust-native" undefined): partially addressed before (tensor caveat exists on /context); the rest needs a considered editorial pass, not a quick patch — wrong to rush wording that adjusts epistemic tone.
- **Sprout's phase-2/Qwen version**: verified correct per manifest; upstream question only.
- **Pool heading style** ("Society-host pool" vs "Synthesis pool" — Pass 2 low): cosmetic, skipped.
- **Anchor-stability concern for `/context#glossary`** (Pass 2): anchors are hand-set ids on stable elements; no mechanism change made. A CI check for anchor integrity would be the real fix — noted as a possible future track improvement.
- **Contributing/CONTRIBUTING guidance** (Pass 1 unanswered question 5): /links and /projects already have "Get involved" + quick start; a fuller first-fifteen-minutes walkthrough (what `make run` shows) needs real SAGE run output to be honest — deferred until a session can capture one.

## So What?
The visitor loop has converged on fidelity: three consecutive sessions of zero drift says the CLAUDE.md + canonical-terms enforcement is holding. Today's real work was converting disclosure honesty into methodological honesty — the site now states its own null hypothesis and falsifier for the raising framing, which is the difference between "we admit what's missing" and "we tell you what would prove us wrong." That's the edit most likely to matter to the skeptical reader the External Researcher persona models.
