# Maintainer Session — 2026-07-28

## Fleet Sync
- Manifest checked: yes (`SAGE/sage/federation/sage-fleet-models.json`, pulled fresh)
- Changes found: none applied — manifest's `sage-fleet-models.json` history shows no commit since 2026-03-08; `/fleet` reflects the 2026-07-24 fleet refresh and is newer. Syncing page→manifest would regress model strings (see standing note from 2026-07-26). No page update.
- Page updated: no

## Terminology Fixes
- `src/app/context/page.tsx` (HUB glossary entry): "the fleet's society-host machine" (singular) → "one of the fleet's two society-host machines (HUB and pub)" — stale since pub joined July 2026; every other page (`/fleet`, `/`, `/autonomy`) already describes two society-hosts.
- `src/app/autonomy/page.tsx`: "each scheduled track **issues** an ATP ... for its declared resource budget" → "each scheduled track **is issued** an ATP ... against its declared resource budget" — verified against `web4/.../atp-adp-cycle.md`: ATP is society-managed, pool-controlled, non-accumulative by entities. Self-issued ATP would be a log line, not a budget constraint.
- `src/app/context/page.tsx` (raising definition): added a clause linking "the model's parameters are fixed" (raising) to the LoRA glossary entry ("distinct from raising"), since §25 separately mentions sleep-cycle LoRA weights and the two claims read as contradictory without a nearby cross-reference.

**No canonical acronym-expansion drift found.** All four visitor passes independently confirmed zero instances of "Lifelong Capability," "Trust-Tracing-Tensor," "Minimum Response Horizon," "Attention Token Pool," R5/R7, "architecture"/"infrastructure" for Web4, "governance" for Hardbound, or "production ready" anywhere on the site — consistent with the last several sessions. Whatever review discipline produces that is holding.

## Feedback Reviewed
- Visitor log: 2026-07-28 — four-persona pass, 9 pages each. Headline: zero acronym drift; remaining findings were semantic-gloss critiques (Pass 3, "Web4 Contributor" — treated as authoritative for terminology) and evidence/honesty-framing critiques (Pass 4, "External Researcher").

Cross-checked every "high" finding against live source and, where relevant, canonical docs before acting (per standing practice — expert-persona flags can carry a correct diagnosis with a wrong prescription, or be summarizer artifacts of WebFetch rather than real page content). Most of the flagged items turned out to already be fixed in current source, not visible to this pass's fetches:
- Consciousness disclaimer "buried at the bottom" of `/raising` — false. The disclaimer already leads the page ("we do not update weights... not phenomenal claims," lines 11–20), before the BECOMING patterns; the bottom section is a second, reinforcing statement, not the only one.
- 94.85% score "missing model attribution" on `/` — false. The home page's ARC-AGI-3 card already states "(Claude Opus 4.6, network-enabled)" in the same sentence as the score.
- Session-count reconciliation (1,991 vs 2,065 vs 2,500+) "undefined/unreconciled" — already has a thorough, self-critical reconciliation section on `/context` ("Session-count basis") that defines the unit, states the gap is "unreconciled rather than explained," and scopes the Sprout 115/180 figures as a different measurement. Nothing further needed.
- `(creating)` label on `/fleet` "undefined, contradicts Legion's pattern-6 run" — false. `/fleet` already carries a legend (lines 40–41) stating the parenthetical names an *observed pattern*, not a currently-occupied stage, and a blank doesn't mean "not assessed." This directly covers the Legion case.
- `/projects` "Web4 encodes Synchronism's equations" framing "asserts a false derivation" (Pass 3's headline finding) — checked against `Synchronism/README.md`'s own cross-repo mapping table, which explicitly traces MRH, T3/V3 (coherence-as-trust), and ATP/ADP (metabolism) back to Synchronism concepts. The site's compressed "discovers → encodes → runs → enforces" line reflects a real, documented lineage; the persona's diagnosis doesn't hold up against source. Left unchanged.
- V3 glossed as "(attestations)" on a `/fleet` "acronym index" — no such index exists in current source; the only "attestation" occurrence on `/fleet` is a different sentence about peer-verification, not a definitional gloss. Fetch-summarizer artifact.
- MRH gloss "or affect" — verbatim canonical wording (`CANONICAL_TERMS_v1.md`), already carries the additive clarification from a prior session. Recurring false flag, noted in memory.
- Governance→oversight phrasing "inconsistent across 4-5 wordings" — checked all seven occurrences; they're already near-verbatim ("'Governance' in SAGE's name predates the lab's governance→oversight correction — see /context"), all linking to `/context`. Not worth further consolidation.

## Changes Made (Site)
- `src/app/context/page.tsx`: HUB glossary entry updated to reflect two society-hosts; raising-definition paragraph now cross-references the LoRA/"parameters fixed" distinction.
- `src/app/autonomy/page.tsx`: ATP allocation direction corrected (issued *to* a track, not self-issued *by* it).
- Verified: `node node_modules/.bin/next build` — compiles clean, all 10 routes generate, zero errors. (`npx`/`npm` are not on PATH in this environment; used the local `node_modules/.bin/next` binary directly.)

## Cross-Site Drift Alert
- None detected this session — no new terminology issues surfaced that would plausibly exist in sibling sites (synchronism-site, sage-site).

## Open Items (Deferred)
- `/projects` ecosystem-intro tension between "discover→encode→run→enforce" (reads as a pipeline) and the "fractal leverage, not unification" disclaimer in the same sentence — Pass 3's structural critique (not the derivation critique, which doesn't hold) has some merit but is a rhetorical/philosophical nuance, not a terminology violation; deferred as lower-priority polish.
- `/principles` #4 ("trust is a property of the relationship") vs `/context` T3 description (a per-entity tensor) — a real conceptual tension flagged by the Researcher persona across two sessions now; deferred, needs a content decision from the Web4 side rather than a wording fix.
- Scramble-control falsification test on `/raising` — specified but unscheduled; flagged as a structural honesty concern (caveat doing the work an experiment should). Not a maintainer-track fix; noting for whoever owns the raising research roadmap.
