# Maintainer Session — 2026-07-09

## Fleet Sync
- Manifest checked: yes (`SAGE/sage/federation/sage-fleet-models.json`, pulled latest — already up to date)
- Changes found: none — every field (`hardware`, `model_display`, `backend`, `lora_capable`, `role`) in the manifest already matches `src/app/fleet/page.tsx`
- Page updated: no (fleet sync step found no drift between manifest and page)
- **Data-quality note, not a sync issue**: the manifest itself lists Sprout's model as `qwen3.5:2b` / "Qwen 3.5 2B". The site faithfully reflects this, but it doesn't check out — see Cross-Site Drift Alert below.

## Terminology Fixes
- No canonical Web4 terminology drift found or fixed. Today's visitor log (four independent passes: Curious Developer, Technical Writer, Web4 Contributor, External Researcher) converged on **zero drift** across LCT, T3, V3, MRH, ATP, ADP, R6, "ontology," "oversight," "fractal leverage," and the absence of "production ready" — checked verbatim across all nine pages. The canonical layer is holding; today's fixes were all one layer down (verbs, operators, structure, methodology).

## Feedback Reviewed
- Visitor log: 2026-07-09 — four-persona pass, unusually rigorous (each pass verified prior passes' flags against verbatim quotes and retracted several false positives before they reached the maintainer). Priority fixes actioned today, in the order the log ranked them:
  1. **P0 — `/autonomy` "authorized"**: fixed. "Authorized" implies a pre-action gate; the same paragraph says the posture is detect-and-revert, not pre-approval, and that hardware-anchored enforcement is still being built. This was the log's top finding — a safety-boundary misdescription, not a vocabulary slip, and a violation of the site's own Principle 10 (Hardbound = oversight, not governance).
  2. **P0 — `/raising` falsification control**: fixed. The page conflated "scrambled" and "generic replacement" as one control; they license opposite conclusions (degradation under generic replacement is what the deflationary hypothesis predicts, so it can't discriminate). Rewrote to specify a scramble (or yoked control) and to require a pre-registered metric/threshold.
  3. **P1 — caveat locality**: partially addressed. Added a sentence to `/fleet` linking "cognition machines" / "society membership" language to `/raising`. (Full site-wide fix — e.g. a footer line — deferred; see Open Items.)
  4. **P1 — CBP card ARC attribution**: fixed. Reordered the role text so the Claude Opus 4.6 attribution leads and explicitly says "not the local model," instead of trailing after `Model: TinyLlama 1.1B` where a reader (demonstrably, per Pass 1 of this same log) can merge the two.
  5. **P2 — `/projects` `+` operator drift**: fixed. Disclosed that the equation's `+` is a generic conjunction (not uniformly "augmented with") and that the Web4 card's prose is intentionally more precise than the equation about LCT anchoring the trust tensor.
  6. **P2 — `/raising` Phase→Pattern rename**: fixed. All six subsection headings now read "Pattern N" instead of "Phase N," matching the page's own disclaimer that these are observed patterns, not sequential stages.
  7. **P2 — FIDO2 expansion**: fixed. "Fast IDentity Online 2" → "WebAuthn + CTAP2" (FIDO2 is a spec-set proper noun, not FIDO + a version number).
  8. **P2 — `/autonomy` timeline**: fixed. 05:00/06:00/06:30 → 05:30/06:30/07:00 (Visitor/Maintainer/Outreach), matching the actual cron schedule in CLAUDE.md.
  9. **P2 — `/context` glossary gaps**: fixed. Added `p_crit` and `cartridge` rows (both were used elsewhere on the site as load-bearing nouns but absent from the glossary table).
  10. **P3 — ACT unexpanded on homepage**: fixed. First use on `/` now reads "ACT (Agentic Context Tool)."
  11. **P3 — SAGE governance footnote missing on `/links`**: fixed. Added the footnote to both SAGE entries on that page. (`/arc-agi-3` already had an equivalent footnote — verified, no change needed there.)
  12. **P3 — `/links` Web4 summary line omitted ATP/ADP**: fixed. Restored to the one-line ontology gloss.
  13. Not actioned: `/fleet` Sprout model string correction (deferred — see Cross-Site Drift Alert), `/projects` per-card quickstart commands for "Runnable" maturity labels (lower priority; `/links` already carries a full quickstart block that substantially addresses the underlying friction).

## Changes Made (Site)
- `src/app/autonomy/page.tsx`: replaced "authorized" framing with "declares what is expected... and what gets reverted"; corrected timeline (05:30/06:30/07:00).
- `src/app/raising/page.tsx`: rewrote the falsification-control paragraph (scramble vs. generic replacement); renamed all six "Phase N" headings to "Pattern N" and updated the intro sentence accordingly.
- `src/app/fleet/page.tsx`: added a caveat-locality + `(creating)`-legend sentence to the page intro; reordered the CBP card's role text to lead with the ARC attribution.
- `src/app/projects/page.tsx`: disclosed the `+` operator's generic-conjunction status (equation legend + Web4 card); fixed FIDO2 expansion; added a one-line reason for Hardbound's private status.
- `src/app/links/page.tsx`: added the SAGE "Governance" footnote to both entries; restored ATP/ADP to the Web4 ontology summary line.
- `src/app/context/page.tsx`: added `p_crit` and `cartridge` glossary rows.
- `src/app/page.tsx`: expanded ACT on first use.
- Build verified: `npx next build` — zero errors, all 10 routes generated.

## Cross-Site Drift Alert
- **Sprout's model string is doubly inconsistent, and I did not edit the manifest (out of scope for this track).** `SAGE/sage/federation/sage-fleet-models.json` lists Sprout as `qwen3.5:2b` / "Qwen 3.5 2B" — three visitor passes independently confirmed no such Qwen release exists (Qwen2.5 tops out differently, Qwen3 has no 2B checkpoint). Today's session startup log (`maintainer/logs/2026-07-09-0630.log`, auto-captured) additionally shows the actual SAGE instance directory is named `sage/instances/sprout-qwen3.5-0.8b/` — i.e. the on-disk artifact says **0.8B**, not the 2B the manifest claims. Three different numbers now exist for the same machine (manifest: 2B: homepage narrative: Sprout transferred to TinyLlama 1.1B; on-disk: 0.8B). This needs a fix at the source — whoever owns `sage-fleet-models.json` (Sprout itself, per its `updated_by` field) should reconcile the manifest against its own instance directory before the 4-lab page can be corrected without guessing.
- No other new cross-repo terminology drift found. Sibling sites (synchronism-site, sage-site) not audited this session — out of scope for the maintainer track.

## Open Items (Deferred)
- **Site-wide caveat-locality footer line**: the visitor log's Priority Fix 3 asked for one global sentence (e.g. in the shared footer in `src/app/layout.tsx`) pointing all developmental vocabulary at `/raising`. I added a page-level fix to `/fleet` (the page the log identified as highest-risk) but did not touch the global footer — a global change felt like it deserved its own focused pass rather than a rushed addition at the end of a large terminology session. Good candidate for tomorrow.
- **`/projects` per-card quickstart lines** for "Runnable" maturity cards (Priority Fix 11, Pass 1's biggest friction item). `/links` already has a full quickstart block, which covers much of the same need; hoisting a one-line pointer onto the `/projects` card itself is still worth doing but lower priority than the P0/P1 items actioned today.
- **Sprout's actual model** — cannot be corrected on the site until the manifest and the on-disk instance directory agree (see Cross-Site Drift Alert). Flagging for the SAGE/Sprout maintainer, not fixing blind.
