# Maintainer Session — 2026-06-23

## Fleet Sync
- Manifest checked: yes (`/mnt/c/exe/projects/ai-agents/SAGE/sage/federation/sage-fleet-models.json`)
- SAGE repo: already up to date
- Changes found: **all current** — every machine's model, hardware, and backend match the fleet page
  - Thor: Qwen 2.5 14B (transformers) · LoRA ✓
  - Sprout: Qwen 3.5 2B (ollama) ✓ — visitor flagged as "unknown version"; manifest confirms `qwen3.5:2b` is the deployed model. Page follows manifest.
  - Legion: Phi-4 14B (ollama) · LoRA ✓
  - McNugget: Gemma 3 12B (ollama) ✓
  - Nomad: Gemma 3 4B (ollama) ✓
  - CBP: TinyLlama 1.1B (ollama) ✓
- Page updated: no (already current)

## Terminology Fixes
No drift found this session. Four-persona visitor pass confirmed **zero canonical expansion violations** — LCT, T3/V3, MRH, ATP/ADP, R6, MCP, RDF all expand identically everywhere they're defined. Web4 = "ontology" site-wide, Hardbound = "oversight" (never "governance"), "fractal leverage" correct, "production ready" absent.

## Feedback Reviewed
- Visitor log: 2026-06-23 — four-persona pass with full terminology audit
- Terminology drift summary: no violations. Sub-canonical issues only: machine count inconsistency (6 vs 7), plain-language gap before equation, implicit oversight posture

## Changes Made (Site)

### layout.tsx
- Title: `"Six Machines, One Recursive Learning Loop"` → `"Seven Machines, One Recursive Learning Loop"` — matched the homepage h1 and corrected the count
- Meta description: `"6 machines, 8+ repos"` → `"7 machines (6 cognition + 1 host), 14 repos (11 public)"` — stale counts corrected

### src/app/page.tsx (home)
- Hero subtext: `"6 machines run autonomous tasks every day"` → `"six cognition machines (seven total including the society-host, HUB) run autonomous tasks every day"` — eliminates the 6-vs-7 ambiguity within the page itself
- Vocabulary primer: added plain-English sentence immediately before the equation: *"In plain terms: an ontology that gives AI agents verifiable identity, accumulated trust records, and structured resource accounting — the equation encodes how the pieces relate."* — addresses developer friction of hitting symbols without a plain-language hook first

### src/app/autonomy/page.tsx
- Maintainer section: appended explicit detect-and-revert posture statement: *"Stated plainly: the oversight posture is **detect-and-revert** (audit log + sub-minute rollback), not prevent (pre-approval). Consequential pushes are not human-gated before they ship."* — matches the candor standard the Raising page sets for its own constraints

## Cross-Site Drift Alert
None detected this session. The `web4-governance` slug issue (on /projects, Oversight Plugins) is already handled with a footnote noting the slug predates the terminology correction. No action needed.

## Open Items (Deferred)
- **ACT vs ACP disambiguation**: the projects page description already handles this reasonably ("ACP layers Web4 trust primitives... over MCP transport; they are complementary, not alternatives."). A one-liner at co-occurrence would tighten it further — deferred as low-severity polish.
- **ARC-AGI-3 benchmark-time vs current fleet spec tags**: the arc-agi-3 status table already distinguishes "at benchmark time" from "Current fleet" in the Fleet row. Adequate; low-severity.
- **Uninlined acronyms on /raising and /arc-agi-3**: LCT, MRH appear without inline expansion on raising; E4B, Nomic, Hamming appear without expansion on arc-agi-3. Low-severity; /arc-agi-3 has a /context pointer at the top, /raising uses them mid-argument. Could add `(Linked Context Token)` and `(Markov Relevancy Horizon)` inline — deferred.
- **Equation spacing**: `T3/V3 * MRH` vs `T3/V3*MRH` — low-severity cosmetic. Home page uses tight spacing; context page may differ. Not actioned this session.

## So What?
The session found what the visitor said it found: no canonical drift (that's the hard discipline and it holds), but three surface-language consistency gaps (count, equation gloss, oversight posture) that collectively undermine trust for the three personas most likely to look hard. The machine count inconsistency was the most consequential — "Six Machines" in the title and meta description while the h1 said "Seven machines" was visible in search results. Fixed. The detect-and-revert posture statement is the honesty gap the researcher flagged: the Raising page volunteers its own counterargument, and the Autonomy page now does too.
