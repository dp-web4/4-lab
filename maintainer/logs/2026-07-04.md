# Maintainer Session — 2026-07-04

## Fleet Sync
- Manifest checked: yes (`SAGE/sage/federation/sage-fleet-models.json`, repo pulled first — already up to date)
- Changes found: none — every field (hardware, model_display, backend, lora_capable, role) for all six machines (thor, sprout, legion, mcnugget, nomad, cbp) matches `fleet/page.tsx` exactly
- Page updated: no

## Terminology Fixes
- No drift found this session. All four visitor personas (Developer, Technical Writer, Web4 Contributor, External Researcher) independently confirmed zero canonical Web4 term drift — every acronym (LCT, T3, V3, MRH, ATP, ADP, MCP, RDF, R6, SNARC) expands identically across all nine pages and is used correctly in context, not just defined.

## Feedback Reviewed
- Visitor log: 2026-07-04 — four-persona pass, zero terminology drift. Remaining items were presentation/clarity friction, several already resolved in current copy since the log was written against the source (SAGE-governance footnote, ACT/ACP lineage, six-vs-seven count, E4B expansion, "identity is organism" caveat). Confirmed all of those are already in place; focused this session on what was genuinely still open.

## Changes Made (Site)
- `src/app/fleet/page.tsx`: added one line noting machine names (Thor, Sprout, Legion, McNugget, Nomad, CBP, HUB) are proper names, not acronyms — Tech Writer/Contributor passes both flagged CBP/HUB as unglossed bare tokens.
- `src/app/links/page.tsx`: ACT description now states it implements ACP (Agentic Context Protocol) — Contributor pass noted /links couldn't tell a reader ACT implements a protocol; expanded WASM → "WASM (WebAssembly)".
- `src/app/projects/page.tsx`: expanded WASM → "WASM/WebAssembly" in the Oversight Plugins card (ACT/ACP already fully expanded there, no change needed).
- `src/app/autonomy/page.tsx`: turned the "Maintainer track: higher blast radius" paragraph into a visually distinct red-bordered callout box labeled "Known oversight gap" — Researcher pass said this, the site's most important honest limitation, was accurate but buried under the schedule.
- `src/app/context/page.tsx`: (1) softened Synchronism's Hill-function-kinetics language from stating the analogy as established ("describe both...") to explicit "borrowed by analogy... not yet a derivation showing why it must hold at both" — Researcher pass flagged borrowed physics/enzyme vocabulary as outpacing an unreplicated result; (2) added a new "Worked example: one action through the equation" section walking LCT + ATP + T3/V3 + MRH + R6 + ADP through one concrete maintainer commit — Developer and Researcher passes both asked for this, independently, as a way to turn the glossary from a map into a manual.
- `src/app/page.tsx`, `src/app/context/page.tsx`, `src/app/arc-agi-3/page.tsx`: tightened the overloaded `/` operator legend (previously "= verified by (T3/V3) or allocation pair (ATP/ADP)") to explicitly state the glyph carries two unrelated meanings on one line — Contributor pass flagged this as a notation hazard for a newer reader.

## Cross-Site Drift Alert
- None detected this session — all Web4 canonical terms confirmed clean across all four visitor passes.

## Open Items (Deferred)
- **Front door reorg (Developer, HIGH in visitor log)**: current landing page already leads with concrete stats (7/14/31+) and a "New here? Start with /context, then clone SAGE or ACT" CTA within the first section — largely already addresses this. Left as-is; a full hero-order rewrite (concrete hook before "ontology" framing) would be a bigger structural change than warranted by remaining friction.
- **Inspectable artifact (Researcher, LOW)**: publishing a redacted raising-log excerpt + coding rubric — not something a maintainer session can generate; needs real log material and a human call on redaction. Flagging for the Explorer/human track, not attempting here.
