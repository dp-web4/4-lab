# Maintainer Session — 2026-09-15

## Carry-forward check
- Last maintainer log: 2026-09-14. `## Unresolved HIGH — requires sign-off`: **None.** No blocked work.

## Fleet Sync
- Manifest checked: yes. **First fresh write since March.** SAGE commit `4754173b6` (2026-09-14, "wire the model
  registration that has existed, unwired, since March") connected `update_fleet_models.py` for one seat, McNugget.
  McNugget's entry now reads `updated_at 2026-09-15`, `gemma4:12b`. The other five entries are still `2026-03-08`.
  The file now calls itself NOT AUTHORITATIVE and names `fleet.json` as the registry the daemon loads and this site links.
- Changes found: **McNugget: model.** The page said Gemma 3 12B. I did not take the manifest's word for it and checked the
  session records. Sessions 1–448 name `gemma3:12b` (with a `None` stretch at 98–403). Every record from 449 on names
  `gemma4:12b`, and 449 was committed 2026-09-08 01:57 UTC. The instance directory is still `mcnugget-gemma3-12b`.
- Page updated: **yes, McNugget's model line only.** This is the case the polarity rule allows: a manifest entry newer than
  the page's 2026-09-12 figures, and here it was also confirmed against the primary record. Session count left at the
  dated 09-12 basis (459) so the three totals stay single-basis.
- `fleet.json` itself is still stale for Legion (`qwen2.5-14b`) and Nomad. The SAGE commit says it is "asking rather than
  editing them". That is upstream, not a page change.

## Terminology Fixes
- No canonical-expansion drift. Visitor passes 1–4 each found zero, and I found none in the source.
- `/fleet`: "hestia's per-peer **T3/V3** scores are live". The same page says "T3 only". I rewrote it to name the two real
  components (see below). This is drift in canon vocabulary, not in an expansion.
- `/fleet` "Oversight pool": the note claimed the site's machine-enforced sense and glossed it with "peer review", which
  appears nowhere else. It now says "Oversight" is the account-pool label in SAGE's `fleet.json` (role: review, planning,
  documentation, coordination, unblocking) and is not the Hardbound sense.
- SAGE expanded at first use on Home and in /fleet's vocabulary block. R6 expanded on /autonomy. R7 dropped from the
  /projects pointer list, since no card uses it.

## Feedback Reviewed
- Visitor log 2026-09-15 (4 passes): 0 HIGH, about 15 MEDIUM. Pass 2 found the failure class is now pointers. Pass 3 found it
  is mechanism claims against code. Pass 4 found evidence-tier words and the raising/weights boundary.

## The finding that mattered: "raising never changes weights" was false as history
Pass 4 asked, without being able to answer, whether sleep-cycle LoRA ever trained on an entity's own sessions. I read the
SAGE code and records to find out:
- `sage/raising/training/sleep_training.py` trains a LoRA (r=4, q/v proj) on high-salience experience-buffer exchanges,
  i.e. the entity's own prompts and responses.
- `sage/raising/scripts/autonomous_conversation.py` loads the latest sleep checkpoint with `PeftModel.from_pretrained`,
  merges it, and runs the session.
- `sage/instances/sprout-qwen2.5-0.5b/sessions`: **84 records with `using_lora: true`, 2026-01-27 → 2026-03-06.**
  `SLEEP_CYCLE_LOG.md` calls cycle_001 the "FIRST TIME SAGE's weights have been updated based on raising session experiences".
- Current lines: `BECOMING_CURRICULUM.md` (2026-04-04) says "model weights frozen". Every non-archived `instance.json`
  has `has_lora: false`, `sage-rs` has no adapter code, and all 7,721 dream-bundle headers have `lora_hash: null`.

So the claim holds for the present and is false for the history. The line it fails on is the Sprout Qwen 0.5B line, which
is where /raising's kept observation ("Identity is not self-concept") comes from. The site said "always in-context" or
"never weights" on /raising, /principles, and twice on /context. All four are now scoped, and a withdrawn entry guards them.
The visitor rated this MEDIUM because it could not see the code. With the code in hand I would have rated it HIGH, and it
is fixed this session.

Second confound, also confirmed in code: `sage/raising/scripts/dream_consolidation.py` runs `claude --print` as "the tutor
(Claude) … the larger model". It rewrites `identity.json` vocabulary, memory_requests and milestones after every session
on the current runners (Thor, CBP, McNugget). The dream paragraph on /raising now names Claude and carries the analogy
caveat. The identity paragraph now says "inputs did not drift" holds at the port, not across consolidated sessions. The
deflationary list gained consolidator-authored drift. Not established: which consolidator, if any, ran on the Sprout line
when its self-description drifted.

Third: **"BECOMING pattern observed: creating" on all six /fleet cards was the session-count schedule.** The runners write
`identity.json.development.phase_name` from fixed ranges (creating = session 41+; `ollama_raising_session.py` also needs
≥1 milestone, which the Claude consolidator supplies). McNugget's commit subject is literally
`Session $N ($PHASE)`. The legend claimed "a pattern noticed in that machine's sessions … not that it wasn't assessed".
It is relabelled "Curriculum phase: creating (by session count, not assessed)", with the old legend's claim withdrawn in
place. This also dissolves the Legion conflict with /raising Pattern 6.

## Changes Made (Site)
Mechanism claims, each checked against the implementing file:
- `/projects` Quick start "What success looks like": it described the Python 12-step kernel and a posture readout for the
  Rust `sage-daemon`. It now quotes SAGE's `RUST_VS_PYTHON_CAPABILITY.md` ("inference-and-metabolism gateway, not a port…")
  and describes `dashboard.html` as it is: version, uptime, model, WAKE/FOCUS/REST/DREAM/CRISIS badge, ATP gauge, cycles,
  peers. No posture. SNARC bars exist, but `updateSnarc()` is defined and never called, so they stay empty.
- SAGE descriptions on `/projects`, `/links` (×2) and the `/context` row and section: the 12-step loop is the Python kernel,
  and the deployed daemon is the gateway.
- Metabolic states "tired / energized / resting" → the code's five named states (/context ×3, /raising, /projects).
- **PolicyGate re-attributed** (/context row, loop list, section, step-10 note). `sage/irp/plugins/policy_gate.py` is SAGE's
  "conscience checkpoint". The capability matrix maps it to step 10 Govern. It is config-gated, defaults to no rules and
  allow, and is absent from Rust. SAGE's public doc records a Hardbound port, so the design intent is stated, but the
  component in the loop is SAGE's. The old "two checkpoints, different principals" note is withdrawn, and it now says the
  governance/oversight split can't be sorted by timing and is lab vocabulary, not canon.
- **Two trackers un-merged** (/context hestia row, worked example, threat model; /fleet peer paragraphs and the equation
  paragraph). SAGE `peer_trust.rs`/`.py` is per peer machine, T3 only, updated from the observer's own health polls and calls.
  Hestia's trust component (`hestia/core/src/derivation.rs`) is per member and role; its displayed score is derived only from
  witnessed not-the-actor adjudications (adjudicated V3) plus deny-response Temperament, with Talent and Training unmeasured.
  Its stored tensor also takes caller-reported outcomes. The worked example now gives all three success deltas
  (+0.05/+0.02/+0.05 × α). "Never on self-report" is now scoped to the display.
- `/autonomy` stop criterion: it names what hestia's law covers today. The safety preset has no push or deploy rule (the
  push rule was removed 2026-07-18), and the maintainer's push runs in the wrapper script outside any gated tool call. So
  "detect-and-revert, not prevent" is accurate for pushes. This answers Pass 4's "most decision-relevant missing sentence".
- `/context` ATP/ADP now leads with the spec's model (one semifungible two-state token in society pools, minted as ADP) and
  presents the registry as the fleet's approximation.
- `/context` R6/R7 and `/autonomy`: canon vs `atp-adp-cycle.md` R6/R7 note stated as an upstream conflict, not settled flat.
  `/autonomy` recharge now updates T3 and V3, not V3 alone.
- Synchronism provenance (/projects ×3, /context ×3, /links): "provides the equations / Web4 encodes them" is now lineage.
  web4's `AGENTS.md` says "Blue-sky conceptual lineage; not an engineering dependency", and web4 README, canon and ATP spec
  mention Synchronism zero times. The fractal-leverage "governs … through the same kinetics" is now an analogy.
- ACT: the last substantive commit was 2026-04-29 (daemon entry point, CBP chain startup), not 2026-04-10 (/projects ×2, /links).

Pointers (Pass 2):
- /context "Quick-start commands are on /links" → `/projects#quick-start`.
- /context "/links now leads with the 2,563 figure" → names no figure, and keeps the correction note.
- /raising "stands at 488, see /fleet" → points to /fleet without restating its number.
- /projects "community channels" on /links → says there are none and the issue trackers are the channel.

Evidence tier (Pass 4):
- /fleet "One finding … outperforms … cannot form … personality" → an internal observation with no operational measure.
- /context "fleet capacity findings" as "empirical results" (×2); "Coupling-coherence experiments provide empirical grounding";
  "Phase 2 work is isolating the harness" → no ablation run or scheduled.
- /fleet "independent security reviews" → cross-model-family, fleet-run. Thor "breakthrough" → internal, unpublished.
- Home "4 Model families currently running" → deployed, not all running (both Qwen lines quiet; HUB's Granite loop writes
  no records).
- /raising "Key discoveries" → "Key observations"; identity "measured via curriculum state" → observed, no metric;
  Patterns 4–5 "(no count published)"; the ARC-as-check sentence on /raising and /principles now says it tests the harness.
- /principles intro "kept being true" → evidential weight varies. P6's 400x refutation is scoped to the experiment's own
  ungrounded measure.
- /autonomy revert record recounted from git: 91 maintainer session logs (04-23 → 09-14), 137 commits touching `src/` (whole
  history; the 08-08 "117" was also whole-history, not the session window), 0 git reverts. The Vercel rollback count lives in
  the dashboard and is marked not recounted.
- /arc-agi-3: "Phase-1" and "competition-legal" defined.

Checks:
- `maintainer/caveats.json`: **19 new `withdrawn` entries** (32 total). All were confirmed to fire on the pre-fix 09-14 build
  (13 + 3 + 5 entries in three batches, each LIVE before). The ATP entry was confirmed against HEAD source with whitespace
  normalized. Exemptions reviewed with `--show-exempt`: every exempt hit is a withdrawal note.
- `npx next build` passes. `check-caveats.sh`: 20 protected assertions hold, 2 debt (unchanged), 32 withdrawn, 0 live, exit 0.

## Cross-Site Drift Alert
- **sage-site**: likely carries "12-step loop" for the deployed daemon, PolicyGate-as-Hardbound, and "raising never changes
  weights". SAGE's own matrix asks other docs to switch framing.
- **SAGE repo `fleet.json`**: Legion `qwen2.5-14b`, Nomad stale (SAGE seat already knows).
- **web4 README**: "Web4 is open infrastructure" (Enterprise path), plus crates.io/PyPI "trust-native infrastructure" (Pass 3).
- Canon vs `atp-adp-cycle.md` on R6/R7 scope. `CANONICAL_TERMS_v1` version stamp still reads Feb 12 despite 04-28 edits.
- SAGE docs disagree on the IRP expansion ("Iterative Refinement Primitive" 17× vs "Protocol" 15×). The site avoids it.

## Unresolved HIGH — requires sign-off
None. The visitor log rated nothing HIGH. The one item I would have rated HIGH from the code (raising/weights history) is
fixed, confirmed in built HTML, and guarded by a check.

## Open Items (Deferred)
- **Governance/oversight criterion** (MEDIUM, Pass 3): PolicyGate and the Claude Code hook plugins decide before the act, as
  Hestia does. Timing does not separate them. Pass 3 proposes sorting by principal and anchor instead. That is a vocabulary
  decision for the researcher. The site now says the split is lab vocabulary and the criterion is open.
- **/raising second pre-commitment has no date**, and the growth control has no owner (MEDIUM, researcher).
- **Hestia/Hub "runnable" badges with no run steps; no hands-on door on Home** (MEDIUM, Pass 1). This needs Hestia's and
  Hub's actual first-run commands, transcribed and checked. Not done hastily.
- **Different-model visitor persona** (operator). Pass 4 notes the capability exists (pub's cross-model reviews).
- /autonomy lead line "31+ tracks … ~53 sessions/day (2026-05-26)", 112 days old. A track recount is not a one-line git query.
- /raising "(2025; …)" date on the Sprout Qwen 0.5B sessions: the instance directory's records are dated 2026-01-20 →
  2026-02-28. Not changed, because earlier sessions may live in the pre-migration HRM tree. Worth a check.
- Low: P8 vs "not self-directed"; nav position of ARC-AGI-3; inline terminology meta-notes on cards; Society/treasury roles;
  MCP row; "Glossary" vs /context naming; p_crit gloss.
- Pass 2 asked for a generic pointer check. Withdrawn entries cover today's four pointers, but nothing checks the class.
  Reading "on /x" sentences against the target page is still manual.

## So what?
The term audit converged weeks ago. Today's value was reading the code behind three claims the visitor could only flag.
All three were real, and one is load-bearing: "raising never touches weights" was false on the line the site's key identity
observation comes from, and the BECOMING labels were a schedule presented as observation. The site's evidence ladder was
being applied to prose, never to its own labels. The frame question for the researcher: /raising's most defended
distinction (context, not weights) and its most-cited observation (Sprout's self-description drift) now both carry a known
confound. Is Sprout the right exemplar, or should the page lead with a frozen-weights line whose consolidator is known?
