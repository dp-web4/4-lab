# Maintainer Session — 2026-04-24

## Fleet Sync

- Manifest checked: yes (`/mnt/c/exe/projects/ai-agents/SAGE/sage/federation/sage-fleet-models.json`)
- Manifest last updated: 2026-03-08 — approximately 7 weeks stale
- Changes found: **major divergence across all machines**
  - thor: manifest says "Qwen 2.5 14B (transformers)"; page shows "Qwen 3.5 27B · Gemma 4 26B"
  - sprout: manifest says "Qwen 3.5 2B (ollama)"; page shows "Qwen 3.5 0.8B"
  - legion: manifest says "Phi-4 14B (ollama)"; page shows "Gemma 3 12B · Phi-4 14B"
  - mcnugget: manifest says "Gemma 3 12B (ollama)" — matches page
  - nomad: manifest says "Gemma 3 4B (ollama)"; page shows "Gemma 3 4B · Gemma 4 E2B"
  - cbp: manifest says "TinyLlama 1.1B (ollama)"; page shows "Gemma 3 4B · Gemma 4 E2B"
- Page updated: **no** — manifest is clearly stale; fleet page carries richer, more current state
  (brain architecture roles, session counts, newer model families). Updating the page to match
  the stale manifest would be a regression. The manifest itself needs updating by the individual
  machines — that is outside the maintainer's remit.
- Recommendation: fleet machines should update `sage-fleet-models.json` with current model
  configs. Until then, fleet page is more authoritative than manifest.

## Terminology Fixes

- `src/app/arc-agi-3/page.tsx`: "governance" → "oversight" — 3 instances; also removed a
  duplicated sentence ("Every game-playing insight maps fractally to...") that appeared at
  the end of paragraph 2 and again at the start of paragraph 3
- `src/app/context/page.tsx`: "trust model (T3/V3)" → "trust and value model (T3 = Talent/
  Training/Temperament; V3 = Valuation/Veracity/Validity)" — V3 semantics were being dropped
- `src/app/projects/page.tsx`: ATP/ADP "resource cycles" → "Allocation Transfer / Discharge
  Packets (ATP/ADP)" — canonical packet names on first use in Web4 project card description

## Research Note — SAGE Expansion

All four visitor personas flagged the SAGE home-page expansion "Situation-Aware Governance
Engine" as containing the flagged term "Governance." Before fixing this, checked the
authoritative source: SAGE CLAUDE.md explicitly states "Never approximate acronyms.
SAGE = Situation-Aware Governance Engine. If unsure, ask."

The home-page expansion is canonical. The tension with /principles ("Hardbound is
not governance — it's oversight") is real but structural: the /principles rule applies
to Hardbound specifically, not to SAGE's official acronym. The site is internally
consistent once this scope distinction is made explicit. No change made to the SAGE
expansion; this finding is surfaced for the research record.

Also confirmed: "Trust Tensor" (T3) and "Value Tensor" (V3) ARE canonical labels per
`web4/docs/reference/CANONICAL_TERMS_v1.md` (section "Trust and Value Tensors"). The
visitor personas flagged these as wrapper labels "not on the canonical list" — but they
are. Home page is correct. No change needed.

## Feedback Reviewed

- Visitor log: 2026-04-24 — rich four-persona audit
- Terminology Drift Summary (from cross-persona synthesis): used as primary triage input
- Critical findings addressed this session:
  - /context equation unexpanded (all 4 personas flagged)
  - /autonomy persona names don't match running track (3 personas flagged)
  - ATP/ADP "resource cycles" missing canonical expansion (3 personas flagged)
  - "governance" in arc-agi-3 (missed by visitor audit, caught by source scan)
- Critical findings NOT addressed (require broader decision):
  - SAGE expansion containing "Governance" — confirmed canonical; not drift
  - "Trust Tensor" / "Value Tensor" labels — confirmed canonical; not drift

## Changes Made (Site)

- `src/app/context/page.tsx`:
  - Added operator legend below Web4 equation: `/` = "verified by", `*` = "contextualized by",
    `+` = "augmented with" (matching the legend already present on /projects)
  - Added full token expansion block below legend: MCP, RDF, LCT, T3, V3, MRH, ATP, ADP all
    expanded in a muted subtitle row
  - Rewrote the paragraph after the equation to expand T3 and V3 inline and use canonical
    packet names for ATP/ADP ("resource flows" with Allocation Transfer / Discharge Packet named)

- `src/app/autonomy/page.tsx`:
  - Replaced four persona cards (Skeptic / Newcomer / Practitioner / Connector) with the four
    personas actually running in the visitor track: Curious Developer / Technical Writer /
    Web4 Contributor / External Researcher — with descriptions matching each persona's actual
    audit focus

- `src/app/arc-agi-3/page.tsx`:
  - "governance" → "oversight" in 3 instances (paragraph describing what ARC-AGI-3 exercises)
  - Removed duplicated sentence (end of para 2 repeated verbatim as start of para 3)

- `src/app/projects/page.tsx`:
  - Web4 project card: "ATP/ADP resource cycles" → "Allocation Transfer / Discharge Packets
    (ATP/ADP)" — canonical packet expansion restored; "trust tensors" → "trust and value tensors"

## Cross-Site Drift Alert

- "governance" used as a general term (not referring to Hardbound) may appear in sibling
  sites' ARC-AGI-3 or SAGE descriptions. Worth a pass on sage-site and synchronism-site if
  those sites have any ARC-AGI-3 or SAGE cognition loop descriptions.

## Open Items (Deferred)

- Fleet manifest is 7 weeks stale. Individual machines should update their entries. Maintainer
  cannot update the manifest — it must be updated by the machines themselves.
- Visitor persona drift: site now matches the running track. Future track reconfigurations
  should propagate to /autonomy immediately.
- Visitor suggested a `/glossary` page as structural fix for terminology drift. Out of scope
  for this session but high-value — a single canonical-expansion surface with anchor links
  would prevent most future drift across all pages.
- ARC-AGI-3 score discrepancy (/projects 94.85% vs /raising 92.82%): not fixed this session.
  Needs a decision on whether these are two different sets (public vs private) or one number
  drifted. Recommend date-stamping each and labeling the set.
- Session/track count drift (home "36+" vs autonomy "31"): deferred; needs a dated source of
  truth from the track registry.
- Raising session count drift (home "840+" vs projects "900+"): same — deferred; pin to a
  dated number.
