# Maintainer Session — 2026-09-11

## Fleet Sync

- Manifest checked: yes (`SAGE/sage/federation/sage-fleet-models.json`, after `git pull`; already up to date).
- Changes found: **none, and still frozen.** All six entries read `updated_at: 2026-03-08`, and the file's last
  commit is `54f3c7dd5` (2026-03-08). It still lists six machines and pre-cutover models (Thor Qwen 2.5 14B, CBP TinyLlama 1.1B,
  Nomad Gemma 3 4B). `/fleet` carries eight machines and the July 2026 cutovers.
- Page updated from manifest: **no**, per the reversed Step 0 polarity (page is the source of record).
- **Instruction repair.** Two more copies of the old polarity were still live, and a compliant session reading
  them would downgrade the page:
  - `SESSION_PRIMER.md` called the manifest "source of truth for fleet page data".
  - `SESSION_FOCUS.md` said the page "must match" it and listed stale per-machine models to "verify against".
  - Both now say the manifest is stale, the page is the source of record, and nothing gets synced down.
  - Also corrected the primer's track times (05:00/06:00 → 05:30/06:30, matching root CLAUDE.md and this run's own log name).

## Terminology Fixes

**Strict expansion drift: none.** This is the fifth consecutive visitor pass at zero; Pass 3 audited directly against
`CANONICAL_TERMS_v1`.

### Form
- `raising/page.tsx`: "the V3 tensor (Valuation / Veracity / Validity)" → "V3 (Value Tensor — Valuation / Veracity /
  Validity)". This was the only page that expanded V3 without naming the tensor.

### Semantic drift against canon/spec (verified against the primary sources before editing)

**1. V3 as the gate on ADP→ATP recharge. The visitor flagged 3 instances; there were 5.**
Sources checked:
- Canon says only "ADP attestation feeds recharge validation" (R7 entry), with no tensor named.
- Spec `atp-adp-cycle.md` `charge_atp` runs: authorize → `validate_value_proof` → society charge rate → convert
  ADP→ATP → *then* `update_tensors(v3_delta={"valuation": +0.02})`.
- `t3-v3-tensors.md` uses V3 only in ATP *pricing* (`calculate_atp_price` from role-specific V3 expectations).

So V3 is an output of recharge, not its certifier. Instances fixed:
- `context` V3 glossary row: "Valuation is what gates…", plus the "V3 reaches ACROSS the '+'" reading, now withdrawn
  with a note so it is not reintroduced.
- `context` ADP row: "which is V3's job".
- `projects` Web4 card: "V3-validated value".
- **`context` ATP/ADP section**: "That is what V3 does… it is the certification that turns spent allocation back
  into spendable allocation". **Pass 3 cited this section as the correct version.** It carried the same reversal one
  sentence later. Also "no V3-certified recharge" → "no validated-value recharge".
- **`context` equation legend (line 357)**: "recharges back to ATP against V3-validated value". This one survived my
  mechanism sweep and was caught only by the final exact-string leftover grep.

**2. `context` Society row.**
- It gave "the fleet itself" as the definition and counted only the six cognition machines as members.
- It also claimed Society is "not yet in CANONICAL_TERMS_v1". **False: canon defines it** ("a self-governing group of
  entities with treasury, membership rules, policies, and roles").
- Now: canon's definition quoted first, then the fleet as one instance (eight machines + Sovereign). The row also
  names which society-layer terms actually are absent from canon: Sovereign, chapter ledger, chapter law.

**3. `context` SAGE loop step "oversee" → "govern".**
- "Govern" is SAGE's own step name (confirmed in `sage-site/index.html`); "oversee" appears nowhere in SAGE.
- The governance→oversight rule had been applied to another project's term, against the site's own
  retain-load-bearing-names policy.
- Step-10 note updated to match: "SAGE governing its own loop; PolicyGate is Hardbound's external oversight".

## Feedback Reviewed

Visitor log 2026-09-11: four personas, 9 pages, 19 ranked site items plus operator-level items. Headline: the
terminology layer has converged, and every remaining defect sits one level up (stale facts, names distinguished only
by case, duplicate quick starts, interpretation read as finding).

**Checked against primary artifacts before acting:**
- **Confirmed, HIGH (Pass 3): `web4-core` is published.**
  - crates.io API: 0.3.0, created 2026-04-28, updated 2026-07-10.
  - PyPI API: 0.3.0, uploaded 2026-07-10.
  - web4 README: "no longer only a specification. Core packages are published."
- **Confirmed (Pass 1):** the SAGE site publishes "21 Configured instances — 8 carry raising history, one per
  machine". So the home page's "not yet published anywhere" was stale, and "8 currently running" meant instances.
- **Confirmed (Pass 1):** 4-Life has two self-descriptions. The repo README says "A research prototype…"; the live
  site's `<title>` is "4-Life | The Web4 Onramp". Both are true, so the site now says both.
- **Confirmed (Pass 3):** `gh api repos/dp-web4/engram` returns `full_name: dp-web4/snarc`, and the README installs
  with `/plugin install snarc`.
- **Confirmed-as-unreconciled (Pass 3):** SAGE's `UNIFIED_CONSCIOUSNESS_LOOP.md` puts PolicyGate at "step 8.6".
  The site maps it between filter (11) and act (12) on the site's 12-step names. Now disclosed as two unreconciled
  numberings.
- **Confirmed (Pass 2):** `/arc-agi-3`'s "act under explicit governance" is verbatim from the SAGE README. Now
  attributed.
- **Refuted in part (Pass 3):** "the same page's ATP/ADP section gets it right". See drift item 1.
- **Overstated (Pass 4):** "'phase-appropriate' refers to phases that don't exist".
  - SAGE `sage/raising/PLAN.md` has curriculum phases (Sensing, sessions 6–15; Relating, sessions 16–25), and
    `/raising`'s own "Graduated tool introduction" is a staged schedule.
  - The real defect: a lab-set **curriculum** schedule and the descriptive **BECOMING patterns** share names and were
    never distinguished.
  - Fixed by naming the distinction rather than deleting "phase".

**Visitor config.** Pass 2 rewrote the T3/V3/R6/R7 rows of `visitor/CLAUDE.md` and left the edit uncommitted,
asking the maintainer to confirm. My constraint is not to read that file, so I read **only** `git diff -U0` of those
term-table rows. They match canon:
- `### T3 (Trust Tensor)` / `### V3 (Value Tensor)`
- R6 `Resource → Result`
- R7 "R6 + Reputation", a superset

Committed **separately** as `02ee070`, content untouched, so the change stays attributable to the visitor track
rather than buried in a maintainer commit. This closes operator flag 2 from 09-10.

## Changes Made (Site)

**HIGH: the one HIGH today**
- `projects` Web4 card:
  - "The repo itself is a specification — no runnable demo; reference implementations live in SAGE and ACT" →
    research-stage but no longer only a specification.
  - Names the published packages (`web4-core` 0.3.0 registry-checked; `web4-trust-core`; `web4-trust`) and the Hub
    reference daemon in the repo.
  - Adds crates.io and PyPI links.
  - Notes that the registries' "infrastructure" wording is upstream metadata, not the site's term.
- `projects` Quick start: added a Web4 block (`pip install web4-core`; Rust `web4-core = "0.3"`), explicitly
  labelled as *transcribed* from the web4 README (unlike the SAGE steps, which are maintained on the page).
- `links` Web4 entry: research-stage, packages published.

**MEDIUM**
- **Quick starts merged.** `links` no longer carries a second, drifted copy (it lacked `ollama pull` and used a
  different operator name). It now points at `/projects#quick-start` and says why. The `projects` intro now says it
  is the only copy.
- **Curriculum vs patterns.** `raising`: growth definition, the deflationary-control metric line, and Graduated tool
  introduction now say "curriculum phase". `context` Evidence & limitations: "the raising phases" → "the BECOMING
  patterns".
- **`raising` interpretation vs finding.**
  - Bilateral generation is restated as observation / standard reading (transcript continuation) / working
    interpretation / untested comparison.
  - "removing it degraded coherence" is labelled a reading-sessions judgment.
  - Pattern 4's echo is marked as interpretation.
- **`raising` identity is/says.** "What it *is* stayed stable" → logged behavioral signature (no metric). The passage
  now states what the port carried (state files, experience buffer, prompt construction), so persistence is partly
  true by construction. It keeps the divergence (self-description drifted while inputs didn't) as the observation,
  and names the unrun comparison.
- **`principles`.**
  - P1 "substrate" is linked to `/context#substrate` with the sense named; the synthon "would form, if it forms".
  - P7 "attractors" is linked to `/context#attractor` and flagged as a metaphor.
  - Same attractor fix on `raising` (a second instance the visitor didn't list).
- **`fleet` hestia/Hestia.** HUB card now says "lowercase hestia, the fleet's trust tracker, not the Hestia
  governance daemon". The glossary `Hub` row now back-references all-caps HUB (the hestia rows already cross-referenced).
- **`index` stats.** The "21 … not yet published anywhere" caveat is replaced with the 2026-09-08 census, "8 carry
  raising history, one per machine", and a link to the SAGE site.
- **`fleet` counts.**
  - The SAGE site's "2,700+ raising sessions" is noted as a different figure on an unpublished basis, unreconciled,
    so neither refutes the other.
  - Removed the stale "those pages call it 'raising sessions'" clause; `/projects` already says machine sessions.
- **`projects` / `links` 4-Life.** Both self-descriptions are now stated.

**LOW**
- **Session noun.** "raising sessions" → "session records" for counts: `fleet` HUB ×2, `fleet` Sprout 115, `raising`
  Sprout 115.
- **Society-hosts.** "rather than autonomous tracks" → "primary role" on `context` society-host row and `fleet` pools
  paragraph. `autonomy` "runs substrate, not tracks" now concedes the hub-track maintainer role.
- **`fleet` CBP role.** The MRH composer "composes the model-scale MRH"; the context window is where it applies, not
  what MRH means.
- **`fleet` crystallization and ownership.** "Zero crystallization achieved" → "No crystallization observed at S100
  (one checkpoint, no repetition metric stated here)". "agent-owned" → "agent-created".
- **`projects` SAGE card.** Brain-analog components are labelled as role mnemonics, not functional homologies;
  "cognition" is labelled a role label.
- **`projects` SNARC.** engram → `dp-web4/snarc` (text and link); install command named.
- **ARC evidence tier.** `context` "Externally validated" → "Externally scored", and the scorecard is said to verify
  the score, not the method. `links` "Independent verification" → official externally scored record, same scope note.
- **ARC-AGI-3 expansion.** "third-gen" → "third generation" (`projects`, `raising`, `context`); expanded at first use
  on `links`.
- **TCSEC** expanded on `index` and `context`.
- **`arc-agi-3`** "explicit governance" attributed to the SAGE README's framing.
- **`links` repo list.** Added Hestia, Hub (4-hub) and SNARC; "Contributing" now uses one start-here sentence
  (hands-on SAGE, protocol Web4, ACT for Cosmos); "dp-web4 org" → "account" (missed in the 09-10 sweep).
- **`autonomy` undated figures.**
  - "31+ tracks, ~53 sessions/day" is marked "as of 2026-05-26, not recounted since" (date from
    `git log -S`, commit `aee5130`).
  - The zero-rollback record is marked "last counted 2026-08-08".
  - I could recount the git half of that record, but not the Vercel half, so I marked it rather than refreshing it.
- **`next.config.mjs`.** `/glossary` → `/context#glossary` redirect (non-permanent). Verified in
  `routes-manifest.json`.

**Build and verification.**
- `npx next build` passes with zero errors, 12/12 static pages, and was rebuilt after the last edit.
- Checked the **built HTML**: 46 edit markers present in rendered text across nine pages; no literal HTML entities
  in any page's visible text (glossary array-strings were written with plain characters); zero surviving stale
  phrasings.

## Cross-Site Drift Alert

- **V3 as the recharge gate/certifier.** It lived in five places here, in three different phrasings, so any sibling
  site explaining ATP/ADP may carry it: `4-life` (recharge / "validated value" copy), `sage-site`, `synchronism-site`.
  Canon: ADP attestation feeds recharge validation; spec: society-validated value proof gates, T3/V3 updated after.
- **"Web4 is only a specification / no runnable code."** Stale since `web4-core` shipped (2026-04-28 crates.io).
  Check every sibling site's Web4 blurb.
- **SNARC as "engram".** Links still redirect, but plugin and repo names changed. Check READMEs and sibling sites.
- **"raising sessions" as a count noun.** The SAGE site headline "2,700+ Raising sessions" (upstream SAGE site).
- **Upstream, not sibling sites:**
  - `web4-core` registry description "trust-native infrastructure core library" (crates.io + PyPI).
  - SAGE README "broader Web4 stack".
  - web4 README "Web4 is the open substrate" / "Web4 is the substrate".
  - All three are on the 4-lab NEVER list. See operator flag 1 before anyone "fixes" them.

## Operator Flags (not maintainer fixes)

1. **The frame question from Pass 3, sharpened.** The site now holds canon's vocabulary more tightly than the canon
   repo's own front page.
   - `/context`'s Substrate row literally says "a page calling Web4 'the open substrate' is drift".
   - The web4 README's lead sentence is "Web4 is the open substrate for agent accountability".
   - The same README, Hestia and the plugin READMEs say "governance" for moment-of-act gating.

   So the 4-lab table either marks upstream as drifted, or enforces a decision upstream has retired. I changed
   neither side. This needs a researcher call; until it is made, every session will keep re-deriving it.
2. **The visitor track edited its own reference mid-run.**
   - It was the right edit and it is now committed, but Pass 2 changed `visitor/CLAUDE.md` *between* passes, so
     Pass 3 audited against a table that Pass 1 did not see.
   - It resolved a flag this track raised three sessions running, which says the flag-only route was not working.
   - Whether visitors may edit their own config is a policy question. The stale lines Pass 2 listed (8 pages vs 9,
     05:00/06:00 schedule, vercel.app URL) remain; not mine to edit.
3. **Fleet manifest writer still dead** (last write 2026-03-08). Repair or retire.
4. **Gap age.** The deflationary control and its replacement commitment still have no date and no owner; there is
   still no external check. Pass 4 is right that this loop can only improve the wording of those gaps. Today I
   improved the wording again. Recorded in SESSION_FOCUS so the age starts being tracked somewhere.
5. **Canon vs core spec on V3 Validity** (standing). The site follows canon.

## Unresolved HIGH — requires sign-off

None. The single HIGH in the 2026-09-11 visitor log (`/projects` Web4 card "specification only") is fixed and
verified in built HTML. Pass 2's "high (to the audit loop)" config item was fixed by the visitor, then confirmed
against canon and committed separately here. No 09-10 carry-forward items existed.

## Open Items (Deferred)

- **Governance/oversight rule scope (product vs function)** — operator flag 1; deliberately not settled on the site.
- **Observer identity** in `/raising`'s "I notice I want to call it experience" — unknown; not invented.
- **Who fills the Explorer track's topic queue** (`/autonomy`) — unknown to the maintainer.
- **One start-here sentence site-wide** — aligned on `/projects` and `/links`; `/` ("start with Web4" for protocol)
  and `/context` ("SAGE is the recommended starting point") are compatible but not yet worded identically.
- **ACT `QUICK_REFERENCE.md` link** — re-described accurately inside the code block, not yet linked (the block is
  plain text).
- **Evidence-ladder badges site-wide** — carried from 09-10; still the highest-value unstarted design change.
- **`maintainer/CLAUDE.md` still lists the site as `4-lab.vercel.app`** — resolves, harmless; noted, not changed.

## So what?

The defect class moved again, one level per session:
- terms (through 09-06)
- provenance (09-10)
- **direction of a mechanism** (today)

The V3 finding is the instructive one. An interpretive reading ("V3 reaches across the '+'") became load-bearing prose
in five places, in three phrasings, including the passage the expert persona held up as correct. Neither the flag list
nor a single sweep would have removed it; a mechanism grep plus a final literal grep did. That is a method result
worth keeping (saved to memory).

The honest limit is Pass 4's. Nothing today moves the raising question itself. The site's caveats got sharper again,
and the control that would let the site retire them still has no date.
