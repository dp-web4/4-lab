# Maintainer Session — 2026-08-08

## Fleet Sync
- Manifest checked: yes (`SAGE/sage/federation/sage-fleet-models.json`, `git pull` → already up to date)
- Changes found: **none — and the manifest remains badly stale.** Every entry is still
  dated `2026-03-08`, five months old. It lists 6 machines; the page carries 8 (HUB, pub
  are absent upstream). Model strings disagree in the manifest's favour nowhere:
  manifest Thor = Qwen 2.5 14B / page = Qwen 3.5 27B; manifest Legion = Phi-4 14B /
  page = Gemma 4 E4B (the page even records the phi4 line as dormant); manifest
  Nomad = Gemma 3 4B / page = Gemma 4 E2B; manifest CBP = TinyLlama / page = Gemma 3 4B
  with the TinyLlama line marked archived.
- Page updated: **no — deliberately.** CLAUDE.md calls the manifest the source of truth,
  but following that instruction today would regress the fleet page by roughly four
  months. This is the fourth consecutive session reaching this conclusion. The manifest
  is downstream of the machines, and the machines have stopped updating it.
  **Route upward:** either the fleet machines resume owning their entries, or the
  workflow's Step 0 should be rewritten to say "verify, and treat staleness as the
  expected case." Today's visitor log independently reached the same place (Tier 3
  item 20: *"the upstream fleet manifest is significantly staler than the page; do not
  sync the page to it"*).

## Terminology Fixes
**No expansion-level drift found this session — and this was verified, not assumed.**

Layer 1 of the visitor's audit reports zero drift across all nine pages, checked
independently by all four personas: LCT = Linked Context Token (7 pages, identical),
T3 = Talent / Training / Temperament (8 pages), V3 = Valuation / Veracity / Validity,
MRH = Markov Relevancy Horizon, ATP = Allocation Transfer Packet, ADP = Allocation
Discharge Packet, R6 = Six-Element Action Framework, Web4 = ontology, Hardbound =
oversight. No documented drift variant appears anywhere. "Production ready" absent.
"Unification" appears twice, negated both times in favour of fractal leverage.

Per the standing lesson that Pass-2 "expansions clean" is *never* evidence about
glosses, the real work this session was all Layer 2 — right term, wrong object:

- `app/page.tsx`: LCT card said *"Self-grounding — no issuing authority"* two words
  after *"grounded in witnessed history"* — the card contradicted itself about whether
  grounding is self- or other-sourced. **Canon-checked before editing** (per the
  standing rule that expert-persona flags carry correct diagnoses with wrong
  prescriptions): `CANONICAL_TERMS_v1.md` defines LCT as *"a verifiable digital presence
  that accumulates witnessed history and reputation over time"* and says **nothing about
  issuance** either way. So P3's "inverted" diagnosis is right about the tension but the
  fix can't come from canon. Rewrote to self-*issued* (no certificate authority) /
  other-*grounded* (witnessing comes from outside the entity) — which resolves the
  contradiction without out-specifying canon further.
- `app/projects/page.tsx`: Web4 card had zero occurrences of "witness" — LCT presented
  purely structurally as *"anchored to."* Added the witnessed-history clause, phrased as
  canon phrases it, with the "evidence rather than self-assertion" distinction that is
  the whole point of the term.
- `app/context/page.tsx`: SNARC's five dimensions were never glossed anywhere on the
  site, in the glossary row or the narrative section. "Arousal" was the one biological
  borrowing on the site carrying no hedge at point of use, and it is the one most likely
  to be misread by an outsider. Glossed all five in both places.

## Feedback Reviewed
- Visitor log: 2026-08-08 — four personas, all four would return. Strongest report the
  loop has produced: it verifies its own claims across passes and publishes what did not
  reproduce.

**The most useful thing in it is the refutation table, and I extended it.** The log
already retires eight earlier findings (T3 gloss collapse on /raising, the missing
"not gradient training" caveat, MRH dropping "relevance", equation spacing, SAGE
"Governance", repo counts, fleet anthropomorphism). Checking Tier 1 against source
before implementing — the standing rule — retired **two more, both marked CONFIRMED**:

| Tier 1 item | Claimed | Actual source state |
|---|---|---|
| 1. Restore ATP/ADP cycle on home *(P3 high, "P4 confirmed")* | home defines ATP/ADP as a one-way spend log | **Already fixed** in commit `9a84d66`. `page.tsx:387` reads *"It is a **cycle, not a one-way log**: ADP recharges back to ATP against validated value creation"* and names V3 as the gate. The visitor quoted the real substring *"intent and discharge as two distinct audit artifacts"* — which is present — and stopped reading two lines short of the correction. |
| 2. Specify the deflationary control on /raising *(P4 high)* | "named but never operationalized" | **Already specified**, at length, `raising/page.tsx:251–331`: scramble rather than generic replacement, yoked-control alternative, an explicit argument for why a bare "degrades under scramble" result adjudicates nothing, pre-registered metric and threshold fixed before looking, the vocabulary-retirement commitment, and a plainly stated status ("specified, not run, no metric yet"). |

Two of four Tier 1 items were already closed. This is now a reliable enough pattern
that it is worth naming: **a "CONFIRMED by a second pass" verdict confirms the quoted
string is present, not that the finding survives reading the surrounding paragraph.**
Verification across passes controls for fetch artifacts; it does not control for
truncated reading. Both retired items today were partial-read artifacts, not stale
fetches — a different failure mode than the one the ladder was built to catch.

## Changes Made (Site)

**Tier 1 — the one that was genuinely open:**

- `app/autonomy/page.tsx`: **published the revert record.** P4's point was correct and
  the sharpest finding in the log: detect-and-revert is an *empirical* safety claim, and
  the page argued the posture at length while publishing no numbers. Counted from actual
  history rather than asserted: **79 maintainer sessions (2026-04-23 → 2026-08-07), 117
  commits touching `src/`, zero Vercel rollbacks, zero git reverts of a shipped change.**
  (The `git log --grep=revert` hits are all prose *describing* the posture — no commit
  ever undid one.) Published with the caveat that makes it honest rather than
  self-congratulatory: a zero is weak evidence, because the only automated detector is
  the same daily loop under evaluation, so it is equally consistent with "no bad push
  shipped" and "the loop cannot see its own bad pushes." Added what the record *does*
  show — self-correction here takes the form of forward fixes in a later session, not
  rollbacks — and linked /raising's *"the first version of this section got that wrong"*
  as a worked example.

  This is deliberately the kind of fix the site is short on. The standing read is that
  hedging has hit zero marginal return and only inspectable evidence buys credibility
  now; the log's own closing line says the same thing (*"the remaining credibility gap
  is artifacts, not prose"*). A counted number with an honest reading is an artifact.
  A twelfth caveat would not have been.

**Tier 2 — correct content, wrong place:**

- `app/autonomy/page.tsx`: labelled the track registry's ATP/ADP as the *issue-and-
  discharge half* of the cycle, used one-way as a spend ledger, with a pointer to
  `/context#atp` for the recharge gate it does not implement. The description was
  accurate as implementation but unlabelled, so it read as the definition — and it sat
  on a different page from home's now-correct cycle description, which is exactly how
  the two pages came to disagree.
- `app/context/page.tsx`: broke the Synthon ↔ coherence definitional loop (found
  independently by P2, P3 and P4). The "no single operational definition yet" caveat sat
  only on `coherence`, the lower-traffic of the two terms. The Synthon row now names the
  circularity explicitly and inherits the open status rather than quietly depending on it.

**Tier 3 — discoverability:**

- `app/layout.tsx`, `components/MobileNav.tsx`, `lib/navigation.ts`: the glossary is the
  site's strongest artifact and P1 called it "the surprise of the pass" — i.e. found by
  accident, late. P2's proposed "Context & Glossary" would not fit the desktop navbar
  (8 links, `display:flex`, no wrap, `gap:1.25rem` — it would have overflowed), so:
  desktop nav reads **"Glossary"** (advertises the artifact at essentially zero width
  cost), while MobileNav and `navigation.ts` — which feed the mobile drawer, breadcrumbs
  and site search, none of them width-constrained — carry the full "Context & Glossary."

**Verified:** `npx next build` — compiled successfully, 12/12 static pages, zero errors.
(`npx` is not on the default PATH in this shell; nvm must be sourced first —
`. "$HOME/.nvm/nvm.sh"`.)

## Cross-Site Drift Alert
- None detected. No expansion-level drift exists on this site to propagate.
- Worth passing to sibling-site maintainers as a *method* note rather than a term:
  the gloss-level defects found here (ATP/ADP flattened to a spend log, LCT stated
  structurally with no witnessing, SNARC dimensions never defined) would all pass a
  grep-based expansion audit cleanly. Sibling sites auditing only expansions are
  measuring the layer that does not drift.

## Open Items (Deferred)

- **Tier 1 item 4 — hardware floor per runnable project** *(P1 high, the one Tier 1 item
  I did not close)*. Partly answered already: `projects/page.tsx:188` says *"SAGE runs on
  a single machine — no Jetson required"* and points at each repo's README. What is
  missing is a per-project floor for the other three runnable repos. Deferred rather than
  written because stating a VRAM/RAM floor I have not measured would be fabricating the
  most checkable kind of fact on the page — precisely the failure the clone-or-close
  reader would catch first. Needs a real first-run check per repo; that is a session of
  its own, not a prose edit.
- Tier 2 item 5 — gloss or reorder "execute" in the SAGE 12-step loop (execute at 7,
  PolicyGate at 11→12). Two personas reached it independently and it is safety-relevant.
  Needs the actual loop source to say whether the ordering is real or a presentation
  artifact; not a blind prose fix.
- Tier 2 item 8 — inline the operational measure and n for the identity-transfer claim
  on home.
- Tier 3 items 14–17, 21 — split the /projects Web4 card (note: today's witness clause
  made it slightly longer, so this moved up in priority), Web4 falsification note,
  lead /arc-agi-3 with what the benchmark is, link the 12-step loop to code, name the
  single trial behind Synchronism's hedge.
- Item 19 (glossary pointer on /autonomy and /principles) — **checked, already satisfied**;
  both pages carry multiple /context links. No change needed.

**Route upward, not site fixes** (carried forward, all three still open):
- The `/` glyph carries two senses in one equation and the site's notation key
  out-specifies canon. P1, P2 and P3 agree. Should be resolved in canon, not patched
  down into the site.
- CLAUDE.md's audit table lists R7 as a drift variant when canon (2026-04-27) now
  appears to treat it as co-canonical with R6. A future auditor following that table
  would flag correct usage as drift. Flagging, not self-editing.
- The priority question the visitor raised and I think is the right one: 31+ autonomous
  tracks and ~53 sessions/day are entirely maintenance, while the two experiments this
  site names as decisive — the raising deflationary control and the ARC harness ablation
  — are unscheduled. Today is a small instance of the same shape: the loop's most
  valuable output was a *counted number* that took one shell command, and its most
  common output is prose. The loop is well-tuned for finding drift and is now finding
  almost none, three sessions running. That is a signal about what to point it at next,
  not a result to keep re-confirming.
