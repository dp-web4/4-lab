# Maintainer Session — 2026-08-11

## Fleet Sync
- Manifest checked: yes (SAGE repo pulled, already up to date)
- Changes found: none — manifest still stamped 2026-03-08, still lists only 6 machines. The fleet page carries 8 (incl. HUB and pub) and is ahead of the manifest. Per standing finding: the manifest is stale on the writer side; never sync the page down to it.
- Page updated: no (nothing to follow)

## Terminology Fixes
- `src/app/fleet/page.tsx` (pub card): "against the hub + trust stack" → "against the hub and its trust components" — the sole canonical-table violation sitewide, flagged independently by Passes 2 and 3. "Stack" is banned for Web4 components; verified in source before editing. This is the second "stack" cleanup (4 fixed 2026-08-10); the pattern holds that new prose reintroduces it.
- `src/app/context/page.tsx`: added **R7** to the glossary (new row beside R6) and a paragraph in the narrative section (retitled "R6 and R7: the action framework"). Verified against CANONICAL_TERMS_v1.md before writing: canon defines R7 as "R6 + Reputation. A superset… adds reputation back-propagation," both modes first-class, choice contextual. Pass 3's point stands in canon's own words: the site describes trust tensors updated from witnessed outcomes — that loop is R7 — while previously only naming R6. Also updated the R6 row gloss to canon's scoping ("without reputation tracking", consequence-tier distinction).

## Feedback Reviewed
- Visitor log: 2026-08-11 — one drift ("trust stack" on /fleet), one canonical omission (R7), one HIGH structural finding (scramble-control blocker is definitional, split across /raising and /context), first-use ordering flags on /principles and /autonomy, nav-label duplicate, /projects onboarding gap.

## Changes Made (Site)
- `src/app/raising/page.tsx`: added the dependency sentence to the scramble-control status paragraph (Pass 4's HIGH item): the missing pre-registered metric requires an operational definition of identity continuity, and the glossary concedes "coherence" has no single operational definition yet — "the blocker is definitional before it is logistical," with a link to /context#coherence. Closes the disclosure gap where the reader had to assemble the chain across two pages.
- `src/app/context/page.tsx`: R6/R7 changes (see Terminology Fixes).
- `src/lib/navigation.ts`: fixed stale descriptions — Home said "Six machines. Fourteen repos." and Fleet said "Six machines" (pre-dating both fleet expansions; live headline is "Eight machines. Forty original repos."). Added hub/pub to Fleet keywords, r6/r7 to Glossary keywords. Self-found, not in the visitor log — these strings surface in site search results.
- `src/lib/navigation.ts` + `src/components/MobileNav.tsx`: unified the /context nav label on **"Glossary"** (was "Context & Glossary" in mobile nav + search, "Glossary" in the header). Pass 2's duplicate-label flag verified in source: both pointed at /context.
- `src/app/projects/page.tsx`: added the "start here" callout (Pass 1's onboarding gap) — points to the page's own Quick start section (gave the h3 an `id="quick-start"` anchor) and to /links. First draft claimed the ACT card carried a clone command; verified in source that clone commands live in the bottom Quick start section, not on cards, and corrected before shipping.
- Build: `npx next build` passes, 12/12 static pages, zero errors.

## Findings Against Visitor Log (stale-flag check)
- /principles T3/V3 "bare at first use": **already satisfied in source** — Principle 4's quote expands both in full (`principles/page.tsx:84`). Fetch artifact or stale.
- /autonomy V3 "used before expansion": **already satisfied in source** — the only V3 occurrence links to /context#v3 with the expansion adjacent (`autonomy/page.tsx:214`).
- Session counts (2,065 vs 1,991): synthesis already resolved it — reconciles exactly; no edit, per standing memory.

## Cross-Site Drift Alert
- "stack" applied to Web4 trust components keeps reappearing in newly written prose (second cleanup in two days). synchronism-site and sage-site maintainers should grep their pages for "stack" near Web4/trust/hub wording.
- R7 absence may also hold on sibling sites: any page that names R6 and also describes reputation/trust updates from outcomes is describing R7 without naming it. Canon (CANONICAL_TERMS_v1, 2026-04-27) makes R7 co-canonical.
- Note for the CLAUDE.md owner (not self-editing): both 4-lab CLAUDE.md files still list R7 under "NEVER use" drift for R6 — stale against canon, which now defines R7 as a first-class superset. Today's site edit follows canon.

## Open Items (Deferred)
- Assign an owner / review-by date for the scramble control (Pass 4's other HIGH half) — not a site edit; the sentence now on /raising names the definitional blocker, but ownership is a lab decision. Route upward.
- Three ahead-of-canon claims on /context (notation key, LCT non-transferability, "not a scalar" tail) — route upstream to CANONICAL_TERMS_v1; do not edit the site down. Still needs a channel.
- Publish one redacted session-log sample with coding criteria — the site remains caveat-saturated, artifact-poor; not producible by this track.
- Low-severity copyedits skipped: TPM/FIDO2 parentheticals on /arc-agi-3, ACT/ACP near-collision style note, "Hestia identity" / "crystallization" one-liners on /fleet (the Hestia gloss also touches private-context boundaries — needs a careful public-safe definition before adding).
